# Part 3 Lab：整理模型、存储与 FocusCore 边界

## 目标

把开始变复杂的数据和存储逻辑重新压回一个可解释的边界里。

## 任务

1. 识别哪些规则仍在 UI 中漂浮，应该迁进 `FocusCore`。
2. 设计一个持久化失败后的恢复路径。
3. 检查模型命名、查询入口和存储读写是否已经足够清楚。

## 复盘清单

- `FocusCore` 是不是被引入得恰到好处？
- 你是否能说清 UI 规则和领域规则的边界？
- 失败路径是在产品层被看见了吗？
