# Part 1 Lab：完成你的第一个 FocusList 冲刺

## 目标

把一个只有最小列表和导航的 `FocusList` 推进成“足够清楚、足够可运行”的 App 起点。

## 任务

1. 修一个局部状态放错位置导致的更新问题。
2. 给 `Inbox` 加一个小型输入流，让用户能新增任务。
3. 复盘：哪些状态仍然应该留在 `View`，哪些已经显得过重。

## 复盘清单

- 你是否能解释 `@State` 和 `@Bindable` 的边界？
- 你是否知道为什么教程先做 App 再补语言？
- 你是否能说清现在的 `FocusList` 还缺什么？
