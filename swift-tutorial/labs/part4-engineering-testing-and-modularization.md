# Part 4 Lab：把产品推进成真正的代码库

## 目标

通过测试、包边界和 `focusctl`，让 `FocusList` 从“能继续写”升级成“能被稳定维护”。

## 任务

1. 先补一个失败测试，再实现最小修复。
2. 检查 `FocusListApp`、`FocusCore` 和 `focusctl` 之间是否有职责串位。
3. 扩展一条 CLI 输出，让它准确复用共享核心逻辑。

## 复盘清单

- 你写的测试是在锁定行为，还是只是在凑覆盖率？
- 现在的模块边界是否能解释给别人听？
- `focusctl` 是否真的证明了共享核心的价值？
