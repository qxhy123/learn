# Part 5 Lab：并发、失败面与跨平台打磨

## 目标

让异步行为、错误反馈和平台差异一起可解释，而不是在功能堆起来后互相打架。

## 任务

1. 设计一个刷新失败但可恢复的界面路径。
2. 给批量操作加一个不会误导用户的反馈流。
3. 对照 `iOS` 和 `macOS`，检查哪些交互应该分化、哪些不该分化。

## 复盘清单

- 取消和失败是否被区分清楚？
- 性能问题是在猜，还是在观察？
- 两个平台的体验是否都保留了同一个产品中心？
