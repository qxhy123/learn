\documentclass[tikz,border=4pt]{standalone}
\usepackage{ctex}
\usepackage{amsmath}
\usetikzlibrary{calc}

\begin{document}
% 一元二次方程各项系数示意图
\begin{tikzpicture}[scale=1.0, thick]

  % 方程展示
  \node[font=\Large] (eq) at (0, 0) {$ax^2 + bx + c = 0$};

  % 括号标注各项
  % 二次项
  \draw[blue, thick, ->] (-1.8, -0.5) -- (-1.8, -1.2)
      node[below, blue, font=\small] {二次项系数 $a$\\（$a \neq 0$）};

  % 一次项
  \draw[red, thick, ->] (0.3, -0.5) -- (0.3, -1.2)
      node[below, red, font=\small] {一次项系数 $b$};

  % 常数项
  \draw[black!70, thick, ->] (1.6, -0.5) -- (1.6, -1.2)
      node[below, black!70, font=\small] {常数项 $c$};

  % 说明框
  \draw[gray, dashed, rounded corners=4pt]
    (-3.5, 0.6) rectangle (3.5, -0.6);

  % 判断三连说明
  \node[below, align=center, font=\small] at (0, -2.8)
    {判断三连：① 最高次数为 2？\quad ② 只有一个未知数？\quad ③ 整式方程？};
  \node[below, align=center, font=\small] at (0, -3.4)
    {以上均满足，且 $a \neq 0$，才是一元二次方程。};

\end{tikzpicture}
\end{document}
