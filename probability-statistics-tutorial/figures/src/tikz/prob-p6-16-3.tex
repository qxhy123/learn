\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{amsmath}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{ctex}

\begin{document}
\begin{tikzpicture}

\begin{axis}[
  width=12cm, height=8cm,
  xmin=0, xmax=115,
  ymin=0, ymax=0.35,
  xlabel={样本量 $n$},
  ylabel={$\text{Var}(\hat\theta)$},
  title={\textbf{Cramér-Rao 下界与各估计量方差}},
  axis lines=left,
  xtick={10,20,30,50,100},
  xticklabel style={font=\scriptsize},
  yticklabel style={font=\scriptsize},
  ytick={0,0.05,0.10,0.15,0.20,0.25,0.30},
  grid=major, grid style={gray!20},
  clip=false,
  legend style={
    font=\scriptsize,
    at={(1.02,1.0)},
    anchor=north west,
    draw=gray!50,
    fill=white,
    inner sep=4pt,
    row sep=2pt
  }
]

  % CR 下界曲线: 1/(n * I(theta)), 设 I(theta)=1
  \addplot[gray!70!black, very thick, domain=5:105, samples=80]
    {1/(x*1.0)};
  \addlegendentry{CR 下界 $\frac{1}{n I(\theta)}$}

  % UMVUE 估计量：贴近下界
  \addplot[blue!70!black, thick, domain=5:105, samples=80]
    {1/(x*1.0) + 0.002};
  \addlegendentry{$\hat\theta_1$（UMVUE，贴近下界）}

  % 渐近有效估计量
  \addplot[green!60!black, thick, domain=5:105, samples=80]
    {1/(x*0.85)};
  \addlegendentry{$\hat\theta_2$（渐近有效）}

  % 低效估计量：明显高于下界
  \addplot[orange!80!black, thick, domain=5:105, samples=80]
    {1/(x*0.5)};
  \addlegendentry{$\hat\theta_3$（低效估计量）}

  % 标记若干数据点
  \addplot[only marks, mark=*, mark size=2.5pt, blue!70!black] coordinates {
    (10,0.102)(20,0.052)(30,0.035)(50,0.022)(100,0.011)
  };
  \addplot[only marks, mark=square*, mark size=2.5pt, orange!80!black] coordinates {
    (10,0.202)(20,0.102)(30,0.068)(50,0.042)(100,0.021)
  };

  % 标注 Fisher 信息量：移到左侧中部空白区（小 n 处曲线已快速下降后的空白）
  % 在 x=15~40, y=0.20~0.32 之间的区域放置，避开所有曲线
  \node[draw, fill=yellow!15, rounded corners=3pt, font=\small, align=center,
        inner sep=5pt]
    at (axis cs:28, 0.275) {
      Fisher 信息量\\[2pt]
      $I(\theta)=-E\!\left[\dfrac{\partial^2 \ln f}{\partial\theta^2}\right]$
    };

  % 标注下界区域：放在曲线右侧端点附近，且在曲线下方
  \node[font=\scriptsize, gray!70!black, anchor=west]
    at (axis cs:106, 0.010) {CR 下界};

\end{axis}

\end{tikzpicture}
\end{document}
