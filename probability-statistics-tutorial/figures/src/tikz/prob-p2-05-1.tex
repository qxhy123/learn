\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{ctex}
\begin{document}
\begin{tikzpicture}
\begin{axis}[
  width=10cm, height=6cm,
  xmin=-3.5, xmax=3.5,
  ymin=-0.04, ymax=0.48,
  xlabel={$x$},
  ylabel={$f(x)$},
  title={\textbf{标准正态 PDF 与区间概率（面积）}},
  axis lines=left,
  xtick={-3,-2,-1,0,1,2,3},
  xticklabels={$-3$,$-2$,$-\sigma$,$0$,$+\sigma$,$+2\sigma$,$3$},
  ytick={0,0.1,0.2,0.3,0.4},
  yticklabel style={font=\scriptsize},
  xticklabel style={font=\scriptsize},
  grid=major, grid style={gray!30},
  clip=false
]
  % Shade area between -1 and 1
  \addplot[fill=blue!25, opacity=0.7, draw=none, domain=-1:1, samples=80]
    {exp(-x^2/2)/sqrt(2*pi)} \closedcycle;

  % Shade area between 1 and 2
  \addplot[fill=orange!30, opacity=0.7, draw=none, domain=1:2, samples=40]
    {exp(-x^2/2)/sqrt(2*pi)} \closedcycle;

  % Main curve
  \addplot[blue!70!black, ultra thick, domain=-3.5:3.5, samples=120]
    {exp(-x^2/2)/sqrt(2*pi)};

  % Annotation for shaded areas
  \node[font=\scriptsize, blue!70!black] at (axis cs:0,0.18) {$\approx 68.3\%$};
  \node[font=\scriptsize, orange!70!black] at (axis cs:1.5,0.08) {$\approx 13.6\%$};

  % Dashed vertical lines
  \addplot[dashed, gray, thick] coordinates {(-1,0) (-1,{exp(-0.5)/sqrt(2*pi)})};
  \addplot[dashed, gray, thick] coordinates {(1,0) (1,{exp(-0.5)/sqrt(2*pi)})};
  \addplot[dashed, gray, thick] coordinates {(2,0) (2,{exp(-2)/sqrt(2*pi)})};

  % Labels on x-axis are handled via xticklabels above

  % Formula — placed outside axis at top-right to avoid curve overlap
  \node[font=\small, align=center, fill=white, draw=gray!60, thin,
        rounded corners=2pt, inner sep=4pt]
    at (axis cs:2.6,0.44) {$f(x)=\dfrac{1}{\sqrt{2\pi}}e^{-x^2/2}$};

\end{axis}
\end{tikzpicture}
\end{document}
