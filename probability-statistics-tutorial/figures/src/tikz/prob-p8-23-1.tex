\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{ctex}
\usetikzlibrary{calc, positioning, arrows.meta, shapes, fit, backgrounds}

\begin{document}
\begin{tikzpicture}

\begin{axis}[
  name=ax1,
  width=11cm, height=7cm,
  xmode=log,
  xmin=8, xmax=12000,
  ymin=0, ymax=0.38,
  xlabel={样本量 $N$},
  ylabel={标准误差},
  axis lines=left,
  xtick={10, 100, 1000, 10000},
  xticklabels={$10$, $10^2$, $10^3$, $10^4$},
  xticklabel style={font=\small},
  yticklabel style={font=\small},
  ytick={0, 0.05, 0.10, 0.15, 0.20, 0.25, 0.30, 0.35},
  grid=major, grid style={gray!30},
  clip=true,
  legend style={at={(1.02,1.0)}, anchor=north west, font=\small,
                fill=white, draw=gray!50, thin, rounded corners=2pt},
]

% ±2σ 阴影带 (理论曲线的上下界)
\addplot[fill=blue!15, draw=none, opacity=0.7,
  domain=10:10000, samples=60]
  {2 * 0.5 / sqrt(x)} \closedcycle;
\addplot[fill=white, draw=none, domain=10:10000, samples=60]
  {0.5 / sqrt(x) / 3} \closedcycle;

% 上界线
\addplot[blue!20, thin, domain=10:10000, samples=60]
  {2 * 0.5 / sqrt(x)};

% 理论曲线 1/sqrt(N)
\addplot[red, dashed, very thick, domain=10:10000, samples=80]
  {0.5 / sqrt(x)};
\addlegendentry{理论 $\sigma/\sqrt{N}$（$\sigma=0.5$）};

% 模拟数据点（手工给定，模拟散点）
\addplot[blue!70!black, only marks, mark=*, mark size=2.2pt] coordinates {
  (10,   0.158)
  (20,   0.118)
  (50,   0.072)
  (100,  0.053)
  (200,  0.038)
  (500,  0.024)
  (1000, 0.017)
  (2000, 0.011)
  (5000, 0.007)
  (10000,0.005)
};
\addlegendentry{蒙特卡洛估计误差};

% ±2σ 图例（手工）
\addplot[fill=blue!15, opacity=0.6, draw=none] coordinates {(0,0)};
\addlegendentry{$\pm 2\sigma$ 范围};

\end{axis}

% 标题
\node[font=\large\bfseries] at ($(ax1.north)+(0,0.5cm)$)
  {蒙特卡洛收敛：标准误差 $\propto 1/\sqrt{N}$（维度无关）};

% 底部注释框
\node[font=\small, align=center,
      fill=white, draw=gray!50, thin, rounded corners=2pt, inner sep=5pt]
  at ($(ax1.south)+(0,-0.9cm)$)
  {维度无关性：无论积分维度 $d$ 为多少，标准误差始终 $\propto 1/\sqrt{N}$};

\end{tikzpicture}
\end{document}
