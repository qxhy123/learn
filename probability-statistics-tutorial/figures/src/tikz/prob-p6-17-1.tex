\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{amsmath}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{ctex}

\begin{document}
\begin{tikzpicture}

\begin{axis}[
  width=11cm, height=7cm,
  xmin=-3.8, xmax=3.8,
  ymin=-0.06, ymax=0.65,
  xlabel={$z$},
  ylabel={$\phi(z)$},
  title={\textbf{置信区间：标准正态分布的 $1-\alpha$ 置信区域}},
  axis lines=left,
  xtick={-1.96, 0, 1.96},
  xticklabels={$-z_{\alpha/2}$, $0$, $z_{\alpha/2}$},
  xticklabel style={font=\small},
  yticklabel style={font=\scriptsize},
  ytick={0, 0.1, 0.2, 0.3, 0.4},
  grid=major, grid style={gray!20},
  clip=false
]

  % 左侧尾部红色阴影（alpha/2）
  \addplot[fill=red!40, opacity=0.8, draw=none, domain=-3.8:-1.96, samples=60]
    {exp(-x^2/2)/sqrt(2*3.14159)} \closedcycle;

  % 右侧尾部红色阴影（alpha/2）
  \addplot[fill=red!40, opacity=0.8, draw=none, domain=1.96:3.8, samples=60]
    {exp(-x^2/2)/sqrt(2*3.14159)} \closedcycle;

  % 中央蓝色阴影（1 - alpha = 0.95）
  \addplot[fill=blue!30, opacity=0.7, draw=none, domain=-1.96:1.96, samples=80]
    {exp(-x^2/2)/sqrt(2*3.14159)} \closedcycle;

  % 正态曲线
  \addplot[blue!70!black, ultra thick, domain=-3.8:3.8, samples=120]
    {exp(-x^2/2)/sqrt(2*3.14159)};

  % 垂直虚线
  \addplot[red!70!black, thick, dashed] coordinates {(-1.96, 0) (-1.96, 0.058)};
  \addplot[red!70!black, thick, dashed] coordinates {(1.96, 0) (1.96, 0.058)};

  % 中央区域标注
  \node[font=\small, blue!70!black] at (axis cs:0, 0.18)
    {$1-\alpha = 0.95$};

  % 尾部标注
  \node[font=\scriptsize, red!70!black] at (axis cs:-2.8, 0.05)
    {$\frac{\alpha}{2}=2.5\%$};
  \node[font=\scriptsize, red!70!black] at (axis cs:2.8, 0.05)
    {$\frac{\alpha}{2}=2.5\%$};

  % 上方大注释框（推高避免与曲线顶部重叠）
  \node[draw, fill=yellow!15, rounded corners=4pt, font=\small, align=center]
    at (axis cs:0, 0.52)
    {$P(-z_{\alpha/2} \leq Z \leq z_{\alpha/2}) = 1-\alpha = 0.95$\\
     $z_{\alpha/2} = z_{0.025} = 1.96$};

  % 数值标注（公式框已含 z=1.96，此处省略重复标注）

\end{axis}

\end{tikzpicture}
\end{document}
