\documentclass[tikz,border=6pt]{standalone}
\usepackage{ctex}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{calc,angles,quotes,arrows.meta,positioning,decorations.markings}
\begin{document}
\begin{tikzpicture}
\begin{axis}[
  width=13cm, height=9cm,
  xmin=-4.3, xmax=4.3, ymin=-0.6, ymax=4.5,
  axis lines=middle,
  xlabel={$x$}, ylabel={$y$},
  xlabel style={at={(axis description cs:1,0.13)},anchor=north east},
  ylabel style={at={(axis description cs:0.5,1)},anchor=south east},
  xtick={-4,-3,-2,-1,1,2,3,4}, ytick={1,2,3,4},
  tick label style={font=\small},
  domain=-4.2:4.2, samples=400,
  clip=false,
  legend style={at={(0.03,0.97)},anchor=north west,font=\small,
                fill=white,fill opacity=0.85,text opacity=1,draw=gray},
]
  % max(x,0) = ReLU broken line
  \addplot[very thick, red!80!black] coordinates {(-4.2,0) (0,0) (4.2,4.2)};
  \addlegendentry{$\max(x,0)$（ReLU 折线）}

  % LSE(x,0) = softplus = ln(e^x + 1), smooth approximation
  \addplot[very thick, blue] {ln(exp(x)+1)};
  \addlegendentry{$\mathrm{LSE}(x,0)=\ln(e^{x}+1)$}

  % corner of ReLU at origin
  \addplot[only marks, mark=*, mark size=2pt, red!80!black] coordinates {(0,0)};
  % softplus value at x=0: ln 2
  \addplot[only marks, mark=*, mark size=2pt, blue] coordinates {(0,0.6931)};
  \node[blue, font=\footnotesize, anchor=west] at (axis cs:0.12,0.78)
    {$\ln 2\approx0.69$};

  % annotation box (semi-transparent), placed lower-right, avoiding curves' peak
  \node[draw, rounded corners, fill=yellow!18, fill opacity=0.80, text opacity=1,
        align=left, font=\small, inner sep=5pt]
    at (axis cs:2.45,1.15)
    {LSE 是 $\max$ 的\\\textbf{光滑近似}：\\ 拐角被磨圆，\\ 两端贴合折线};
\end{axis}
\end{tikzpicture}
\end{document}
