# 第5章：函数变换、比较与增长层级

> 对数函数看起来增长很慢，但这种“慢”正是它在比较尺度时最有价值的地方。

## 学习目标

完成本章学习后，你将能够：

1. 分析对数函数的平移、伸缩与复合变换
2. 比较不同底数对数函数的大小
3. 理解对数增长慢于任意正幂函数
4. 识别对数图像中的关键结构
5. 用对数视角理解增长层级

---

## 正文内容

### 5.1 基本变换

函数

$$
y=\log_a(x-h)+k
$$

可看作 $y=\log_a x$ 的平移：

- $h$ 控制左右平移
- $k$ 控制上下平移
- 定义域变为 $x-h>0$

例如：

$$
y=\log_2(x-3)+1
$$

的定义域是：

$$
x>3
$$

竖直渐近线是 $x=3$。

### 5.2 伸缩与反射

函数

$$
y=c\log_a x
$$

会改变纵向尺度：

- $c>0$：纵向放大或缩小
- $c<0$：关于 $x$ 轴反射

而

$$
y=\log_a(bx)
$$

可以通过运算律写成：

$$
\log_a b+\log_a x
$$

所以它本质上对应纵向平移。

### 5.3 不同底数之间的比较

换底公式给出：

$$
\log_a x=\frac{\ln x}{\ln a}
$$

当 $x>1$ 且 $a,b>1$ 时，底数越大，对数值越小。例如：

$$
\log_2 8=3,\qquad \log_4 8=\frac32
$$

因为以更大的底数达到同一个数，需要的指数更少。

### 5.4 对数增长为什么很慢

对数函数会不断增长，但增长速度越来越慢。一个重要层级关系是：

$$
\ln x \ll x^\alpha \ll a^x
$$

其中 $\alpha>0,\ a>1$。这句话的意思是：当 $x$ 足够大时，对数增长慢于任意正幂函数，而幂函数又慢于指数函数。

---

## 例题

**问题**：函数 $y=\log_3(x+2)-4$ 的定义域和渐近线是什么？

**解答**：

要求真数为正：

$$
x+2>0
$$

所以定义域为 $x>-2$，竖直渐近线为：

$$
x=-2
$$

---

## 常见误区与检查清单

- 是否把 $\log_a(x-h)$ 的定义域误写成 $x>0$？
- 是否忘记水平平移会移动渐近线？
- 是否以为底数越大，对数值一定越大？
- 是否把“增长慢”误解成“不会增长”？

---

## 本章小结

| 主题 | 结论 |
|------|------|
| 平移 | $\log_a(x-h)+k$ 改变定义域和位置 |
| 渐近线 | 真数为 0 的位置形成竖直渐近线 |
| 底数比较 | 对同一 $x>1$，底数越大，对数越小 |
| 增长层级 | $\ln x$ 慢于任意正幂函数 |
| 核心直觉 | 对数擅长压缩尺度而不是放大差异 |

---

## 练习题

1. 求 $y=\log_2(x-5)$ 的定义域和渐近线。
2. 比较 $\log_2 16$ 与 $\log_4 16$。
3. 说明为什么 $\ln x$ 虽然增长很慢，但仍会趋向无穷。
4. 判断 $y=-\log_3 x$ 的图像与 $y=\log_3 x$ 的关系。
5. 用一句话解释 $\ln x\ll x^2$ 的含义。
