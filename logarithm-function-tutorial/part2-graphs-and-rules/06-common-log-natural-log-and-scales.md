# 第6章：常用对数、自然对数与对数尺度

> 常用对数强调数量级，自然对数连接连续变化；它们是同一种对数语言的不同坐标。

## 学习目标

完成本章学习后，你将能够：

1. 区分常用对数 $\lg x$ 与自然对数 $\ln x$
2. 理解不同底数对数之间只差常数比例
3. 解释分贝、pH、震级等对数尺度的思想
4. 知道自然对数为什么在微积分中重要
5. 用对数尺度描述巨大跨度的数据

---

## 正文内容

### 6.1 常用对数：以 10 为底

常用对数记作：

$$
\lg x=\log_{10}x
$$

它非常适合描述十进制数量级：

$$
\lg 10^n=n
$$

例如：

$$
\lg 1000=3
$$

表示 1000 是 $10^3$ 这个数量级。

### 6.2 自然对数：以 $e$ 为底

自然对数记作：

$$
\ln x=\log_e x
$$

它在微积分中尤其自然，因为：

$$
\frac{d}{dx}\ln x=\frac1x
$$

并且指数函数可以统一写成：

$$
a^x=e^{x\ln a}
$$

这说明自然对数是连接任意底数指数函数与自然指数函数的桥梁。

### 6.3 不同底数只是单位不同

由换底公式：

$$
\log_a x=\frac{\ln x}{\ln a}
$$

可知同一个数的不同底数对数只差一个固定比例。就像长度可以用米、厘米或英寸表示，对数也可以用不同底数表示同一尺度信息。

### 6.4 对数尺度的现实例子

很多现实量跨度巨大，用原始尺度很难比较，因此采用对数尺度：

| 场景 | 对数思想 |
|------|----------|
| 分贝 | 声强比值取对数 |
| pH | 氢离子浓度的负常用对数 |
| 地震震级 | 能量或振幅的对数尺度 |
| 天文亮度 | 亮度跨度过大，需要压缩尺度 |
| 数据可视化 | 半对数图或双对数图显示增长规律 |

对数尺度不是为了“让数变小”而已，而是为了比较倍率、数量级和相对变化。

---

## 例题

**问题**：如果某个量从 $10^3$ 增加到 $10^7$，常用对数增加了多少？

**解答**：

$$
\lg 10^7-\lg 10^3=7-3=4
$$

这表示数量级增加了 4 个十倍层级，也就是整体放大了 $10^4$ 倍。

---

## 常见误区与检查清单

- 是否把 $\lg x$ 与 $\ln x$ 混淆？
- 是否以为换底会改变数学本质？
- 是否只看原始差值，而忽略倍率变化？
- 是否理解 pH、分贝等不是线性尺度？

---

## 本章小结

| 主题 | 结论 |
|------|------|
| 常用对数 | $\lg x=\log_{10}x$，适合十进制数量级 |
| 自然对数 | $\ln x=\log_e x$，适合连续变化和微积分 |
| 换底 | 不同底数只差常数比例 |
| 对数尺度 | 用于比较跨度巨大或按倍率变化的量 |
| 核心联系 | 常用对数重数量级，自然对数重连续结构 |

---

## 练习题

1. 计算 $\lg 10^6$。
2. 用换底公式写出 $\log_2 x$ 与 $\ln x$ 的关系。
3. 说明为什么 pH 适合用对数尺度。
4. 判断：自然对数只是常用对数的另一种记号。这个说法是否准确？
5. 举一个适合使用半对数图展示的数据场景。
