\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{ctex}
\usetikzlibrary{calc,positioning,arrows.meta,trees}

% LC 98 Validate BST
% 三棵树对比：合法BST / 非法(right.left < root) / 非法(left > root)

\begin{document}
\begin{tikzpicture}

%% ── 标题 ──────────────────────────────────────────────────
\node[font=\large\bfseries] at (5.5, 0.8)
  {LC 98：验证二叉搜索树（Validate BST）};
\node[font=\small,gray] at (5.5, 0.35)
  {关键：每个节点须满足\textbf{全局}上下界，而非仅与直接父节点比较};

%% ══════════════════════════════════════════════════════════
%%  左树：合法 BST（根5，左3，右7）
%% ══════════════════════════════════════════════════════════
\begin{scope}[xshift=0cm]
  % 树标题
  \node[font=\normalsize\bfseries,green!60!black] at (1.2,-0.5)
    {合法 BST \checkmark};

  % 节点
  \node[draw,circle,minimum size=0.72cm,fill=green!10,font=\bfseries]
    (a5) at (1.2,-1.5) {5};
  \node[draw,circle,minimum size=0.72cm,fill=green!10,font=\bfseries]
    (a3) at (0.0,-2.9) {3};
  \node[draw,circle,minimum size=0.72cm,fill=green!10,font=\bfseries]
    (a7) at (2.4,-2.9) {7};

  % 边
  \draw[->,>=Stealth,thick] (a5) -- (a3);
  \draw[->,>=Stealth,thick] (a5) -- (a7);

  % 性质标注
  \node[draw=gray!50,fill=white,thin,rounded corners=2pt,
        font=\tiny,inner sep=2pt] at (0.0,-3.8) {$3 < 5$ \checkmark};
  \node[draw=gray!50,fill=white,thin,rounded corners=2pt,
        font=\tiny,inner sep=2pt] at (2.4,-3.8) {$7 > 5$ \checkmark};

  % 不等式链
  \node[draw=gray!50,fill=green!5,thin,rounded corners=2pt,
        font=\scriptsize,inner sep=3pt,align=center]
    at (1.2,-4.8)
    {$3 < 5 < 7$\\全局有序 \checkmark};
\end{scope}

%% ══════════════════════════════════════════════════════════
%%  中树：非法 BST（根5，左3，右7；右子节点7的左孩子=4）
%%  问题：4 < 5 = root，但 4 在根的右子树 → 违法
%% ══════════════════════════════════════════════════════════
\begin{scope}[xshift=4.2cm]
  \node[font=\normalsize\bfseries,red!70!black] at (1.5,-0.5)
    {非法 BST $\times$};
  \node[font=\tiny,red!60!black] at (1.5,-0.9)
    {right.left $<$ root};

  \node[draw,circle,minimum size=0.72cm,fill=blue!10,font=\bfseries]
    (b5) at (1.5,-1.5) {5};
  \node[draw,circle,minimum size=0.72cm,fill=blue!10,font=\bfseries]
    (b3) at (0.0,-2.9) {3};
  \node[draw,circle,minimum size=0.72cm,fill=blue!10,font=\bfseries]
    (b7) at (3.0,-2.9) {7};
  \node[draw,circle,minimum size=0.72cm,fill=red!15,font=\bfseries]
    (b4) at (2.1,-4.3) {4};

  \draw[->,>=Stealth,thick] (b5) -- (b3);
  \draw[->,>=Stealth,thick] (b5) -- (b7);
  \draw[->,>=Stealth,thick] (b7) -- (b4);

  % 正常关系
  \node[draw=gray!50,fill=white,thin,rounded corners=2pt,
        font=\tiny,inner sep=2pt] at (0.0,-3.8) {$3 < 5$ \checkmark};
  \node[draw=gray!50,fill=white,thin,rounded corners=2pt,
        font=\tiny,inner sep=2pt] at (3.0,-3.8) {$7 > 5$ \checkmark};

  % 违法标注
  \node[font=\tiny,red!80!black,align=center]
    at (2.1,-5.05) {$4 < 5$=root\\但在右子树！$\times$};

  % 说明
  \node[draw=gray!50,fill=white,thin,rounded corners=2pt,
        font=\scriptsize,inner sep=3pt,align=center]
    at (1.5,-6.1)
    {仅看父节点 $4 < 7$ \checkmark\\
     全局下界 $>5$ 被违反 $\times$};
\end{scope}

%% ══════════════════════════════════════════════════════════
%%  右树：非法 BST（根5，左8，右7）
%%  问题：左子节点 8 > 5 = root → 违法
%% ══════════════════════════════════════════════════════════
\begin{scope}[xshift=9.5cm]
  \node[font=\normalsize\bfseries,red!70!black] at (1.2,-0.5)
    {非法 BST $\times$};
  \node[font=\tiny,red!60!black] at (1.2,-0.9)
    {left $>$ root};

  \node[draw,circle,minimum size=0.72cm,fill=blue!10,font=\bfseries]
    (c5) at (1.2,-1.5) {5};
  \node[draw,circle,minimum size=0.72cm,fill=red!15,font=\bfseries]
    (c8) at (0.0,-2.9) {8};
  \node[draw,circle,minimum size=0.72cm,fill=blue!10,font=\bfseries]
    (c7) at (2.4,-2.9) {7};

  \draw[->,>=Stealth,thick] (c5) -- (c8);
  \draw[->,>=Stealth,thick] (c5) -- (c7);

  % 违法标注
  \node[font=\tiny,red!80!black,align=center]
    at (0.0,-3.85) {$8 > 5$=root\\在左子树！$\times$};

  % 右节点
  \node[draw=gray!50,fill=white,thin,rounded corners=2pt,
        font=\tiny,inner sep=2pt] at (2.4,-3.8) {$7 > 5$ \checkmark};

  % 说明
  \node[draw=gray!50,fill=white,thin,rounded corners=2pt,
        font=\scriptsize,inner sep=3pt,align=center]
    at (1.2,-5.1)
    {左子节点 $8 > 5$\\
     违反 BST 定义 $\times$};
\end{scope}

%% ── 分隔线 ────────────────────────────────────────────────
\draw[gray!35,dashed,thick] (3.9, 0.1) -- (3.9,-7.5);
\draw[gray!35,dashed,thick] (9.2, 0.1) -- (9.2,-7.5);

%% ── 验证算法说明 ──────────────────────────────────────────
\node[draw=gray!50,fill=white,thin,rounded corners=3pt,
      font=\scriptsize,inner sep=5pt,align=left]
  at (5.5,-8.3)
  {\textbf{递归验证算法}：\texttt{isValid(node, min=-inf, max=+inf)}\\
   \quad 若 node=null $\Rightarrow$ true\\
   \quad 若 node.val $\leq$ min 或 node.val $\geq$ max $\Rightarrow$ false\\
   \quad 递归：left 传入 (min, node.val)，right 传入 (node.val, max)};

\end{tikzpicture}
\end{document}
