{"id":"u4","lessons":[{"id":"u4-l1","title":"单机训练基线","subtitle":"从 DataLoader 到 checkpoint 的完整闭环","intro":[{"title":"为什么单机训练是一切的原点","body":"单机训练不是「规模小的训练」，而是训练系统的**最小闭环**。上分布式之前，如果单机的数据路径、显存账本、精度策略、profiler 链条、checkpoint 协议没有建立，DDP/FSDP/TP 只会把问题复制到更多 rank 上。\n\n单机的价值在于**变量少**：没有跨节点网络，没有 collective 退化，也没有大规模 rank failure，因此因果链清晰可查。"},{"title":"Training Step 的四条路径","body":"一个完整 training step 可按责任拆成四条路径：\n\n- **控制路径**：launcher、配置、precision policy、checkpoint interval、resume 逻辑\n- **数据路径**：`Storage → page cache → DataLoader worker → CPU 预处理 → pinned memory → H2D → GPU HBM`\n- **状态路径**：model params、gradients、optimizer states、scheduler、RNG、dataset cursor、`optimizer_step` / `microstep_idx`\n- **故障路径**：OOM、NaN、DataLoader hang、I/O stall、checkpoint stall\n\n很多「GPU 低利用率」实际是数据路径问题；很多「NaN」实际是 precision policy 问题。四条路径必须分开诊断。"},{"title":"microstep_idx 与 optimizer_step 的区别","body":"这是最常被混淆的两个计数器：\n\n- `microstep_idx`：每执行一次 forward/backward 就递增，对应 DataLoader 进度和 gradient accumulation substep\n- `optimizer_step`：**只在** accumulation boundary、梯度检查通过、optimizer update **完成后**才递增\n\nFP16 `GradScaler` 检测到 overflow 时会跳过 `optimizer.step()`。此时 `microstep_idx` 可以增加，`optimizer_step` **不能增加**，否则 tokens/s 看起来正常但实际 update 数少了。\n\nCheckpoint 默认只在 `optimizer_step` 边界保存，这样不需要存储半累计梯度。","tip":"resume 时必须同时恢复 optimizer_step 和 microstep_idx，否则 LR schedule 和 dataset cursor 都会错位。"},{"title":"显存账本：五项必填","body":"单机 OOM 只靠猜是最大反模式。完整显存预算公式：\n\n`M_peak = params + grads + optimizer_states + activations + temp + fragmentation`\n\n以 7B 模型 BF16 + AdamW FP32 m/v 为例（每张 80 GB H100）：\n\n| 项目 | 大小 |\n|---|---|\n| BF16 params | ~12.5 GiB |\n| BF16 grads | ~12.5 GiB |\n| FP32 Adam m/v | ~49.9 GiB |\n| 合计（含 activation 前）| ~74.9 GiB |\n\n已超 80 GB 额度，还没算 activation。这就是为什么 7B 必须用 `FSDP FULL_SHARD` 或 `ZeRO-3` 而不是朴素 DDP。\n\n生产准入规则：`M_peak_P95 ≤ 0.85 × HBM`，留 15% 给 allocator、shape 抖动和临时 kernel workspace。","tip":"Activation 是最容易被低估的一项，它随 microbatch × sequence_length × hidden_size × layers 增长。OOM 若与 batch/seq 线性相关，优先怀疑 activation。"},{"title":"MFU、HFU、tokens/s 的关系","body":"这三个指标回答不同问题，不能互相替代：\n\n- **tokens/s**（`non_pad_tokens/s`）：有效训练数据吞吐，需区分 `raw_sequence_slots/s`、`compute_tokens/s`、`non_pad_tokens/s`、`loss_tokens/s`\n- **MFU**（Model FLOPs Utilization）= 有效模型 FLOPs/s ÷ 硬件峰值 FLOPs/s，衡量**有效训练进展**\n- **HFU**（Hardware FLOPs Utilization）= 实际执行 FLOPs/s ÷ 峰值，包含 padding 浪费和 activation checkpointing 重算\n\nDecoder-only Transformer 的 FLOPs/token 近似 `6 × N_params`。\n\nLLaMA-7B（6.7B 参数）on 8×H100 BF16 示例：若 `non_pad_tokens/s = 95,000`，则\n`MFU = 95,000 × 40.2B / (8 × 989 TFLOPS) ≈ 48.3%`\n\n**GPU utilization 98% 但 MFU 25%** 是非常常见的现象，说明硬件忙但没做有效模型计算。","tip":"Activation checkpointing 会增加重算 FLOPs，可能让 HFU 升高但 MFU 和 tokens/s 持平甚至下降。"},{"title":"精度策略：BF16 / FP16 / FP8","body":"精度不是一个布尔开关，必须拆成四层记录：\n\n1. **Compute autocast**：matmul/linear 进入 BF16 Tensor Core；norm/softmax/reduction 可能保留 FP32\n2. **Parameters**：FSDP `param_dtype=bf16` 只说明 shard/通信工作集 dtype\n3. **Gradients**：gradient bucket 或 reduce dtype，clip/norm 统计可能需要 FP32 语义\n4. **Optimizer state**：AdamW `exp_avg`/`exp_avg_sq` 通常保持 FP32\n\n**BF16** 是大模型训练的稳健默认：exponent 范围接近 FP32，无需 loss scaling，训练稳定性优于 FP16。\n\n**FP16** 需要 `GradScaler`：scale 持续下降、`found_inf` 频繁出现是溢出信号；overflow 时跳过 optimizer step，但 `microstep_idx` 已递增。\n\n**FP8** 需要 H100 硬件 + Transformer Engine + amax/scale lifecycle，`amax history`、`scale`、`scale_inv` 都是训练状态，resume 时必须恢复，否则前几步会用错误 scale。"},{"title":"DataLoader 数据路径的四个关键事实","body":"DataLoader 是一个**带预取的多进程 producer-consumer 系统**，解决 CPU 预处理异步化和批次预取，但无法解决远端存储慢、单样本 decode 太重和 padding 效率低。\n\n四个常被忽视的事实：\n\n- `num_workers` 增加并行 CPU 预处理能力，**不会提高磁盘 I/O 吞吐**\n- `pin_memory=True` 必须与 `non_blocking=True` 和 dedicated copy stream 配套才能真正 overlap H2D\n- `loss.item()`、`tensor.cpu()` 会触发 GPU→CPU 同步，散落在训练循环里会把异步执行变串行\n- NUMA 错配会让 CPU worker 在一个 socket 上备数据，却通过另一个 socket 连接的 PCIe 送 GPU，拉低吞吐","tip":"DataLoader visible wait 应低于 step time 的 5%。若超标，先用 torch.profiler 确认来自 read/preprocess/collate 哪段，再单变量调整。"},{"title":"Profiler 工具链：逐层收窄","body":"诊断从症状到根因，推荐工具链顺序：\n\n1. `torch.profiler` → 定位 PyTorch 阶段（DataLoader wait、forward、backward、optimizer、checkpoint）\n2. `Nsight Systems` → 确认 CUDA stream 时间线和 H2D overlap\n3. `Nsight Compute` → 对坏 kernel 做 SM occupancy、Tensor Core、memory stall 分析\n4. `DCGM` → 验证 GPU health、SM active/tensor active、PCIe/NVLink、功耗\n5. `iostat` / `perf` / `pidstat` → 检查 host 侧数据路径\n\n**不要一开始就用 Nsight Compute 抓全量训练**。先用 torch.profiler 找到阶段，再针对具体 kernel 分析。","tip":"profile window 必须避开 warmup 和首步——首步含 CUDA context 初始化、kernel autotune、allocator 初始扩张等一次性开销，放入统计会误导 admission。"},{"title":"单机 Baseline 准入 Checklist","body":"单机 baseline 不是「先跑一下」，而是**可审计的证据包**，必须包含：\n\n- HBM peak P95 < `0.85 × capacity`\n- DataLoader visible wait < 5%（稳态 step）\n- `raw_sequence_slots/s`、`compute_tokens/s`、`non_pad_tokens/s`、`loss_tokens/s` 四项齐全\n- MFU ≥ 45%，并注明 FLOPs 口径和 token 分母\n- checkpoint 可以 strict resume：loss、LR、`optimizer_step`、`microstep_idx`、dataset cursor 连续\n- step time P99 无未解释尖峰\n\n没有这份证据包，进入多节点扩展等于带着未知 bug 扩容。","reveal":{"q":"为什么 LLaMA-7B 用朴素 DDP + AdamW 在 8×80 GB H100 上通常无法满足准入要求？","a":"DDP 每卡复制完整 params（12.5 GiB BF16）+ grads（12.5 GiB）+ Adam m/v（49.9 GiB FP32）= 74.9 GiB，还未算 activation。生产 usable HBM = 80 × 0.85 = 68 GiB，已超限。必须用 FSDP FULL_SHARD 或 ZeRO-3 做状态分片。"}}],"questions":[{"type":"choice","prompt":"LLaMA-7B（6.7B 参数）用 BF16 params + BF16 grads + FP32 AdamW m/v 训练，每卡的「参数+梯度+optimizer state」合计约多少 GiB？","options":["约 26 GiB","约 50 GiB","约 75 GiB","约 13 GiB"],"answer":2,"explain":"BF16 params ≈ 12.5 GiB，BF16 grads ≈ 12.5 GiB，FP32 Adam m/v（每参数 8 bytes）≈ 49.9 GiB，合计约 74.9 GiB，超过 80 GB H100 的 0.85 准入线（68 GiB）。","id":"u4-l1-q1"},{"type":"judge","prompt":"GPU utilization 达到 98% 就说明 MFU 也很高，可以作为训练效率良好的充分证据。","answer":false,"explain":"GPU utilization 只反映 GPU 是否忙，不说明忙的是有效模型 FLOPs 还是 kernel launch gap、padding 浪费、unfused optimizer 等无效计算。MFU = 有效模型 FLOPs / 峰值，需要单独计算。","id":"u4-l1-q2"},{"type":"choice","prompt":"FP16 训练中 GradScaler 检测到 overflow，此时应如何处理 microstep_idx 和 optimizer_step？","options":["两个计数器都不递增","microstep_idx 可递增，optimizer_step 不递增","两个计数器都递增","optimizer_step 递增，microstep_idx 不递增"],"answer":1,"explain":"microstep 已经消费了数据并执行了 forward/backward，所以 microstep_idx 可以递增；但 optimizer 实际 update 被跳过，optimizer_step 不应递增，否则 LR schedule 和 tokens/s 统计都会错误。","id":"u4-l1-q3"},{"type":"match","prompt":"将左侧工具与其主要回答的问题匹配","left":["torch.profiler","Nsight Systems","Nsight Compute","iostat"],"right":["PyTorch 各 op 的 CPU/CUDA 时间和 memory 分配","CUDA stream 时间线、H2D DMA、kernel launch gap","单个 kernel 的 SM occupancy 和 Tensor Core 使用率","本地磁盘 I/O 队列深度和 await 延迟"],"explain":"四个工具分层诊断：torch.profiler 对应 PyTorch 层，Nsight Systems 对应 CUDA stream/timeline 层，Nsight Compute 对应 kernel 内部性能计数器，iostat 对应 host 侧磁盘 I/O。","id":"u4-l1-q4"},{"type":"input","prompt":"activation checkpointing 开启后，HFU 升高但 MFU 和 tokens/s 基本不变，原因是什么？（填核心原因，不超过 15 字）","accept":["重算增加了硬件执行 FLOPs","重算使硬件 FLOPs 增加","backward 重算 forward 消耗更多 FLOPs","增加了重算导致实际 FLOPs 增大"],"explain":"activation checkpointing 用算力换显存：backward 时重算被 checkpoint 的 forward 激活，实际执行的硬件 FLOPs 增加（HFU 升），但有效训练进展（每 token 的模型 update）不变（MFU 不变）。","id":"u4-l1-q5"},{"type":"choice","prompt":"下列哪项不属于完整 checkpoint 状态协议必须保存的内容？","options":["model state_dict","optimizer state（Adam m/v）","GPU 驱动版本号","RNG state 和 dataset sampler cursor"],"answer":2,"explain":"GPU 驱动版本是环境信息，不需要写入 checkpoint。checkpoint 必须保存 model、optimizer state（momentum）、scheduler、RNG（CPU+CUDA）、sampler cursor、optimizer_step 和 microstep_idx，才能保证 resume 后状态连续。","id":"u4-l1-q6"},{"type":"judge","prompt":"增加 DataLoader 的 num_workers 可以解决远端对象存储读取速度慢导致的 DataLoader visible wait 问题。","answer":false,"explain":"num_workers 增加的是并行 CPU preprocessing 和预取能力，不能提高远端存储的 I/O 吞吐。远端存储慢需要在存储侧解决（本地缓存、shard 预拉取、更换存储系统），盲目加 worker 只会加重 CPU 和内存压力。","id":"u4-l1-q7"},{"type":"choice","prompt":"对 LLaMA-7B 估算训练 FLOPs/token，`6N_p` 近似包含哪些部分？","options":["仅 forward pass 矩阵计算","forward 和 backward 的主要矩阵计算","forward、backward 加上 optimizer update 和 activation checkpointing 重算","所有 attention 二次项和 optimizer 更新"],"answer":1,"explain":"`6N_p` 是 decoder-only Transformer 的常用近似，包含 forward 和 backward 的主要矩阵（GEMM）计算。不包含 attention 二次项（长序列需修正）、重算、optimizer update 和 padding 浪费等。","id":"u4-l1-q8"},{"type":"match","prompt":"将左侧 token 口径与其正确定义匹配","left":["raw_sequence_slots","non_pad_tokens","loss_tokens","compute_tokens"],"right":["固定 shape 中的 token 槽位数，等于 B × S × N_gpu","排除 padding 的真实 token 数，等于 attention_mask.sum()","进入 cross entropy 的 shifted labels 数，labels != -100","实际执行主要模型 FLOPs 的 token 数，无 unpadding 时等于 raw slots"],"explain":"四种口径层层收窄：raw slots 是硬件执行的 dense shape，non_pad 排除 attention mask 的 pad，loss tokens 是真正有监督信号的位置，compute tokens 取决于是否做了 unpadding/compaction。","id":"u4-l1-q9"},{"type":"choice","prompt":"单机训练的「状态路径」包含以下哪些内容？","options":["model params、gradients、optimizer states、scheduler、RNG、dataset cursor","GPU 驱动版本、CUDA toolkit 版本、PyTorch 版本","DataLoader worker 数量、pin_memory 设置、collate_fn 实现","NCCL 版本、InfiniBand 固件、PCIe 拓扑"],"answer":0,"explain":"状态路径负责记录训练的完整可恢复状态，包括 model params、gradients、optimizer states（Adam m/v）、scheduler、RNG（CPU/CUDA）、dataset cursor 以及 microstep_idx/optimizer_step 计数器。驱动版本属于环境，DataLoader 参数属于配置，NCCL 版本属于通信栈。","id":"u4-l1-q10"},{"type":"judge","prompt":"FP8 训练中，`amax history`、`scale` 和 `scale_inv` 是普通的超参数，不需要写入 checkpoint，恢复后重新冷启动即可。","answer":false,"explain":"FP8 的 amax history、scale 和 scale_inv 是训练精度状态（precision state），属于必须保存的 checkpoint 内容。冷启动这些值会导致恢复后前几步用错误的 scale，引发精度抖动甚至 NaN，与精确 resume 不等价。","id":"u4-l1-q11"},{"type":"input","prompt":"生产单机训练准入规则要求 HBM 峰值 P95 不超过单卡容量的多少比例？（填百分比数值，如 85）","accept":["85","85%","0.85"],"explain":"准入规则：M_peak_P95 ≤ 0.85 × HBM，留 15% 给 allocator 碎片、shape 抖动和临时 kernel workspace。超过 0.85 门槛的作业在规模化时 OOM 风险很高。","id":"u4-l1-q12"},{"type":"choice","prompt":"单机训练循环中，以下哪种操作会把 GPU 的异步执行变成串行，拖慢训练步吞吐？","options":["使用 `pin_memory=True` 的 DataLoader","在训练循环内频繁调用 `loss.item()` 或 `tensor.cpu()`","开启 `non_blocking=True` 的 `.to(device)` 调用","启用 `torch.compile` 对模型做静态图优化"],"answer":1,"explain":"`loss.item()` 和 `tensor.cpu()` 会触发 GPU→CPU 同步（host 等待 device 完成），把异步 CUDA 队列变成串行执行。pin_memory 和 non_blocking 相反——正确使用时可以提升异步度。","id":"u4-l1-q13"},{"type":"match","prompt":"将左侧单机训练「四条路径」与其典型故障症状匹配","left":["数据路径","状态路径","精度策略路径","故障路径"],"right":["GPU 利用率低，DataLoader visible wait 超 step time 的 20%","resume 后 LR schedule 错位，loss 出现台阶式跳变","训练中途 loss 突然变 NaN，GradScaler found_inf 频繁出现","训练循环卡死，无日志输出，进程存活但无前进"],"explain":"数据路径问题表现为 GPU 等数据（利用率低或 DataLoader wait 高）；状态路径问题在 resume 时暴露（LR/cursor 错位）；精度策略问题表现为 NaN 或 overflow；故障路径对应 OOM、hang、I/O stall 等。","id":"u4-l1-q14"},{"type":"input","prompt":"LLaMA-7B（6.7B 参数）在 8 张 H100 BF16 训练，non_pad_tokens/s = 95,000，H100 BF16 峰值算力 989 TFLOPS，MFU 约等于多少？（保留一位小数，以百分比填写，如 48.3）","accept":["48.3","48.3%","~48.3","约48.3"],"explain":"MFU = tokens/s × FLOPs/token ÷ (N_gpu × 峰值算力) = 95000 × (6 × 6.7×10⁹) ÷ (8 × 989×10¹²) = 95000 × 40.2×10⁹ ÷ 7.912×10¹⁵ ≈ 3.819×10¹⁵ ÷ 7.912×10¹⁵ ≈ 48.3%。","id":"u4-l1-q15"},{"type":"judge","prompt":"NUMA 错配（CPU worker 在一个 socket 备数据，却通过另一个 socket 连接的 PCIe 送 GPU）主要影响模型训练的精度，而不是数据吞吐。","answer":false,"explain":"NUMA 错配影响的是数据吞吐（H2D 带宽下降），不是训练精度。数据流通过错误 NUMA 节点的 PCIe 控制器，PCIe 带宽被跨 socket 互联（如 QPI/UPI）消耗，降低有效 H2D 带宽和 DataLoader 填充率。","id":"u4-l1-q16"},{"type":"choice","prompt":"profiler 排查单机训练瓶颈时，下列工具使用顺序哪个是推荐的「逐层收窄」正确顺序？","options":["先 Nsight Compute 分析每个 kernel，再 torch.profiler 验证阶段","先 torch.profiler 定位 PyTorch 阶段，再 Nsight Systems 确认 CUDA stream，最后 Nsight Compute 分析坏 kernel","先 DCGM 看 GPU 健康，再 iostat 看磁盘，最后 torch.profiler 做应用层分析","直接用 Nsight Compute 抓全量训练，获取最完整数据"],"answer":1,"explain":"推荐顺序：torch.profiler（PyTorch 阶段） → Nsight Systems（CUDA stream 时间线） → Nsight Compute（坏 kernel 内部指标）→ DCGM（GPU 健康）→ iostat/perf（host 侧 I/O）。先用轻量工具定位阶段，再针对具体 kernel 做重量级分析，避免 Nsight Compute 全量抓取产生 GB 级 trace 并扭曲 profiling 结果。","id":"u4-l1-q17"},{"type":"judge","prompt":"BF16 训练不需要 GradScaler，是因为 BF16 的指数位宽（8 bit）与 FP32 相同，不存在梯度溢出问题。","answer":true,"explain":"BF16 的指数（exponent）宽度为 8 bit，与 FP32 的 8 bit 指数相同，因此动态范围（数量级范围）接近 FP32。梯度不容易溢出到 Inf，无需 GradScaler 做 loss scaling。FP16 只有 5 bit 指数，动态范围窄，必须配 GradScaler。","id":"u4-l1-q18"},{"type":"choice","prompt":"单机 baseline 准入 checklist 中，DataLoader visible wait 的合格阈值是多少？","options":["低于 step time 的 1%","低于 step time 的 5%","低于 step time 的 15%","低于 step time 的 50%"],"answer":1,"explain":"准入要求 DataLoader visible wait < 5%（稳态 step）。超过 5% 说明 GPU 在等数据，是明显的数据路径瓶颈，需要先用 torch.profiler 确认来自 read/preprocess/collate 哪段，再单变量调整，不能直接上分布式扩容。","id":"u4-l1-q19"}]},{"id":"u4-l2","title":"数据并行：DDP、FSDP 与 ZeRO","subtitle":"复制计算、同步梯度、切分状态","intro":[{"title":"数据并行的第一性原理","body":"数据并行的核心矛盾只有一句话：\n\n**复制计算者以提高样本吞吐，同时让所有计算者在 optimizer step 前重新达成参数一致。**\n\n这推出四个必然后果：\n1. 每个 rank 必须知道自己处理哪一份数据（sampler 分片）\n2. 每个 rank 必须使用等价的模型状态（init broadcast）\n3. backward 产生的局部梯度必须变成全局平均梯度（AllReduce 或 ReduceScatter）\n4. 任意 rank 变慢都会拖慢整个同步组（straggler 效应）\n\n数据并行不是「加卡就加速」，而是把单节点瓶颈延伸到通信和状态路径。"},{"title":"DDP vs FSDP vs ZeRO：各自切什么","body":"一句话总结：**DP 切样本，FSDP/ZeRO 切训练状态，TP 切层内张量，PP 切层段。**\n\n| 策略 | 复制 | 切分 | 主要通信 | 适用场景 |\n|---|---|---|---|---|\n| DDP | params + grads + optimizer state | 数据 | Gradient AllReduce | 完整副本能放进单卡 |\n| ZeRO-1 | params + grads | optimizer state | AllGather/partition | Adam state 顶显存 |\n| ZeRO-2 | params | grad + optimizer state | ReduceScatter | gradient 也太大 |\n| ZeRO-3 / FSDP FULL_SHARD | 按需临时完整参数 | params + grad + optimizer | AllGather + ReduceScatter | 完整副本放不下 |\n\nLLaMA-7B BF16 + AdamW FP32：DDP 每卡 params+grad+Adam 约 100 GiB，H100 80GB 放不下——这就是为什么必须用 FSDP/ZeRO-3。"},{"title":"ZeRO 真实节省倍数（不要被误导）","body":"ZeRO 节省幅度常被高估。以 8 个 DP rank、BF16 训练 + Adam FP32 为例，每参数单卡字节数是 2（params）+2（grad）+12（master+m+v）= **16 bytes**：\n\n| 阶段 | 单卡字节/参数 | 端到端 reduction（N=8）|\n|---|---|---|\n| DDP | 16 | 1× |\n| ZeRO-1 | 4 + 12/8 = 5.5 | **~2.9×**（不是 8×）|\n| ZeRO-2 | 2 + 14/8 = 3.75 | **~4.3×**（不是 8×）|\n| ZeRO-3 | 16/8 = 2 | **≈ N = 8×** |\n\n常见错误：把「optimizer state 内部切了 8 份」误读成「整体省了 8 倍」，导致容量估算错误。\n\n另外 ZeRO 不切 activation，只有 CP/SP 才切；activation 随 batch/seq/layer 大幅变化。","tip":"N 越大 ZeRO 收益越大，但 ZeRO-3 每层 forward/backward 都要 AllGather + ReduceScatter，对跨节点带宽极度敏感。100Gbps 以太网环境慎用大 world ZeRO-3。"},{"title":"DDP backward overlap 机制","body":"DDP 不等 backward 全部完成后才一次性通信，而是用 **gradient bucket** 实现 backward compute 与 AllReduce 重叠：\n\n1. 参数梯度按 bucket 分组（默认 `bucket_cap_mb=25`）\n2. 某 bucket 内所有梯度 ready → 立即启动 async AllReduce\n3. 理想情况下，早期 bucket 的通信被后续 backward compute 掩盖\n4. optimizer step 前等待所有 in-flight bucket 完成\n\n调优不靠直觉，靠 profiler timeline：\n- bucket 太小：大量小 collective，launch overhead 高\n- bucket 太大：backward 结束后 tail NCCL 仍在运行，暴露通信长\n\nGradient accumulation 的中间 microstep 必须用 `no_sync()` 跳过 AllReduce，只在最后一个 microstep 同步；否则每步都触发 AllReduce，通信量扩大 `grad_accum_steps` 倍。"},{"title":"FSDP FULL_SHARD 的生命周期","body":"FSDP/ZeRO-3 每层的显存生命周期：\n\n- **稳态**：每 rank 只持有 `flat_param_shard[i]`、optimizer shard\n- **forward 前**：AllGather 所有 rank 的 shard → 临时拼出完整参数\n- **forward 后**：`reshard_after_forward` 释放完整参数，保留 shard\n- **backward 前**：`backward_prefetch` 提前 AllGather 下一层参数\n- **backward 后**：ReduceScatter 梯度，每 rank 保留自己的 grad shard\n- **optimizer**：每 rank 只更新自己拥有的 shard\n\nHBM 峰值常出现在「当前层完整参数 + prefetch 层完整参数 + activation」叠加瞬间，而不是稳态 shard 大小。`limit_all_gathers=True` 可降低峰值但可能减少 overlap。","tip":"FSDP 的 wrap 粒度很重要：粒度太粗单次 AllGather 太大，粒度太细 collective 次数过多。通常以 Transformer Block 为单位 wrap。"},{"title":"暴露通信：真正影响吞吐的指标","body":"训练中真正伤害吞吐的不是总通信量，而是没有被计算覆盖的部分：\n\n`exposed_communication = max(comm_time - overlap_with_compute, 0)`\n\n`step_time ≈ data_visible + compute + exposed_communication + optimizer + misc`\n\n被 compute 覆盖的通信不需要优化；未被覆盖的才影响 tokens/s。\n\n8 节点 64 GPU FSDP 示例：\n- AllGather + ReduceScatter 总计 1405 ms，overlap 1125 ms\n- 暴露通信仅 280 ms P50，约占 step time 的 4.6%\n- 若 NCCL 退化到 socket（无 RDMA），暴露通信可飙到 1030 ms，step time 变慢 ~9%\n\nNon-pad tokens/s 的弱扩展效率 = `throughput_64GPU / (8 × throughput_8GPU)`，健康值应 ≥ 85%。"},{"title":"Straggler 与 data skew：两种根因，诊断路径不同","body":"同步训练的 step time 由**最慢 rank** 决定。当某个 rank 持续慢时，要区分两种根因：\n\n**data skew**：rank 收到的样本 token 数差异大 → `rank_step_skew_p95` 与 `sample_token_skew_p95` 同时上升\n- 根因：shard 按样本数均分，但没按 token 数平衡\n- 修复：按 non-pad token budget 分配 shard\n\n**硬件/网络 straggler**：rank step skew 高，但 token skew 正常 → 查 GPU 降频、ECC/XID、CPU steal、NUMA 错、NIC 拥塞\n\n诊断顺序：\n1. 先看 per-rank step time P50/P95\n2. 看 sample_token_skew：若 skew > 1.1，优先看 data path\n3. 若 token skew 正常，查 DCGM/IB counters/`nvidia-smi`\n\n每条日志必须带 `rank`、`dataset_shard`、`sample_tokens` 才能做此诊断。","tip":"NCCL timeout 不一定是「网络慢」，有时是某 rank 因 data skew 晚进入 collective 导致其他 rank 超时等待。"},{"title":"何时从 DP 切换到 TP/PP/CP","body":"数据并行家族（DDP/FSDP/ZeRO）解决的是**训练状态太大**和**样本吞吐不足**，但有三类问题它解决不了：\n\n1. **单层矩阵放不下** → 需要 Tensor Parallel（TP），优先节点内 NVLink\n2. **整网层数/深度超 HBM** → 需要 Pipeline Parallel（PP）\n3. **超长序列 attention/KV 占满 HBM** → 需要 Context/Sequence Parallel（CP/SP）\n\n决策流程：\n- DDP 状态 > HBM admission → 切 FSDP/ZeRO\n- FSDP 后单层仍 OOM → 加 TP\n- 模型太深需要跨 stage → PP + TP + DP/FSDP\n- 序列长度超长 → CP/SP + hybrid\n\n每增加一个并行维度，就增加更多 process group、rank placement 规则、checkpoint metadata 和排障复杂度。**应以最小满足约束的组合为原则。**","reveal":{"q":"为什么 64-way FSDP FULL_SHARD 的 admission 不能只做 HBM/64 心算？","a":"FSDP 在 forward/backward 时 AllGather 完整参数（live_allgather_params）临时驻留在 HBM，加上 prefetch、reduce-scatter buffer、activation 和 fragmentation，峰值远高于 shard 大小除以 world_size。必须用 profiler 或 memory snapshot 验证 P95 HBM，而不是静态心算。"}}],"questions":[{"type":"choice","prompt":"ZeRO-1 在 N=8 的 DP 场景下，7B BF16+Adam FP32 训练状态的端到端显存 reduction 约为多少？","options":["约 8×（optimizer state 切了 8 份）","约 2.9×","约 4×","约 6×"],"answer":1,"explain":"每参数 16 bytes：ZeRO-1 只切 optimizer state（12 bytes），单卡剩 2+2+12/8=5.5 bytes，端到端 reduction = 16/5.5 ≈ 2.9×。常见错误是把「optimizer state 内部节省 8 倍」误读成整体节省 8 倍。","id":"u4-l2-q1"},{"type":"judge","prompt":"DDP gradient accumulation 的每个 microstep 都应该触发一次 AllReduce，以保证梯度同步。","answer":false,"explain":"中间 microstep 应使用 `no_sync()` 跳过 AllReduce，只在最后一个 microstep 触发同步。否则通信量扩大 grad_accum_steps 倍，严重影响吞吐且没有训练语义收益。","id":"u4-l2-q2"},{"type":"match","prompt":"将左侧 FSDP FULL_SHARD 阶段与其发生的通信操作匹配","left":["forward 开始前","backward 结束后","optimizer step","backward 开始前（prefetch）"],"right":["AllGather：拼出当前层完整参数","ReduceScatter：每 rank 保留自己的 grad shard","本地更新：只更新自己持有的 param/Adam shard","AllGather：预取下一层参数"],"explain":"FSDP FULL_SHARD 的生命周期：forward 前 AllGather 完整参数，backward 后 ReduceScatter 梯度，optimizer 本地更新 shard，prefetch 在 backward 期间提前 AllGather 下一层参数。","id":"u4-l2-q3"},{"type":"choice","prompt":"训练中真正影响吞吐的通信量是哪个指标？","options":["NCCL 总通信时间","AllReduce 消息大小","暴露通信 = max(comm - overlap, 0)","AllGather 调用次数"],"answer":2,"explain":"被 backward compute 覆盖的通信不需要优化。真正伤害 step time 的是没有被计算覆盖的「暴露通信」= max(comm_time - overlap, 0)。总 NCCL 时间高但暴露少，对吞吐影响有限。","id":"u4-l2-q4"},{"type":"input","prompt":"DDP 中 `no_sync()` 的作用是什么？（填核心功能，不超过 15 字）","accept":["跳过中间 microstep 的 AllReduce 通信","gradient accumulation 时跳过 AllReduce","推迟 AllReduce 到最后一个 microstep","中间步骤跳过梯度同步"],"explain":"`no_sync()` 让 DDP 在 gradient accumulation 的中间 microstep 跳过 AllReduce，只在最后一个 microstep 触发同步。这样通信次数从 grad_accum_steps 次降到 1 次。","id":"u4-l2-q5"},{"type":"judge","prompt":"如果 rank step skew P95 高，而 sample_token_skew P95 正常，应优先检查 dataset shard 的 token 分布不均。","answer":false,"explain":"两者同时上升才优先查 data skew。若 rank step skew 高但 token skew 正常，说明样本长度分布均匀，应优先查硬件/网络问题：GPU 降频、ECC/XID、CPU steal、NUMA 错误、NIC 拥塞等。","id":"u4-l2-q6"},{"type":"choice","prompt":"下列哪种场景最适合引入 Tensor Parallel（TP）而不是继续扩展 FSDP 的 world size？","options":["7B 模型 optimizer state 太大放不进单卡","FSDP 后单层 GEMM 的 activation 仍超 HBM","跨节点 NCCL 暴露通信超过 step time 的 20%","dataset shard token 分布不均"],"answer":1,"explain":"TP 解决的是「单层矩阵/activation 放不下」的问题，通过在层内切分 tensor dimension 减少单卡峰值。FSDP 解决训练状态冗余，但不切单层 activation。NCCL 暴露通信高应先调 bucket/overlap/topology，dataset skew 应修 shard 策略。","id":"u4-l2-q7"},{"type":"match","prompt":"将左侧症状与最可能的根因匹配","left":["rank_step_skew 和 sample_token_skew 同时高","NCCL timeout，日志 NET/Socket","backward 后 180ms NCCL tail","OOM 只在多节点出现"],"right":["dataset shard 按样本数均分，未按 token 数平衡","RDMA 未注入容器，NCCL fallback 到 socket","bucket 太大，最后一个 bucket 延迟高；或有 rank straggler 破坏 overlap","live AllGather 参数增多或 prefetch 导致 HBM 峰值超限"],"explain":"四种典型 DP 故障的根因各不相同：data skew 表现为 token/rank skew 同时升，socket fallback 表现为 NCCL 日志和 busbw 低，bucket 问题表现为 backward 后 tail，多节点 OOM 常来自 AllGather 瞬时峰值。","id":"u4-l2-q8"},{"type":"choice","prompt":"64 GPU FSDP 训练从 8 GPU baseline 做弱扩展，理想情况下 non-pad tokens/s 应提高约多少倍？","options":["无限接近 8 倍，因为 FSDP 理论效率 100%","接近 8 倍，但实际因通信暴露和 straggler 会略低","固定 2 倍，因为状态切分带来额外开销","与 GPU 数量无关"],"answer":1,"explain":"弱扩展（每卡 batch 不变）理想下 non-pad tokens/s 应线性增长约 8 倍（64/8）。实际因跨节点通信暴露、straggler、prefetch 开销等，弱扩展效率通常在 85%~100% 之间。","id":"u4-l2-q9"},{"type":"choice","prompt":"ZeRO-3（N=16 DP，7B BF16+AdamW FP32）时，每参数单卡字节数约为多少？","options":["1 byte/param","2 bytes/param","4 bytes/param","8 bytes/param"],"answer":0,"explain":"ZeRO-3 把全部状态（params 2B + grads 2B + Adam 12B = 16B/param）均匀切到 N=16 个 rank，每参数单卡 = 16/16 = 1 byte/param。这是 ZeRO-3 在大 world_size 下的理论最优，但 AllGather/ReduceScatter 通信量最大。","id":"u4-l2-q10"},{"type":"judge","prompt":"FSDP FULL_SHARD 的显存峰值出现在稳态 shard 驻留阶段，可以用「总参数/world_size」简单估算。","answer":false,"explain":"FSDP 的 HBM 峰值出现在「当前层完整参数 + prefetch 层完整参数 + activation」叠加的瞬间，而不是稳态 shard。峰值远高于 shard 大小 / world_size，必须用 profiler 或 memory snapshot 验证，不能静态心算。","id":"u4-l2-q11"},{"type":"input","prompt":"FSDP FULL_SHARD 中，`reshard_after_forward` 标志的作用是什么？（填核心功能，不超过 15 字）","accept":["forward 后释放完整参数只保留 shard","前向后释放完整参数","forward 结束后丢弃完整参数保留 shard","forward 后释放 AllGather 的临时完整参数"],"explain":"`reshard_after_forward=True` 在每层 forward 结束后立即释放 AllGather 拼出的完整参数，恢复到稳态 shard，减少 HBM 峰值。代价是 backward 时需要重新 AllGather，增加通信次数。","id":"u4-l2-q12"},{"type":"choice","prompt":"DDP 的 gradient bucket 默认大小是多少？","options":["1 MB","10 MB","25 MB","100 MB"],"answer":2,"explain":"DDP 的默认 `bucket_cap_mb=25`（25 MB）。bucket 太小会产生大量小 NCCL collective，launch overhead 高；bucket 太大会导致 backward 结束后 tail NCCL 仍在运行，暴露通信。25 MB 是 PyTorch 的默认折中值，需要用 profiler 根据实际 step time 调优。","id":"u4-l2-q13"},{"type":"match","prompt":"将左侧 ZeRO 阶段与其切分的训练状态匹配","left":["ZeRO-1","ZeRO-2","ZeRO-3 / FSDP FULL_SHARD","DDP（无 ZeRO）"],"right":["只切 optimizer state（Adam m/v），params 和 grads 每卡完整复制","切 optimizer state + grads，params 每卡完整复制","params、grads、optimizer state 全部切分，forward 时 AllGather 临时拼出完整参数","params、grads、optimizer state 全部每卡完整复制，无切分"],"explain":"ZeRO 三个阶段逐步扩大切分范围：ZeRO-1 仅切 optimizer state，ZeRO-2 再切 grads，ZeRO-3/FSDP FULL_SHARD 三者全切。DDP 无任何切分，每卡完整复制所有状态。","id":"u4-l2-q14"},{"type":"judge","prompt":"FSDP 中 `backward_prefetch=BACKWARD_PRE` 可以减少 backward 阶段的暴露通信，原理是在前一 unit backward 执行时就提前 AllGather 下一 unit 的参数。","answer":true,"explain":"`BACKWARD_PRE` 让 FSDP 在处理第 i 层 backward 时就发起第 i+1 层（下一个 FSDP unit）的 AllGather，使参数通信与前一层 backward 计算重叠，减少暴露通信，改善 HBM 吞吐利用率。","id":"u4-l2-q15"},{"type":"input","prompt":"ZeRO-2 在 N=8、BF16+AdamW FP32（每参数 16 bytes）场景下，端到端显存 reduction 约为多少倍？（保留一位小数）","accept":["4.3","~4.3","约4.3","4.3x"],"explain":"ZeRO-2 切 grads（2B）+ optimizer state（12B），每参数单卡 = params（2B）+ (grads+Adam)/8 = 2 + 14/8 = 3.75 bytes。reduction = 16 / 3.75 ≈ 4.27×，约 4.3×。ZeRO-2 不是整体 8 倍，因为 params 仍每卡完整复制。","id":"u4-l2-q16"},{"type":"choice","prompt":"当「NCCL timeout」和「NET/Socket」字样同时出现在训练日志中时，最可能的根本原因是什么？","options":["某个 rank 因 data skew 处理时间过长，晚进入 collective","RDMA（InfiniBand/RoCE）未正确注入容器，NCCL fallback 到 TCP/Socket","bucket 太大，最后一个 bucket AllReduce 时间过长","GPU 降频导致 backward compute 变慢，其他 rank 超时等待"],"answer":1,"explain":"NCCL 日志中 `NET/Socket` 说明 NCCL 选用了 socket backend（TCP/IP），而非 RDMA，这通常是 InfiniBand/RoCE 设备未正确映射到容器（缺少设备挂载或 RDMA 插件未安装）导致。socket transport 带宽和延迟比 RDMA 差一个数量级，频繁触发超时。","id":"u4-l2-q17"},{"type":"judge","prompt":"在 Tensor Parallel（TP）中，FSDP/ZeRO-3 可以直接用于切分单层 activation（如 attention score 矩阵），降低长序列带来的单层 OOM 风险。","answer":false,"explain":"FSDP/ZeRO-3 只切分训练状态（params/grads/optimizer state），不切分 activation。长序列导致的单层 activation OOM 需要 FlashAttention（将 O(seq²) 降为 O(seq)）、Tensor Parallel（按 head 分片）或 Context Parallel（按 token 分片）来解决，FSDP 无法解决此问题。","id":"u4-l2-q18"},{"type":"match","prompt":"将左侧 DP 弱扩展效率问题与其最合适的优化手段匹配","left":["暴露通信占 step time 25%，计算无法覆盖 AllReduce","某个 rank 持续比其他 rank 慢 30%，token skew 正常","dataset shard 按样本数均分，但某些 rank step time 随 seq length 波动大","FSDP 进入多节点后，OOM 频率上升"],"right":["缩小 bucket size 或启用 gradient compression 减少通信量","检查 DCGM/nvidia-smi 排查 GPU 降频、ECC 错误或 NUMA 错配","改为按 non-pad token budget 均分 shard，而非按样本数均分","用 profiler 验证 AllGather 瞬时峰值并调整 limit_all_gathers 参数"],"explain":"通信暴露靠减少通信或增加重叠；硬件 straggler 靠 DCGM/nvidia-smi 排查物理原因；data skew 靠 token budget 均分；多节点 OOM 通常来自 FSDP AllGather 瞬时峰值，可调 limit_all_gathers 降低。","id":"u4-l2-q19"}]},{"id":"u4-l3","title":"模型并行与流水并行","subtitle":"TP、PP、SP、CP 的机制、代价与组合","intro":[{"title":"五个维度的并行切分","body":"当单 GPU 无法承载训练所需的参数、activation 和计算量时，需要从五个维度做切分：\n\n| 维度 | 切什么 | 典型机制 | 主要收益 | 主要代价 |\n|---|---|---|---|---|\n| 样本 | batch/sample | DP | 提升样本吞吐 | 梯度同步、straggler |\n| 训练状态 | params/grads/Adam state | FSDP/ZeRO | 降低状态冗余 | AllGather/ReduceScatter |\n| 层内张量 | hidden/heads/矩阵行列 | TP | 降低单层峰值 | 高频 collective |\n| 层段 | Transformer blocks | PP | 降低每卡层数 | pipeline bubble |\n| 序列/上下文 | tokens/KV/context | SP/CP | 降低长序列 activation | 序列维度通信 |\n\n一句话总结：**DP 切样本，FSDP/ZeRO 切训练状态，TP 切层内张量，PP 切层段，SP/CP 切序列。**"},{"title":"Rank Mesh：三维并行的坐标系","body":"混合并行的全局 rank 通过公式映射到各维度：\n\n`world_size = DP × PP × CP × TP`\n\n以 row-major 顺序（TP 最内层）：\n\n```\ntp_rank = global_rank % TP\ncp_rank = (global_rank // TP) % CP\npp_rank = (global_rank // (TP × CP)) % PP\ndp_rank = global_rank // (TP × CP × PP)\n```\n\n例如 `DP=2, PP=4, TP=4`（32 GPU）中，同一 PP stage、TP 组内的 rank 共同计算同一层，彼此通信走 NVSwitch；不同 DP 副本间通过 IB/RoCE 同步梯度；PP stage 之间通过 P2P send/recv 传 activation。\n\n**最危险的事故**：训练能跑、checkpoint 能写，但恢复时按另一套 rank order 解释 shard，导致 silent weight corruption。rank mesh 定义必须全局唯一。","tip":"每个 global_rank 必须能反查出 tp_rank、pp_rank、dp_rank。checkpoint metadata 如果缺少 parallel layout，reshape 恢复和推理转换都会失败。"},{"title":"Tensor Parallel（TP）：切层内张量","body":"TP 把一层的权重矩阵按 hidden 维度或 head 维度分到多个 rank。标准 Megatron-style TP：\n\n- **Column Parallel Linear**：切输出 hidden，forward 不 AllGather，后续继续在分片上计算\n- **Row Parallel Linear**：切输入 hidden，局部 matmul 后 AllReduce（或 ReduceScatter if SP）\n\n一个 Transformer block 的 forward 通常有 2 次 AllReduce（attention output projection、MLP FC2），backward 再对称 2 次。\n\n**关键约束**：hidden size、Q heads 必须能被 TP 整除。GQA/MQA 的 KV heads 也通常需要能被 TP 整除（如 KV heads=8 则 TP ≤ 8）。\n\n**TP 优先放节点内 NVSwitch**：节点内 AllReduce bus bandwidth ≈ 600 GB/s，跨节点仅 ~50 GB/s（400G IB），跨节点 TP 每层通信延迟约扩大 12×，成为严重瓶颈。"},{"title":"Pipeline Parallel（PP）：1F1B 调度与 bubble","body":"PP 把模型层段分配到不同 rank（stage），microbatch 依次流过各 stage，形成流水线。\n\n**1F1B 调度**（最常用）的两种 bubble 口径：\n\n- `bubble_overhead = (p-1)/m`：相对理想计算槽的额外成本\n- `bubble_elapsed_fraction = (p-1)/(m+p-1)`：端到端时间线中空槽占比\n\n`p=8 stage, m=16 microbatch` 时：overhead=43.8%，elapsed fraction=30.4%。**生产规则：m ≥ 4p 才能让稳态主导。**\n\n**Interleaved 1F1B**：一个物理 rank 持有多个 virtual stage，bubble 降为 `(p-1)/(v×m)`，但每个 microbatch 跨越更多 stage 边界，activation send/recv 通信量随 v 倍增。\n\n**Zero Bubble**：把 backward 拆成 B-pass（dL/dX）和 W-pass（dL/dW），W-pass 可推迟到空槽执行，bubble 接近 0，但 activation 驻留时间更长。","tip":"PP stage 边界的 activation payload 大小取决于 layout：Megatron shard layout 下 PP 传输 hidden/TP 宽度，比 full-hidden layout 小 TP 倍，显著影响跨节点带宽压力。"},{"title":"Activation 显存：最容易被低估的项","body":"70B 模型（BF16，hidden=8192，seq=8192，80 层，micro_batch=1）：\n\n| AC 策略 | Per-block activation | 80 层合计 |\n|---|---|---|\n| 无 AC（标准 MHA）| hidden 项 + seq² attention score | ~784 GB（超出 8×80GB 节点）|\n| FlashAttention 替代 | ~hidden × 18 bytes/element | ~97 GB |\n| Selective AC + FlashAttention | ~hidden × 8 bytes/element | ~43 GB |\n| Full Recompute（只存 block 输入）| ~hidden × 2 bytes/element | ~11 GB |\n\n**标准 MHA 不开 FlashAttention，70B 训练 activation 约 784 GB，超出单节点总 HBM 640 GB。**\n\nZeRO/FSDP 不切 activation。TP=8 将 hidden 相关 activation 降到 1/8，但 seq² 项只降 num_heads/TP。CP 对 attention 的二次方项有最显著效果：CP=4 将 attention workspace 降到约 1/16。","tip":"生产 70B 训练必须开 FlashAttention + Selective AC。「显存够了」不代表 activation 没问题——activation 是随 batch、seq、layer 增长最快的项。"},{"title":"Sequence/Context Parallel（SP/CP）","body":"**SP**（通常在 TP 组内）：把 TP 的 AllReduce 改成 ReduceScatter(seq)，非 attention activation（LayerNorm、Dropout、Residual）按 sequence 分片，每 rank 只保留 S/TP 的 token，再在下一个 ColumnParallel 前 AllGather(seq) 还原。总通信量与 TP AllReduce 近似，但 activation 常驻量降到约 1/TP。\n\n**CP**（Context Parallel）：面向超长 attention context。每 rank 持有 S/CP 的 token，通过 Ring FlashAttention 逐轮 send/recv K/V 块，用 online softmax 合并结果。\n\n64K context、CP=4、GQA（8 KV heads）时，每层 CP 通信约 201 MB，80 层×2 ≈ 32 GB/step，400G IB 上纯通信约 0.64 s——**必须靠 Ring FA 的 overlap（KV exchange 与本地 attention 同时进行）才能压到可接受范围**。\n\n**适用场景**：\n- 8K/16K context → SP 有收益，CP 未必值得\n- 64K/128K+ context → CP 必须进入候选\n\n**关键约束**：CP 下每个 rank 的 token 必须保留全局 position（RoPE 用 global_pos），否则 causal mask 在 CP 边界错位。"},{"title":"Stage 负载均衡与 pipeline 效率","body":"pipeline bubble 公式假设所有 stage 计算时间相同。真实情况：\n\n- Stage 0 含 Embedding → 比普通 Transformer block 慢 10-30%\n- 最后 stage 含 LM Head + Loss → 更慢\n\n`stage_imbalance_ratio = max(stage_time_p50) / mean(stage_time_p50) - 1`\n- < 5%：可接受\n- 5-15%：显著，应重新分配层数\n- > 15%：强制修复，即使开 interleaved pipeline，最慢 stage 仍是瓶颈\n\n修复手段：\n- Embedding 单独放 stage 0（0 层 Transformer block）\n- Vocab parallel 切分 LM head\n- DeepSpeed `partition_method=\"parameters\"` 按参数量均分\n\n**注意**：先区分「stage 时间差来自 compute 不均」还是「来自 data skew（某些 rank 样本更长）」，两种修复方式完全不同。"},{"title":"3D Parallel 通信的重叠原则","body":"通信能否被 compute 覆盖决定实际 step time。各类通信的重叠条件：\n\n| 通信类型 | 重叠对象 | 关键实现条件 |\n|---|---|---|\n| TP AllReduce | 下一层 ColumnParallel GEMM | `CUDA_DEVICE_MAX_CONNECTIONS=1` + 独立 CUDA stream |\n| PP send/recv | 同 stage 下一 microbatch 的 compute（1F1B 稳态）| 异步 `isend/irecv`；m ≥ PP |\n| DP gradient AllReduce | 后续 layer backward | DDP bucket 就绪即启动异步 AllReduce |\n| FSDP AllGather（backward prefetch）| 前一 unit backward | `backward_prefetch=BACKWARD_PRE` |\n| CP KV ring exchange | 本地 context block attention | Ring FlashAttention 实现 |\n\n**诊断顺序**：先确认 placement（TP 在节点内），再用 profiler 找是哪类通信暴露，最后才调 NCCL env。","reveal":{"q":"为什么 70B 模型不开 FlashAttention 时，单节点 8×80GB 无法训练（即使有 TP/PP）？","a":"标准 MHA 的 attention score 矩阵是 O(batch×heads×seq²)，70B（64 heads, seq=8192）无 AC 时 attention scores 约 688 GB，加上 hidden 相关 activation 约 97 GB，合计约 784 GB，超出节点 640 GB HBM。ZeRO/FSDP 不切 activation；TP=8 只将 seq² 项按 num_heads 降到约 1/8，仍约 86 GB/层，不解决根本问题。必须用 FlashAttention 将 O(seq²) 降为 O(seq) 的 LSE 存储。"}}],"questions":[{"type":"choice","prompt":"70B 模型（hidden=8192, 64 heads, seq=8192）用标准 MHA 无 FlashAttention、无 AC 时，80 层的 attention score activation 约多少 GB？","options":["约 97 GB","约 43 GB","约 688 GB","约 11 GB"],"answer":2,"explain":"Attention score 矩阵大小为 batch×heads×seq×seq×dtype_bytes = 1×64×8192×8192×2 bytes ≈ 8.6 GB/层，80 层合计约 688 GB。这超出了单节点 8×80GB 的总 HBM（640 GB），是生产必须开 FlashAttention 的根本原因。","id":"u4-l3-q1"},{"type":"judge","prompt":"TP=16 可以应用于 KV heads=8 的 GQA 模型（如 LLaMA-3 70B），因为 Q heads=64 能整除 16。","answer":false,"explain":"Megatron 常见 GQA layout 要求 num_kv_heads % TP == 0。KV heads=8 时，TP 上限为 8。TP=16 超出 KV heads 数量，需要框架支持 KV replication 或特殊 shard 规则，不能直接使用默认配置。","id":"u4-l3-q2"},{"type":"choice","prompt":"PP=8 stages, m=16 microbatches，1F1B 调度的 bubble_overhead（相对理想计算槽）是多少？","options":["约 7%","约 30%","约 44%","约 87%"],"answer":2,"explain":"bubble_overhead = (p-1)/m = (8-1)/16 = 7/16 ≈ 43.8%。注意 elapsed_idle_fraction = (p-1)/(m+p-1) = 7/23 ≈ 30.4%，两个口径不同，不能混用。","id":"u4-l3-q3"},{"type":"match","prompt":"将左侧并行机制与其主要解决的问题匹配","left":["Tensor Parallel（TP）","Pipeline Parallel（PP）","Context Parallel（CP）","Sequence Parallel（SP）"],"right":["单层矩阵/activation 超出单卡容量，切层内 hidden/heads","整网层数太多，每卡只承载一段层段，microbatch 像流水线流过","超长 attention context，每 rank 只持有 S/CP 的 token，ring 交换 KV","TP 组内非 attention activation 仍重复存储，按 sequence 分片减少冗余"],"explain":"TP 切层内张量，PP 切层段，CP 切长上下文 attention，SP 切非 attention 的序列激活（通常在 TP 组内）。四个机制解决不同瓶颈，经常组合使用。","id":"u4-l3-q4"},{"type":"input","prompt":"为什么 TP 应优先放在节点内 NVSwitch 而不是跨节点？（填核心原因，不超过 20 字）","accept":["跨节点带宽远低于节点内 NVSwitch","节点内 AllReduce 带宽远高于 IB/RoCE","每层 AllReduce 跨节点延迟增大约 12 倍","跨节点 TP 每层通信成为严重瓶颈"],"explain":"节点内 NVSwitch AllReduce bus bandwidth ≈ 600 GB/s，跨节点 IB（400G）≈ 50 GB/s，差距约 12 倍。TP 每个 Transformer block 有 2 次 AllReduce，跨节点时每次通信延迟从 ~0.4 ms 变成 ~4.8 ms，160 次串行约 768 ms，远超计算时间。","id":"u4-l3-q5"},{"type":"judge","prompt":"Interleaved 1F1B 通过将一个物理 rank 分配多个 virtual stage 来降低 pipeline bubble，但增加了每个 microbatch 的 activation send/recv 通信次数。","answer":true,"explain":"Interleaved 1F1B 将 bubble 降为 (p-1)/(v×m)，但 virtual stage 数量 v 越大，每个 microbatch 跨越的 stage 边界越多，activation send/recv 通信量随 v 倍增加。在带宽不足时，v 太大反而拖慢总吞吐。","id":"u4-l3-q6"},{"type":"choice","prompt":"64K context、CP=4 的 Ring FlashAttention 中，每个 rank 的 Q token 需要参与多少轮 ring step？","options":["1 轮（只看本地 K/V）","CP 轮（每轮轮转一块 K/V）","CP-1 轮（不包括本地那一轮）","seq/CP 轮"],"answer":2,"explain":"每个 rank 持有 S/CP 的 token 作为 Q，需要看到全部 S 的 K/V。本地 K/V 直接参与（第 0 轮），然后从相邻 rank 依次接收 CP-1 块 K/V，总共 CP-1 轮 ring exchange。","id":"u4-l3-q7"},{"type":"match","prompt":"将左侧 PP 调度策略与其 bubble overhead 公式匹配","left":["GPipe（全 F 再全 B）","1F1B（Megatron 默认）","Interleaved 1F1B（virtual stage=v）","Zero Bubble"],"right":["约 2(p-1)/m","(p-1)/m","约 (p-1)/(v×m)","接近 0（W-pass 填空槽）"],"explain":"GPipe 约 2(p-1)/m，因为完整 forward 后再完整 backward，空泡翻倍；1F1B 为 (p-1)/m；Interleaved 1F1B 引入 virtual stage 数 v 进一步降低；Zero Bubble 将 backward 拆成 B-pass 和 W-pass，W-pass 填充空槽，bubble 接近 0。","id":"u4-l3-q8"},{"type":"judge","prompt":"Context Parallel（CP）中，每个 rank 持有的 token 可以用 local position index（从 0 开始）计算 RoPE，只要在合并时再加上 global offset 即可。","answer":false,"explain":"CP 中每个 rank 的 token 必须在 RoPE 计算时就使用 global_pos（= cp_rank × S/CP + local_pos），causal mask 也必须基于全局 position。用 local position 计算后再加 offset 会导致 RoPE 相位错误和 causal mask 在 CP 边界错位，表现为 loss 下降但长上下文能力损坏。","id":"u4-l3-q9"},{"type":"choice","prompt":"Selective Activation Checkpointing（选择性 AC）+FlashAttention 相比无任何 AC 的标准 MHA，对 70B 模型 80 层的 activation 大小影响约是多少？","options":["从约 784 GB 降低到约 97 GB","从约 784 GB 降低到约 43 GB","从约 784 GB 降低到约 11 GB","从约 97 GB 降低到约 43 GB"],"answer":1,"explain":"无 AC + 标准 MHA ≈ 784 GB；FlashAttention 替代（不存 seq² attention score）≈ 97 GB；Selective AC + FlashAttention ≈ 43 GB；Full Recompute（只存 block 输入）≈ 11 GB。Selective AC + FlashAttention 比纯 FlashAttention 再省约 50%。","id":"u4-l3-q10"},{"type":"judge","prompt":"Sequence Parallel（SP）与 Tensor Parallel（TP）通常在同一个进程组内一起使用，SP 将 TP 的 AllReduce 替换为 ReduceScatter+AllGather，从而减少非 attention 激活的常驻显存。","answer":true,"explain":"SP 通常在 TP 组内启用：把 TP AllReduce 改为 ReduceScatter(seq)，使 LayerNorm、Dropout 等非 attention activation 按 sequence 维度分片，每 rank 只保留 S/TP 的 activation，下一个 ColumnParallel 前再 AllGather(seq) 还原。总通信量与 TP AllReduce 近似，但 activation 常驻量降约 1/TP。","id":"u4-l3-q11"},{"type":"input","prompt":"32 GPU 的 3D 并行配置（DP=2, PP=4, TP=4），global_rank=17 的 tp_rank 是多少？","accept":["1"],"explain":"按 row-major 公式：tp_rank = global_rank % TP = 17 % 4 = 1。依次验证：cp_rank = (17 // 4) % 1 = 0；pp_rank = (17 // (4×1)) % 4 = 4 % 4 = 0；dp_rank = 17 // (4×1×4) = 17 // 16 = 1。所以 global_rank=17 属于 (dp=1, pp=0, tp=1)。","id":"u4-l3-q12"},{"type":"choice","prompt":"PP=4 stage，stage 0 含 Embedding（比普通 block 慢 20%），stage 3 含 LM Head+Loss（比普通 block 慢 25%）。stage_imbalance_ratio 约为多少？该如何处置？","options":["约 5%，可接受，无需操作","约 25%，必须修复：将 Embedding 和 LM Head 单独放成独立 stage","约 12%，显著，应重新分配层数或单独处理首尾 stage","约 2%，在噪声范围内，继续观察"],"answer":1,"explain":"stage_imbalance_ratio = max(stage_time) / mean(stage_time) - 1。若最慢 stage（LM Head，慢 25%）与均值之差约为 25%，超过 15% 门槛，必须强制修复：将 Embedding 单独放 stage 0（0 层 Transformer block），LM Head 用 Vocab Parallel 分担或单独放末 stage，重新平衡各 stage 层数。","id":"u4-l3-q13"},{"type":"match","prompt":"将左侧 3D 并行通信类型与其覆盖（overlap）计算的对象匹配","left":["TP AllReduce","PP send/recv（1F1B 稳态）","FSDP AllGather（backward prefetch）","CP Ring KV exchange"],"right":["同层下一个 ColumnParallel GEMM（需 CUDA_DEVICE_MAX_CONNECTIONS=1 + 独立 stream）","同 stage 下一个 microbatch 的 forward/backward compute（需 m ≥ PP）","前一 FSDP unit 的 backward compute（需 BACKWARD_PRE 模式）","本地 context block 的 attention 计算（Ring FlashAttention 实现）"],"explain":"各并行通信的 overlap 对象：TP AllReduce 覆盖下一层 GEMM；PP isend/irecv 在 1F1B 稳态被下一 microbatch 计算覆盖；FSDP prefetch 覆盖前一 unit backward；CP ring exchange 在 Ring FA 中与本地 attention 并行。通信被 compute 覆盖的部分不影响 step time。","id":"u4-l3-q14"},{"type":"choice","prompt":"以下关于 Zero Bubble PP 的描述，哪项是正确的？","options":["Zero Bubble 通过增加 microbatch 数量到无穷大来消除 bubble","Zero Bubble 将 backward 拆分为 B-pass（dL/dX）和 W-pass（dL/dW），W-pass 推迟到空槽执行","Zero Bubble 要求每个 PP stage 持有相同数量的层，不支持非均匀切分","Zero Bubble 完全不需要 pipeline 预热（warmup）阶段"],"answer":1,"explain":"Zero Bubble 的核心创新是把 backward 拆成两部分：B-pass 计算 dL/dX（传递给前一层），W-pass 计算 dL/dW（更新本层权重）。W-pass 不需要即时执行，可以推迟填充原本的 pipeline bubble 时间槽，使 bubble 接近 0。代价是 activation 驻留时间更长。","id":"u4-l3-q15"},{"type":"judge","prompt":"在 3D 并行中，rank mesh 的 parallel layout（DP/PP/TP/CP 维度大小及 global_rank 映射规则）只需要记录在代码注释里，不需要写入 checkpoint metadata。","answer":false,"explain":"rank mesh 定义必须写入 checkpoint metadata，全局唯一。若 checkpoint metadata 缺少 parallel layout，恢复时按另一套 rank order 解释 shard 会导致静默 weight corruption——训练能跑、loss 下降，但权重已经错位。这是最危险的恢复错误之一。","id":"u4-l3-q16"},{"type":"input","prompt":"在 Megatron-style TP 中，一个 Transformer block 的 forward 有几次 AllReduce（不含 SP）？","accept":["2","两次","2次"],"explain":"标准 Megatron TP 每个 Transformer block 在 forward 时有 2 次 AllReduce：一次在 attention output projection（Row Parallel Linear 后），一次在 MLP FC2（Row Parallel Linear 后）。backward 再对称各 2 次，共 4 次。","id":"u4-l3-q17"},{"type":"choice","prompt":"64K context、CP=4 时，每层 CP 通信约 201 MB（forward），80 层×forward+backward 约 32 GB/step，400G IB 纯通信约 0.64 s。若不使用 Ring FlashAttention 做 overlap，这 0.64 s 对 step time 的影响是什么？","options":["约 0.64 s 全部暴露，直接加到 step time 上，严重影响吞吐","约 0.64 s 被 backward 计算完全覆盖，对 step time 无影响","影响约 50%，因为 forward 和 backward 各一半通信","影响不到 1%，因为 IB 通信延迟与计算不在同一 CUDA stream"],"answer":0,"explain":"若不用 Ring FlashAttention 做 overlap，0.64 s 的 CP 通信完全串行暴露，直接加到 step time 上。这是 CP 必须通过 Ring FA 实现 KV exchange 与本地 attention 并行的根本原因。CP 的「通信被覆盖」不是自动的，需要 Ring FlashAttention 的实现支持。","id":"u4-l3-q18"},{"type":"match","prompt":"将左侧问题场景与最适合引入的并行策略匹配","left":["FSDP 后单张 H100 仍 OOM，但同一层的 hidden 矩阵本身就超出单卡","模型层数极多，FSDP 后单卡 shard 的参数已经很小，但训练时间极长","训练 128K context 的长文档模型，attention workspace 占满 HBM","7B 模型 BF16+AdamW，单节点 optimizer state 无法放入单卡"],"right":["Tensor Parallel（TP），在节点内 NVSwitch 切分层内 hidden/heads","Pipeline Parallel（PP），切层段减少每卡持有的层数","Context Parallel（CP），按 token 分片做 Ring FlashAttention","FSDP FULL_SHARD 或 ZeRO-3，切分训练状态"],"explain":"TP 解决单层矩阵放不下；PP 解决层数过多；CP 解决超长序列 attention workspace 问题；FSDP/ZeRO-3 解决训练状态过大。按最小满足约束原则选组合。","id":"u4-l3-q19"}]},{"id":"u4-l4","title":"MoE 训练基础设施","subtitle":"Expert Parallelism、All-to-All、路由负载均衡与 Checkpoint","intro":[{"title":"MoE 的核心动机：参数容量与 FLOPs 解耦","body":"Dense 模型把「参数=容量=FLOPs」绑死——每加一个 layer 或加宽 hidden，所有 token 都走完所有权重，FLOPs 线性涨。\n\nMoE（Mixture of Experts）的核心思路：把 FFN 分裂成 N 个 expert，每个 token 由 router 只激活 K 个（top-K），参数容量随 N 扩张，但每 token 的 FLOPs 大致随 K 而不是 N 增长。\n\nDeepSeek-V3 的典型数字：671B 总参数，每 token 激活约 37B（256 个 routed expert + 1 shared expert，top-8）。\n\n**代价**：\n- 每个 MoE layer 必须 dispatch + combine 两次 All-to-All\n- 需要在数百个 expert 间维持 load balance\n- checkpoint 从「按层 shard」变成「按 expert shard」\n- 一个 expert GPU 挂了，全局 All-to-All 会 hang"},{"title":"Routed Expert vs Shared Expert","body":"现代大 MoE（DeepSeek-V2/V3）同时使用两类 expert：\n\n| 维度 | Routed Expert | Shared Expert |\n|---|---|---|\n| 激活方式 | top-K 选择，每 token 只过 K 个 | 所有 token 都过 |\n| 数量 | 几十到几百 | 通常 1-4 |\n| 学习目标 | 专业化分工 | 公共基线能力 |\n| 是否参与 All-to-All | 是 | 否（与 dense 同路径）|\n| EP 放置 | 按 expert shard 分到不同 GPU | 与 dense 层一起处理 |\n\nShared expert 承担「公共基线」，让 routed expert 只学差异，减少 expert 之间的冗余学习。"},{"title":"Expert Parallelism（EP）：把不同 expert 放不同 GPU","body":"以 DeepSeek-V3 为例，256 个 routed expert 每层约 11.3B 参数，58 个 MoE layer 合计约 650B routed expert 参数，加上 optimizer state 和 gradient，单卡或单 TP shard 完全放不下，必须引入 EP。\n\nEP rank mesh：\n`world_size = DP × PP × TP × EP × CP`\n- EP group：固定 (dp, pp, tp, cp)，跨 ep_rank\n- 每个 EP rank 持有 `N_experts / EP_size` 个 expert\n\nEP 与其他维度正交：\n- DP 切样本，每个 DP 副本内部独立 EP\n- TP 切 attention 和 shared FFN，EP 切 routed FFN\n- PP 切层段，EP 切每层内的 expert\n- CP 切序列，与 EP 完全独立\n\n**工程关键**：EP size 通常等于一个 NVSwitch domain（8 或 16 卡）。再大需要跨 IB，All-to-All 的 incast 压力急剧上升。"},{"title":"All-to-All：MoE 的真正瓶颈","body":"EP 引入每个 MoE layer 两次 All-to-All：\n\n- **dispatch**：token owner 把 token 发到 expert owner GPU\n- **combine**：expert owner 把计算结果发回 token owner\n\nbackward 还有两次反向 All-to-All（reverse-combine + reverse-dispatch）。若开 activation checkpointing 还会重跑 dispatch/combine。\n\n**All-to-All vs Ring AllReduce 的关键区别**：\n- Ring AllReduce：环形点对点，网络压力均匀\n- All-to-All：N×N 全互联，产生 **incast**——多个 sender 同时打向同一 receiver，交换机 buffer 容易溢出\n\n**体积估算**（DeepSeek-V3 风格，EP=64，top-8，hidden=7168）：\n每个 EP rank 每层 forward 的 dispatch send+recv 约 1.8-2.4 GB，combine 再加同量，单层 EP 通信 send+recv 约 3.6-4.8 GB，58 层 × 4 microbatch 合计约 0.8-1.1 TB/rank/step。\n\n**必须用 DeepEP 等 overlap 技术**将 dispatch/combine 与 expert 计算重叠，否则 MFU 仅为 dense 模型的 0.6 倍。","tip":"MoE 网络规划必须把 EP size、rail 数、交换机 buffer 同时考虑。按 dense DP AllReduce 规划的 IB 拓扑上线 MoE 后，All-to-All incast 极易打爆交换机 buffer。"},{"title":"Router 与负载均衡：避免 Expert 坍缩","body":"Router 是一个小线性层（hidden → N expert 得分），经 softmax 后 top-K 选 expert。\n\n**Top-K 的离散性**：\n- `topk_idx`（membership）是离散的，不可微——主 loss 不对「选了哪个 expert」求导\n- `gates`（selected gate weight）是连续的，主 loss 通过它回到 router logits\n- 未被选中的 expert 对这个 token 没有 expert-path gradient\n\n**坍缩风险**：无约束 router 会迅速把所有 token 喂给少数热门 expert（强者愈强），导致大部分 GPU 空转、少数 GPU OOM。\n\n**两种平衡策略**：\n1. **Aux Loss**（Switch/GShard 风格）：`L_aux = α × N × Σ_i (f_i × P_i)`，让 router 不把高概率给过载 expert。代价：aux loss 与主 loss 梯度方向冲突。\n2. **Loss-Free Balancing**（DeepSeek-V3）：用动态 bias 调整 expert score，过载 expert 降 bias，欠载 expert 升 bias，不污染主 loss 梯度。**bias 必须随 checkpoint 保存**。","tip":"Router 数值精度很重要：BF16 softmax 在不同 GPU 上可能产生不一致的 top-K 结果，建议用 FP32 router 保证 reproducibility。"},{"title":"Capacity Factor 与 Dropless MoE","body":"**Fixed-Capacity（dropping）MoE**：\n- 每个 expert 预分配 `capacity = ceil(capacity_factor × avg_tokens)` 个 token 槽\n- 超过 capacity 的 token 被 drop 或 overflow\n- `capacity_factor=1.0`：高 drop 风险；`1.25`：常见起点；`2.0`：显存翻倍但基本不 drop\n\n**Dropless MoE**（DeepSeek-V3 风格）：\n- 不设固定 capacity，按真实 token 数用 alltoallv/flex dispatcher 动态搬运\n- 不默认丢 token，但 buffer 峰值不可静态锁死\n\n**监控指标的差异**：\n- Dropping MoE：重点看 `drop_rate`（每层，建议 < 5%；> 20% 持续 1k step 则告警）\n- Dropless MoE：重点看 `max_load/avg_load`、`p99 expert tokens`、`dispatch buffer peak`、`straggler rank`、`overflow fallback count`\n\n**Dropless 的异常处理**必须是 EP group 级事务：任一 rank buffer OOM 或通信超时，必须全 group 停在同一边界（fail closed），不允许部分 rank commit、部分 rank fallback——否则后续 PP/DP collective 会看到不一致 activation。"},{"title":"MoE Checkpoint：Expert Shard 与 EP Reshape","body":"MoE 的 checkpoint 比 dense 模型多了一个 expert 维度：\n\n**最小 checkpoint manifest 结构**：\n```\n{\n  \"schema\": \"moe-v1\",\n  \"world_size\": {\"DP\":2, \"PP\":4, \"TP\":4, \"EP\":64, \"CP\":1},\n  \"num_experts_per_layer\": 256,\n  \"expert_owner\": {\"layer_id\": {\"expert_id\": \"ep_rank\"}},\n  \"router_bias_state\": {\"layer_id\": {\"expert_id\": bias_value}},\n  \"optimizer_state_schema\": \"expert shard\",\n  \"global_step\": 10000\n}\n```\n\n**EP Reshape（训练 EP=64 → 推理 EP=8）**：\n- 256 个 expert 从 64 个 GPU 重新分配到 8 个 GPU（每 GPU 从 4 个 expert 增加到 32 个）\n- 必须用 expert manifest 重映射，不能直接按 rank 索引\n- loss-free balancing 的 bias 也要随 expert 迁移\n\n**ETP 的去重问题**：若 `ETP=1` 且 dense TP rank 复制 expert 参数，多个 TP rank 写出相同 expert。checkpoint writer 必须选 canonical replica 或校验 checksum 后去重，否则 EP reshape 会把复制品误认为独立 shard。","reveal":{"q":"为什么 MoE 的 All-to-All dispatch 在网络上比 DDP 的 AllReduce 更容易引发拥塞？","a":"AllReduce 是环形点对点通信，流量分布均匀，每个链路负载相似。All-to-All 是 N×N 全互联，多个 sender 同时打向同一 receiver（incast 模式），交换机的 shared buffer 容易被打满，ECMP 哈希不均匀时某些链路成为热点。加上 MoE 的 token 路由是动态的（不均匀），incast 程度比固定流量模式更难预测。"}}],"questions":[{"type":"choice","prompt":"MoE 中 router 的 top-K membership（topk_idx）和 gate weight（gates）分别是否可微？","options":["两者都可微","topk_idx 不可微，gates 可微","topk_idx 可微，gates 不可微","两者都不可微"],"answer":1,"explain":"topk_idx 是离散选择，不可微——主 loss 不对「选了哪个 expert」求导。gates 是被选中 expert 的连续权重，主 loss 通过 ∂y/∂gates = Expert_i(x) 传回 router logits，是可微的。","id":"u4-l4-q1"},{"type":"judge","prompt":"MoE 的 forward 在每个 MoE layer 有 2 次 All-to-All（dispatch + combine），backward 没有额外 All-to-All。","answer":false,"explain":"backward 还有 2 次反向 All-to-All（reverse-combine + reverse-dispatch），完整 forward+backward = 4 次 All-to-All。若开 activation checkpointing 覆盖 MoE layer，还会重跑 dispatch/combine（额外 2 次）。","id":"u4-l4-q2"},{"type":"choice","prompt":"与 Ring AllReduce 相比，MoE All-to-All 最显著的网络挑战是什么？","options":["总通信字节数更大","N×N 全互联产生 incast，交换机 buffer 容易打爆","需要更多 CUDA stream","不支持 NVLink"],"answer":1,"explain":"Ring AllReduce 是点对点环形，流量均匀。All-to-All 是 N×N 全互联，多个 sender 同时向同一 receiver 发送（incast），交换机 shared buffer 容易溢出，ECMP 哈希不均时某些链路成为热点。","id":"u4-l4-q3"},{"type":"match","prompt":"将左侧 MoE 负载均衡策略与其特点匹配","left":["Aux Loss（Switch/GShard）","Loss-Free Balancing（DeepSeek-V3）","Capacity Factor dropping","Dropless MoE"],"right":["λ × Σ(f_i × P_i) 加入主 loss，但与主 loss 梯度方向可能冲突","动态 bias 调整 expert score，不污染主 loss 梯度，bias 须随 checkpoint 保存","预分配固定 token 槽，超出 token 被 drop，监控 drop_rate","按实际 token 数动态 dispatch，监控 max_load、buffer peak 和 straggler"],"explain":"四种策略各有取舍：Aux loss 简单但有梯度污染；Loss-free balancing 无污染但需维护 bias 状态；Capacity factor 工程简单但有 token drop；Dropless 不丢 token 但 buffer 管理更复杂。","id":"u4-l4-q4"},{"type":"input","prompt":"DeepEP 等优化通过什么手段将 MoE 通信开销从 30-50% step time 大幅降低？（填核心机制，不超过 15 字）","accept":["将 dispatch/combine 分 chunk 与 expert 计算重叠","通信与计算 overlap（chunking）","把 dispatch 与 expert GEMM 并行执行","分块 dispatch 与 expert 计算并行"],"explain":"DeepEP 将 dispatch/combine 拆成多个 chunk，每个 chunk 的通信与下一个 chunk 或当前 chunk 的 expert 计算并行执行，从而将 All-to-All 通信隐藏在 expert GEMM 计算中，大幅减少暴露通信。","id":"u4-l4-q5"},{"type":"judge","prompt":"训练时 EP=64 的 MoE checkpoint，可以直接加载到 EP=8 的推理环境，因为 expert 参数不变。","answer":false,"explain":"EP reshape 需要重新映射 expert 到新 rank。EP=64 时每 GPU 持有 4 个 expert（256/64），EP=8 时每 GPU 需持有 32 个（256/8）。必须通过 expert manifest（记录 expert_id → ep_rank 的映射）做 reshard，还要迁移 loss-free balancing 的 bias 状态。","id":"u4-l4-q6"},{"type":"choice","prompt":"Dropless MoE 发生通信超时时，正确的处理方式是？","options":["每个 rank 各自判断是否 fallback，速度快","全 EP group 停在同一边界（fail closed），统一选择 fallback","忽略超时继续训练，下一步会自动修复","只需要通信方（expert owner）处理，token owner 继续"],"answer":1,"explain":"dropless MoE 的异常处理必须是 EP group 级事务。若允许部分 rank commit、部分 rank fallback，后续 PP/DP collective 会看到不一致 activation，导致不可复现的 loss spike 或 hang。正确做法是全 group fail closed，再统一 replay。","id":"u4-l4-q7"},{"type":"match","prompt":"将左侧 MoE 监控场景与最关键的监控指标匹配","left":["Dropping MoE 健康监控","Dropless MoE 健康监控","Expert 坍缩早期预警","EP 网络问题诊断"],"right":["drop_rate（每层，告警阈值 >20% 持续 1k step）","p99 expert tokens、dispatch buffer peak、overflow fallback count","某几个 expert 的 f_i（实际选中频率）远高于均值","alltoallv bytes 的 send/recv rank skew，某些 rank 成为 incast 热点"],"explain":"四种场景的监控重点不同：dropping MoE 看 drop rate，dropless 看 buffer 和 straggler，expert 坍缩看负载分布是否均匀，EP 网络问题看通信流量是否均匀。","id":"u4-l4-q8"},{"type":"judge","prompt":"Shared expert 参与 Expert Parallelism 的 All-to-All dispatch，因为它也是 FFN 的一部分。","answer":false,"explain":"Shared expert 不参与 All-to-All。每个 token 都过 shared expert，因此它与 dense 层一样放在相同的计算路径里，不需要按 expert 路由和 dispatch。只有 routed expert 才参与 All-to-All。","id":"u4-l4-q9"},{"type":"choice","prompt":"DeepSeek-V3 有 671B 总参数，每 token 激活约 37B（256 routed expert，top-8）。Dense 等效模型若要达到同等每 token FLOPs，其总参数量约为多少？","options":["约 37B（与激活参数量相同）","约 671B（与总参数量相同，全部激活）","约 150B（折中估算）","约 74B（top-8 比例乘以 256）"],"answer":0,"explain":"MoE 的每 token FLOPs 主要由激活参数量决定（约 37B），而不是总参数量（671B）。Dense 模型若要达到同等每 token 计算量，其参数量约等于 MoE 的激活参数量（37B）。MoE 的核心价值是参数容量（671B）与每 token FLOPs（~37B）解耦。","id":"u4-l4-q10"},{"type":"judge","prompt":"在 MoE 的 Aux Loss 负载均衡（Switch/GShard 风格）中，辅助损失 L_aux 与主任务 loss 的梯度方向完全一致，因此不会对训练精度产生负面影响。","answer":false,"explain":"Aux Loss 要求路由分配更均匀，而主 loss 要求选最有能力的 expert，两者梯度方向存在冲突。α 系数过大会过度干预 router，损害任务性能；过小则平衡效果不足。DeepSeek-V3 正是为了消除这种梯度冲突才引入 Loss-Free Balancing（动态 bias 调整）。","id":"u4-l4-q11"},{"type":"input","prompt":"MoE 的 Loss-Free Balancing 中，checkpoint 除了 model weights 和 optimizer state，还必须额外保存哪类状态？（填关键词）","accept":["router bias","bias","动态 bias","router_bias","expert bias"],"explain":"Loss-Free Balancing 通过动态调整每个 expert 的 bias（过载 expert 降 bias，欠载 expert 升 bias）来维持负载均衡，而不修改主 loss 梯度。这些 bias 是训练状态的一部分，必须随 checkpoint 保存，否则恢复后平衡状态被重置，可能引发短期 expert 坍缩。","id":"u4-l4-q12"},{"type":"choice","prompt":"MoE 的 EP size 通常设置为一个 NVSwitch domain（8 或 16 卡）而非更大，主要原因是什么？","options":["EP size 超过 16 时 router 无法正确计算 top-K","EP size 越大，expert 参数量不变但 FLOPs 增加","EP size 超出单节点后需要跨 IB 做 All-to-All，incast 压力急剧上升","EP size 过大会导致 DP 维度的 AllReduce 通信量成指数增加"],"answer":2,"explain":"节点内 All-to-All 走 NVSwitch（高带宽、低延迟、无 incast 问题）；跨节点需要走 IB/RoCE，All-to-All 的 incast 模式（N×N 全互联）在 IB 交换机上容易打爆 shared buffer。EP size 越大，incast 程度越严重，All-to-All 延迟随之上升，成为主要瓶颈。","id":"u4-l4-q13"},{"type":"judge","prompt":"Dropless MoE 不设固定 capacity，因此不需要监控任何 overflow 相关指标，只需关注 drop_rate 即可。","answer":false,"explain":"Dropless MoE 不设固定 capacity，无法用 drop_rate 指标。应重点监控 `max_load/avg_load`、`p99 expert tokens`、`dispatch buffer peak`、`straggler rank` 和 `overflow fallback count`。buffer 峰值不可静态锁死，动态 buffer OOM 需要 EP group 级事务处理。","id":"u4-l4-q14"},{"type":"match","prompt":"将左侧 MoE EP Reshape 步骤与其关键操作匹配","left":["确定每个 expert 在新 EP 配置下的目标 ep_rank","迁移 Loss-Free Balancing 的 bias","处理 ETP>1 时多个 TP rank 写出的相同 expert 副本","验证 reshape 后权重正确性"],"right":["通过 expert_owner manifest 重映射，不能按 rank 索引直接计算","bias 必须随 expert 一起迁移到新 ep_rank，否则平衡状态丢失","checkpoint writer 选 canonical replica 或 checksum 去重，避免误认为独立 shard","比对 reshape 前后每个 expert 的权重 checksum，确认无静默错误"],"explain":"EP Reshape 的四个关键步骤各有陷阱：manifest 重映射避免 rank 索引混淆；bias 随 expert 迁移保持均衡状态；ETP 去重避免把副本误当独立 shard；checksum 验证避免静默 corruption。","id":"u4-l4-q15"},{"type":"choice","prompt":"Fixed-Capacity MoE 的 capacity_factor 设置从 1.0 增大到 2.0，对训练的主要影响是什么？","options":["token drop rate 降低，但每层 All-to-All 通信量翻倍","token drop rate 降低，expert 的 buffer 显存占用约翻倍，且通信 buffer 也随之增大","token drop rate 不变，仅 activation 显存增大","capacity_factor 只影响推理效率，对训练没有影响"],"answer":1,"explain":"capacity_factor 增大时，每个 expert 预分配的 token 槽翻倍，buffer 显存随之增大约 2 倍，dispatch 通信 buffer 也增大。好处是 drop rate 降低（从高风险到基本不丢）；代价是显存和通信 buffer 成本翻倍。","id":"u4-l4-q16"},{"type":"judge","prompt":"MoE 训练中，如果某个 EP group 中的一个 GPU 挂掉，只影响该 GPU 持有的 expert，其他 EP rank 的 All-to-All 仍可正常完成。","answer":false,"explain":"All-to-All 是同步集合通信，要求 EP group 中所有 rank 参与。任一 rank 挂掉后，其他 rank 的 All-to-All 会无限等待（hang），整个 EP group 被阻塞，最终触发 NCCL timeout，整个训练作业失败。这是 MoE 比 dense 训练对单点故障更敏感的原因。","id":"u4-l4-q17"},{"type":"input","prompt":"MoE 的 BF16 router 在不同 GPU 上可能产生不一致的 top-K 结果，推荐用什么 dtype 来保证 reproducibility？（填 dtype 名称）","accept":["FP32","fp32","float32","float"],"explain":"BF16 的精度（尾数 7 bit）较低，softmax 在不同 GPU 上舍入方式不同，可能导致 top-K 边界样本的 expert 选择不一致（non-determinism）。用 FP32 router 计算 expert score 可以保证 reproducibility，代价是 router 计算开销略有增加（但 router 本身参数量极小）。","id":"u4-l4-q18"},{"type":"match","prompt":"将左侧 MoE 故障现象与其最可能的根因匹配","left":["大部分 GPU 计算利用率低，少数 GPU 持续 OOM 或慢步","All-to-All 延迟突增，IB 交换机监控显示丢包","loss 下降正常，但长时间后 expert 利用率分布极不均匀","EP Reshape 后推理 loss 比训练 loss 高出很多"],"right":["Router 坍缩：无负载均衡约束导致 token 集中于少数热门 expert","All-to-All incast 打爆交换机 shared buffer，导致丢包和重传","Aux Loss 系数 α 过小，负载均衡约束不足，expert 逐渐分化","EP Reshape 未使用 expert manifest 重映射，expert 参数错位"],"explain":"MoE 的四类典型故障：GPU 负载不均→router 坍缩；IB 丢包→incast；expert 利用率不均匀→aux loss 不足；reshape 后 loss 异常→expert 参数映射错误。","id":"u4-l4-q19"}]},{"id":"u4-l5","title":"显存优化、Checkpoint 与恢复","subtitle":"从 activation checkpointing 到 true resume 的生产可靠性控制面","intro":[{"title":"训练是一个长时间运行的状态机","body":"长训练任务不是「一次跑完」的程序，而是持续数天到数月的分布式状态机。它一定会遇到 GPU、网络、存储、节点或代码发布失败。\n\nCheckpoint 不是「文件保存动作」，而是**恢复协议**。它必须回答：\n- 状态有哪些（params、grads、optimizer、scheduler、loss scaler、RNG、dataset cursor、global step、parallel metadata）\n- 状态在哪里（HBM、CPU DRAM、NVMe、对象存储）\n- 谁拥有写入权（每个 rank 写 shard vs coordinator 聚合）\n- 何时可见（半成品不能被 `latest` 指针暴露）\n- 如何验证（shape、dtype、checksum、world size、parallel layout）\n- 如何清理（retention 不能删掉最后一个可恢复版本）"},{"title":"显存预算的六项","body":"训练峰值显存 = `params + grads + optimizer_states + activations + comm_buffers + ckpt_buffer + fragmentation`\n\n常见遗漏：\n- `M_ckpt_buffer`：async checkpoint staging 在大规模作业里经常被遗忘\n- `M_frag`：allocator fragmentation 可使 reserved >> allocated，让「理论能放下」的作业实际 OOM\n\n**Activation checkpointing 的代价模型**：\n- 收益：activation 内存约降低 30%-70%（取决于 granularity）\n- 代价：step time 增加 5%-30%（重算 forward）\n- 适用：activation 是峰值主因、GPU compute 有余量\n- 不适用：step 已被 compute 打满，或重算破坏 kernel overlap\n\n**Offload 的边界**：PCIe Gen4 单向 ~32 GB/s，NVMe 更低，只有在计算能掩盖搬运延迟时才划算。offload 后 step time 从 1.2s 变 4.8s 是常见「解决 OOM 但 GPU duty cycle 只有 35%」的反例。"},{"title":"Checkpoint Schema：最小真实恢复集","body":"「只保存 model.state_dict()」= warm start，不是 true resume。True resume 需要：\n\n| 状态 | 不保存的后果 |\n|---|---|\n| model params | 权重回退 |\n| optimizer states（Adam m/v）| loss 短期跳变，收敛轨迹改变 |\n| scheduler | LR 重复 warmup 或提前 decay |\n| RNG（CPU/CUDA/sampler/worker）| dropout/数据增强轨迹变化 |\n| dataset cursor | 重复或跳过样本 |\n| global step | 日志、LR、eval、checkpoint cadence 错位 |\n| parallel metadata | shard 无法映射或静默错位 |\n| precision state（GradScaler/FP8 amax）| scale 错误，FP8 冷启动 |\n\n生产 checkpoint 目录结构：写入临时路径 `step_N.tmp/`，所有 shard 完整后 coordinator 原子 rename 到 `step_N/`，最后更新 `latest.json` 的 CAS generation。对象存储没有 POSIX rename 时，用 `latest.json` 的 compare-and-swap 原子暴露版本。","tip":"Dataset cursor 必须保存 per-DP-rank sampler epoch/position、streaming shard offset、packing residual 和 gradient accumulation substep，而不只是全局 consumed_tokens。"},{"title":"Atomic Visibility：两阶段写入","body":"Checkpoint 可见性必须是原子的，否则部分写入会被恢复流程读到：\n\n1. 所有 writer 写临时 shard，`fsync` 文件和目录\n2. coordinator 验证 shard count、size、checksum、tensor name/shape/dtype\n3. POSIX 文件系统：`rename(step_N.tmp, step_N)` + `fsync(ckpt_dir)`\n4. 对象存储：写不可变 step prefix，`latest.json` 用 CAS/generation 条件更新\n5. post-publish barrier：所有 rank 必须观察到同一个 latest pointer 才能继续训练\n\n**绝不能** 让部分 rank 的写入被暴露，也不能让 `latest.json` 指向 `status=writing` 或 `status=failed` 的 manifest。","tip":"任一 rank 写入失败必须让整个 checkpoint 标记 failed，不能只记日志。async checkpoint 的后台失败必须回传给训练进程，阻止 latest 更新。"},{"title":"RPO/RTO：可靠性量化","body":"**RPO（Recovery Point Objective）**：故障后最多丢失多少训练进度。\n**RTO（Recovery Time Objective）**：从故障检测到恢复训练吞吐达标最多多久。\n\nRPO 计算基准是已验证且被 pointer 暴露的 checkpoint，不是「开始写 checkpoint 的时间」：\n\n`RPO(t_fail) = t_fail - capture_time(last_validated_capture_step(t_fail))`\n\n**常见 RPO 失效场景**：保存间隔 30 min，step_B 在 03:30 capture、03:38 仍未更新 latest pointer，03:35 故障 → RPO = 35 min（不是 5 min）。若 step_B validation 失败，52 min 后故障 RPO = 52 min。\n\n**Async checkpoint 的正确状态链**：\n`StepCommitted → FenceStreams → SnapshotStaged → TrainMayAdvance → FlushAttempt → Validate → PublishCAS → PostPublishBarrier`\n\n`last_validated_capture_step` 只能在 `PostPublishBarrier` 后更新。\n\n保存间隔建议：百卡训练 30-60 min，千卡预训练按 tokens+wall clock 双触发，checkpoint pause 控制在 step time budget 的 1%-3%。"},{"title":"跨并行策略恢复支持矩阵","body":"恢复时最危险的不是立即报错，而是静默降级成 warm start。平台需要明确支持矩阵：\n\n| 变化类型 | True Resume | Warm Start | 需要工具 |\n|---|---|---|---|\n| 只替换物理节点，world size 不变 | 支持 | 支持 | 不需要 |\n| DP degree 变化，TP/PP/FSDP 不变 | 有条件 | 支持 | optimizer reshard |\n| TP degree 变化 | 通常不支持 optimizer | 支持权重 | tensor reshard |\n| PP degree 变化 | 通常不支持 optimizer | 支持权重 | layer/stage remap |\n| FSDP wrap policy 变化 | 不支持 | 支持权重 | unflatten/reshard |\n| EP/MoE expert parallel 变化 | 不支持 | 有条件支持权重 | offline conversion |\n\n**核心原则**：平台不能在不满足 true resume 条件时静默降级成 warm start。恢复校验失败要 quarantine 当前 checkpoint 并回退到 previous validated。","tip":"FP8 checkpoint 必须包含 amax history、scale、scale_inv 和 FP8 recipe。恢复时丢失这些 metadata 会导致前几步用错误 scale，表现为 silent loss parity drift。"},{"title":"Allocator Fragmentation：理论容量与实际 OOM 的差距","body":"PyTorch CUDA allocator 会缓存显存块，常见现象：\n- `allocated` 不高，但 `reserved` 很高（碎片化）\n- `nvidia-smi` 显示还有空闲，但大块 allocation 失败\n- 训练到某个 sequence length 或 eval step 才 OOM\n\n诊断命令：\n`torch.cuda.memory_summary(device=0, abbreviated=False)`\n\n常见动作：\n- `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:256` 做 A/B 验证\n- 固定 sequence length bucket，减少动态 shape 抖动\n- checkpoint/eval 前释放不再使用的引用\n- 避免在 hot path 反复创建不同大小的临时 tensor","reveal":{"q":"为什么 `last_validated_capture_step` 只能在 PostPublishBarrier 后更新，而不是在 SnapshotStaged 后？","a":"SnapshotStaged 只表示 GPU tensor 已经转移到 staging buffer，但 flush 到存储、checksum 验证、manifest 发布和 latest pointer 更新都还没完成。只有 PublishCAS 成功且 PostPublishBarrier 让所有 rank 观察到同一个 latest pointer 后，该 checkpoint 才真正可以被恢复流程使用。此前任何阶段失败都可能产生不完整的 checkpoint，不能作为 RPO 计算基准。"}}],"questions":[{"type":"judge","prompt":"只保存 model.state_dict() 就足以实现 true resume（完整恢复），因为模型权重包含了所有训练进度。","answer":false,"explain":"只保存 model.state_dict() 是 warm start，不是 true resume。恢复后 optimizer momentum（Adam m/v）会丢失导致 loss 跳变，LR schedule 重新从头开始，RNG 不连续导致 dropout/数据增强轨迹变化，dataset cursor 重置导致样本重复或跳过。","id":"u4-l5-q1"},{"type":"choice","prompt":"以下哪个 checkpoint 状态可以被 latest pointer 暴露给恢复流程？","options":["status=writing（仍在写入）","status=validated（所有 shard 校验通过）","status=failed（写入失败）","status=quarantined（恢复校验失败）"],"answer":1,"explain":"只有 status=validated 的 checkpoint 才能被 latest pointer 暴露。writing 表示还未完成，failed 表示写入校验失败，quarantined 表示恢复时失败。恢复流程必须 fail closed 拒绝这三种状态。","id":"u4-l5-q2"},{"type":"match","prompt":"将左侧显存优化手段与其主要收益匹配","left":["Activation checkpointing","Optimizer state sharding（ZeRO）","CPU offload（ZeRO-Offload）","Allocator expandable_segments"],"right":["用重算 forward 换取 activation 显存降低 30-70%","降低 optimizer state 的单卡常驻量，端到端约 2.9×-N× reduction","将 optimizer state 移到 CPU DRAM，GPU 容量手段但可能大幅降低 GPU duty cycle","减少 CUDA allocator 的内存碎片，缓解 reserved >> allocated 问题"],"explain":"四种手段解决不同问题：activation checkpointing 换算力换显存，ZeRO 分片状态，offload 把状态搬到慢设备，expandable_segments 减少 allocator 碎片。选择时必须考虑各自的代价。","id":"u4-l5-q3"},{"type":"input","prompt":"保存间隔 30 分钟，step_B 在 03:30 capture，03:38 尚未更新 latest pointer，03:35 发生故障，RPO 是多少分钟？","accept":["35","35 分钟"],"explain":"RPO = t_fail - capture_time(last_validated_capture_step(t_fail))。03:35 故障时，step_B 尚未被 latest pointer 暴露，last_validated 是上一个已发布的 checkpoint（假设在 03:00 capture），RPO = 35 - 0 = 35 分钟，不是 5 分钟。","id":"u4-l5-q4"},{"type":"judge","prompt":"TP degree 从 8 改为 4 后，如果权重 shape 能对上，可以直接做 true resume（包括 optimizer state）而无需离线 reshard 工具。","answer":false,"explain":"TP degree 变化会改变 tensor shard axis，optimizer state（Adam m/v）与参数 shard 一一对应，shard 重新分配后无法直接映射。通常只支持权重的 warm start，true resume 需要专门的 tensor reshard 工具链，且必须经过测试才能使用。","id":"u4-l5-q5"},{"type":"choice","prompt":"以下哪种情况 activation checkpointing 最不划算？","options":["训练大型 LLM 时 HBM 主要被 activation 占满","长上下文（128K token）训练，activation 随 seq 二次增长","训练 step 已经被 compute 完全打满，MFU 接近上限","batch size 受限于显存，无法增大"],"answer":2,"explain":"activation checkpointing 用重算 forward 换显存，代价是增加 compute 时间 5%-30%。如果 step 已经被 compute 完全打满，额外的重算会直接线性拉长 step time，MFU 下降，得不偿失。前三种情况（activation 是峰值主因）才是它的适用场景。","id":"u4-l5-q6"},{"type":"match","prompt":"将左侧 async checkpoint 状态与其含义匹配","left":["SnapshotStaged","TrainMayAdvance","PublishCAS","PostPublishBarrier"],"right":["GPU tensor 已转移到 staging buffer，但 flush 尚未完成","训练可进入下一 step，但 checkpoint 仍在后台 flush","manifest validated，latest.json 以 CAS/generation 原子更新","所有 rank 观察到同一 latest pointer，该 step 计入 last_validated_capture_step"],"explain":"async checkpoint 的状态链要严格按顺序推进。SnapshotStaged 只是 staging 完成，PublishCAS 是发布，PostPublishBarrier 是所有 rank 确认可见。只有 PostPublishBarrier 后才能更新 last_validated_capture_step。","id":"u4-l5-q7"},{"type":"judge","prompt":"FP8 训练的 checkpoint 只需保存模型权重和 optimizer state，FP8 的 amax history 和 scale 可以在恢复后重新冷启动。","answer":false,"explain":"FP8 的 amax history、scale、scale_inv 是训练状态。恢复时如果丢失这些 metadata，前几步会用错误的 scale，产生 silent loss parity drift——训练能继续跑，但数值轨迹已经偏离。生产 checkpoint 必须保存完整的 FP8 precision state。","id":"u4-l5-q8"},{"type":"choice","prompt":"True resume 与 warm start 的根本区别是什么？","options":["True resume 从 step 0 开始训练，warm start 从任意 step 开始","True resume 恢复全部训练状态（包括 optimizer、scheduler、RNG、dataset cursor 等），损失曲线与原轨迹连续；warm start 只恢复权重，损失曲线会跳变","True resume 使用更高精度的 checkpoint 文件，warm start 使用压缩版本","两者在工程上完全等价，区别只是营销说法"],"answer":1,"explain":"True resume = 恢复所有训练状态（model params、optimizer states、scheduler、RNG、dataset cursor、global step、precision state）后，loss 曲线、LR 曲线、token 顺序与原运行完全连续。Warm start 只恢复模型权重，optimizer momentum、LR schedule 等重置，损失曲线会出现可见跳变。","id":"u4-l5-q9"},{"type":"judge","prompt":"Async checkpoint 在 `TrainMayAdvance` 状态后，训练作业可以继续推进到下一步，此时即使 checkpoint 还在写磁盘，也是安全的。","answer":true,"explain":"`TrainMayAdvance` 是 async checkpoint 状态机中的关键门槛：训练状态已完整 snapshot 到 CPU DRAM 或 NVMe staging 区，GPU 可以安全推进下一训练步，不会影响正在异步刷写的 checkpoint 内容。这是 async checkpoint 降低 I/O 停顿的核心机制。","id":"u4-l5-q10"},{"type":"input","prompt":"PyTorch CUDA allocator 碎片化诊断命令是哪个？（填核心 Python 调用，格式如 `xxx.yyy()`）","accept":["torch.cuda.memory_summary()","torch.cuda.memory_summary","memory_summary()"],"explain":"`torch.cuda.memory_summary(device=0, abbreviated=False)` 可显示 allocated vs reserved 的差距、block 大小分布、fragmentation ratio 等，是诊断「理论能放下但实际 OOM」的首选工具。当 reserved >> allocated 时说明碎片化严重。","id":"u4-l5-q11"},{"type":"choice","prompt":"以下哪种 checkpoint 原子性写入方案在对象存储（如 S3）上是正确的？","options":["直接覆盖写 `step_N/` 前缀下的所有文件，完成后更新 `latest.json`","写不可变 `step_N/` 前缀，所有 shard 完整后用 CAS/generation 条件更新 `latest.json`","先更新 `latest.json` 指向新 step，再异步完成剩余 shard 写入","每个 rank 独立更新 `latest.json`，最后写完的 rank 的版本胜出"],"answer":1,"explain":"对象存储不支持 POSIX rename，原子性靠：写不可变 step prefix（所有 shard 幂等可重试），全部完成后用条件写（CAS 或 generation check）原子更新 `latest.json`。先更新 pointer 会暴露未完成的 checkpoint；多 rank 独立更新会造成竞争写，最终 pointer 值不确定。","id":"u4-l5-q12"},{"type":"match","prompt":"将左侧 checkpoint 内容与「不保存的后果」匹配","left":["scheduler state","RNG state（CPU/CUDA）","dataset sampler cursor","GradScaler state（FP16 训练）"],"right":["LR 重复 warmup 或提前 decay，收敛轨迹不可复现","dropout 掩码和数据增强随机顺序改变，训练结果不可复现","恢复后重复消费已训练样本或跳过部分样本","scale 重置为初始值，前几步可能因 scale 过高产生大量 overflow"],"explain":"scheduler 不保存导致 LR 错位；RNG 不保存导致随机性不一致；dataset cursor 不保存导致样本重复或跳过；GradScaler 不保存导致 FP16 loss scale 重置，overflow 频繁。每项都有独立可观察的后果。","id":"u4-l5-q13"},{"type":"choice","prompt":"DP degree 从 8 变为 16（TP/PP/FSDP 不变），训练 checkpoint 是否支持 true resume？","options":["不支持，DP 变化导致权重 shard 数量改变，无法加载","有条件支持：权重和 optimizer state 需要经过 reshard 工具处理才能 true resume","完全支持，DP 变化不影响任何 shard，直接加载","只支持 warm start，optimizer state 的 DP reshard 工具尚不存在"],"answer":1,"explain":"DP degree 变化时，optimizer state 的 shard 需要重新分配（原 8 shard → 16 shard），权重本身也需要 reshard。平台的跨并行策略支持矩阵中，「DP degree 变化，TP/PP/FSDP 不变」为「有条件 true resume」，需要 optimizer reshard 工具处理。未经 reshard 直接加载会导致静默错位。","id":"u4-l5-q14"},{"type":"judge","prompt":"Activation checkpointing 对所有训练场景都有收益，只要显存不够用就应该开启，不需要分析 step 是否被 compute 打满。","answer":false,"explain":"Activation checkpointing 用算力换显存（step time 增加 5%-30%）。若 step 已经被 compute 打满（compute-bound），开启 AC 会直接拉长 step time，降低 tokens/s，而且可能破坏 kernel overlap。只有 activation 是峰值主因且 GPU compute 有余量时，AC 才真正划算。","id":"u4-l5-q15"},{"type":"input","prompt":"PyTorch 中用于减少 allocator 碎片的关键环境变量（可配置 expandable_segments 和 max_split_size_mb）是哪个？","accept":["PYTORCH_CUDA_ALLOC_CONF","PYTORCH_CUDA_ALLOC_CONF=","pytorch_cuda_alloc_conf"],"explain":"`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:256` 可配置 PyTorch CUDA allocator 行为：expandable_segments 允许 segment 动态扩展，减少碎片；max_split_size_mb 限制单块最大分裂尺寸，降低大块 allocation 失败概率。","id":"u4-l5-q16"},{"type":"choice","prompt":"checkpoint `post-publish barrier` 的作用是什么？","options":["让所有 rank 等待 I/O 写入完成后再 fsync","确保所有 rank 观察到同一个 latest pointer 后才能继续训练，避免部分 rank 基于旧 pointer 恢复","对 checkpoint 文件做 checksum 验证，确认无数据损坏","通知监控系统 checkpoint 已成功写入，触发告警清除"],"answer":1,"explain":"post-publish barrier 是两阶段写入的最后一步：coordinator 更新 latest pointer 后，所有 rank 必须先观察到同一个（新的）latest pointer，才能安全继续训练。若某些 rank 仍读到旧 pointer，恢复时会基于不同 checkpoint 版本，导致状态不一致。","id":"u4-l5-q17"},{"type":"match","prompt":"将左侧 checkpoint 显存六项与其常被遗漏的原因匹配","left":["M_ckpt_buffer（async checkpoint staging）","M_frag（allocator fragmentation）","M_comm_buffers（NCCL/FSDP 通信缓冲）","M_activations（峰值 activation）"],"right":["大规模作业 async staging 在 CPU DRAM 不够时会溢出到 GPU，但计算显存预算里常不计入","reserved >> allocated 时 nvidia-smi 显示空闲但大块 allocation 仍会 OOM","AllGather/ReduceScatter 的临时 buffer 在 profiler 里不直接显示为 peak","随 batch × seq × hidden × layers 增长最快，且在短序列 baseline 上很难被预估到长序列规模"],"explain":"checkpoint staging buffer 常被遗漏（只计入了 params/grads/Adam）；fragmentation 导致理论足够实际 OOM；通信 buffer 在 profiler 里不直观；activation 随 batch/seq/layers 超线性增长，baseline 低估长序列场景。","id":"u4-l5-q18"}]},{"id":"u4-l6","title":"对齐训练与后训练基础设施","subtitle":"SFT、RLHF/PPO、DPO、GRPO 的系统形态与多模型版本治理","intro":[{"title":"后训练的不可化简问题","body":"预训练把通用能力写进参数，后训练把能力塑造成可交付行为。它的核心矛盾是：**偏好信号不在原始语料里**，来自示范数据、chosen/rejected 偏好对、Reward Model、规则 reward、LLM judge、红队集和线上策略。\n\nPPO/RLHF 不是「换一个 loss 的训练脚本」，而是一个同时运行**推理、打分、训练、评测、checkpoint 和发布门禁**的多模型系统。系统中心是闭环控制，而不是高吞吐训练 step。\n\n三条并行路径：\n- **生成路径**：actor/policy 对 prompt 生成 response（rollout）\n- **反馈路径**：reference 计算 KL 锚点，RM 打分，critic 估 advantage\n- **更新路径**：training engine 消费带 reward 的样本更新 actor，再同步权重回 rollout engine\n\n**闭环版本一致性是核心**：actor checkpoint 是 A，reference artifact 是 B，RM prompt template 是 C，rollout buffer 却来自旧 actor——这种系统能继续跑，但曲线和发布结论都不可信。"},{"title":"各后训练形态的系统特征","body":"判断一个作业是「SFT 类」还是「RL 类」的最简标准：**是否需要当前 policy 生成新 response**。\n\n| 形态 | 系统中心 | 模型数 | 主要瓶颈 | 典型 checkpoint |\n|---|---|---|---|---|\n| SFT | 离线监督训练 | 1 | 数据质量、显存 | model + tokenizer + chat_template + data_version |\n| DPO | 离线偏好优化 | policy + frozen reference | reference 一致性、beta | policy + frozen ref artifact hash |\n| RM 训练 | pairwise/ranking 训练 | 1 | 标注噪声、长度偏差 | reward model + reward head + label schema |\n| PPO/RLHF | 在线 rollout + RL update | actor + reference + reward + critic | rollout 吞吐、RM 延迟、KL 稳定性 | 多模型 manifest + buffer + controller state |\n| GRPO | 在线 group rollout + 相对 update | actor + reference + reward/rule | rollout 大 batch、reward 方差 | actor + reference hash + group sample state |"},{"title":"PPO 多模型版本 Manifest","body":"PPO checkpoint 必须是一个**事务 manifest**，而不是「actor.pt + critic.pt + 一堆外部路径」。\n\n最低要求的 manifest 字段：\n- `actor`：weight_version、optimizer、scheduler、distributed_state（strategy/shards）\n- `critic`：同上\n- `reference`：artifact hash + frozen=true\n- `reward_model`：artifact hash + prompt_template hash\n- `tokenizer`：artifact + chat_template hash\n- `buffer`：policy_version、accepted_policy_versions\n- `controllers`：kl_coef、reward_norm version\n- `rng`、`data cursor`\n- `eval`：last_passed_step、eval suite version\n\n**阶段关系要严格**：`after_ppo_update` 时 actor 已更新到 step 820，但 buffer 只能包含 step 819 的 actor 生成样本，因为这些样本是在 update 前 rollout 的。rollout 权重同步到 820 后，新 prompt 才能写 `policy_version=actor_820_hash`。\n\n恢复时必须同时校验 actor、reference、RM、critic、tokenizer、prompt template、buffer policy_version、optimizer、scheduler 的版本关系。只恢复 actor 权重而丢掉 KL controller 或 buffer policy_version，会让 advantage、KL 和 reward 分布不再对应同一轮 rollout。","tip":"RM 版本固定非常关键：不同 RM artifact hash 会产生不同的 reward 分布。一个 RM worker rolling update 到新版本、另一半还在旧版本，会让同一批 rollout 样本被两个模板打分，导致 reward 方差异常升高。"},{"title":"Buffer 状态机：PPO 的数据契约","body":"replay/buffer 不是 append-only 表，而是 PPO 控制 loop 的状态机：\n\n`ReservedPrompt → RolloutInFlight → Generated → ScoringPending → Ready → TrainerLeased → ConsumedAck`\n\n关键约束：\n1. **版本绑定**：每个 sample 在 `ReservedPrompt` 时就绑定 actor_hash、RM_hash、template_hash、decoding_config_hash\n2. **trainer lease 谓词**：`next_ready_batch(actor_hash, rm_hash, template_hash, decoding_config_hash, count)`——不能跨版本混 batch\n3. **actor too old expire**：`Ready` 样本来自过旧 actor 时必须 expire，PPO on-policy 假设被破坏\n4. **fail closed**：rollout/scoring 返回的 hash 与 reservation 不一致时，sample 进入 quarantine，不能「修字段」后继续训练\n5. **RM retry 只能在同一 hash 下**：换 RM hash = 换训练目标，必须开新 run\n\n`old_logprobs_response` 必须来自 rollout 时的 actor，不能在 update 前临时重算——它是 PPO ratio 的分母。"},{"title":"DPO 的平台复杂度：reference cache 与长度偏差","body":"DPO 不需要在线 rollout，对平台最友好——但不代表可以省掉数据契约和 logprob 版本治理。\n\n**Reference logprob cache**：DPO 每个 step 用当前 policy 重算 chosen/rejected logprob，但 reference logprob 只需计算一次并 cache。cache key 必须覆盖 `reference_hash:tokenizer_hash:chat_template_hash:truncation_policy_hash:pair_id:chosen_hash:rejected_hash`。reference、tokenizer、template 任一 hash 变化，旧 cache 必须 stale invalidated，不能「用到本 epoch 结束」。\n\n**长度偏差**是 DPO 最大的数据质量风险：若 chosen 系统性更长，`sum_logprob` margin 会把长度差带进训练目标，最终 actor 会学会变长而不是变好。平台必须按 chosen/rejected token length bucket 监控 margin 和 win-rate，并做 beta sweep。\n\n**chosen/rejected 完整性检查**：pair 的 prompt_hash 必须一致；tokenization 后 mask 长度和 logprob 长度必须匹配；两者须来自同一 prompt group。","tip":"beta 太小会弱化偏好信号，太大会把 noisy pair 和长度偏置放大。平台必须记录 beta 值作为 training manifest 的一部分。"},{"title":"GRPO vs PPO：权衡比较","body":"GRPO 的核心思想：去掉 critic，用**同一 prompt 的多条 response 组内 reward 均值**作为 baseline。\n\n| 维度 | PPO | GRPO |\n|---|---|---|\n| 模型数量 | actor + reference + RM + critic | actor + reference + reward/rule |\n| 组内样本 | 1 个 response/prompt | G 个 response/prompt（典型 G=8-32）|\n| Baseline | critic value 估计 | 同 group reward 均值 |\n| 主要瓶颈 | RM 延迟、critic 稳定性 | rollout 吞吐（G 倍扩大）|\n| checkpoint 简化 | 无 | 去掉 critic weight/optimizer/checkpoint |\n| 平台复杂度转移 | critic 训练 | rollout 吞吐、组内样本聚合 |\n\n**GRPO 的 batch 不能把 group 当普通样本 shuffle**：每个 group 必须保持完整性（同一 prompt 的 G 条 response 必须在同一 minibatch 内），otherwise advantage 计算无意义。\n\n**数学 reward（规则 reward）**是 GRPO 常用场景：数学/代码的正确性可用规则判断，不依赖 RM，简化系统复杂度但要求 reward 函数确定性强、无歧义。","reveal":{"q":"为什么 PPO 的 old_logprobs_response 必须来自 rollout 时的 actor，而不能在 update 前重算？","a":"old_logprobs_response 是 PPO ratio 的分母：ratio = exp(new_logprob - old_logprob)。如果用 update 后的 actor 重算 old_logprobs，ratio 会偏离真实的策略变化幅度，clip 机制失效，PPO 的 on-policy 保证被破坏。rollout 时记录的 logprob 是固定的历史快照，必须与 response 一起保存在 buffer 中。"}}],"questions":[{"type":"choice","prompt":"判断一个后训练作业是「SFT 类」还是「RL 类」的核心依据是什么？","options":["是否使用 PPO loss","是否有 Reward Model","是否需要当前 policy 生成新 response（在线 rollout）","是否有 KL 散度约束"],"answer":2,"explain":"判断依据是系统形态：若训练过程不需要当前 policy 生成新 response，大概率是 SFT/DPO 类离线训练；若需要当前 policy 生成并被打分，就是 RL 系统，必须按推理和训练混合平台设计，处理 rollout engine、buffer、多模型版本等问题。","id":"u4-l6-q1"},{"type":"judge","prompt":"PPO 的 `after_ppo_update` 阶段，buffer 可以立即包含 step 820 actor 生成的样本，因为 actor 已经更新到 820。","answer":false,"explain":"rollout 必须等 actor 权重同步完成后才能使用新版本。`after_ppo_update` 时 actor 已更新到 820，但 rollout engine 仍在使用 819 的权重。必须等 rollout_weight_sync（清理旧 KV cache，加载 820 hash，校验后）完成，新 prompt 才能写 policy_version=actor_820_hash。","id":"u4-l6-q2"},{"type":"match","prompt":"将左侧 PPO buffer 状态与其含义匹配","left":["ReservedPrompt","ScoringPending","Ready","TrainerLeased"],"right":["prompt cursor 已预留，actor/RM/template/decoding 版本已绑定，等待 rollout","response 完整，等待 reference logprob、RM 分数、critic values 计算完成","所有字段齐全，可被 trainer 按版本谓词 lease","trainer 持有 lease_id，正在用于 PPO minibatch update"],"explain":"buffer 状态机按顺序推进，每个状态有严格的进入和退出条件。版本绑定在 ReservedPrompt 时就固定，后续各阶段不能修改 actor/RM/template hash。","id":"u4-l6-q3"},{"type":"choice","prompt":"DPO reference logprob cache 的 cache key 不需要包含哪个字段？","options":["reference_hash","tokenizer_hash","pair_id（具体的 chosen/rejected 对）","当前 policy 的 weight_version"],"answer":3,"explain":"reference logprob 只取决于 reference 模型、tokenizer、template、truncation policy 和输入数据，与当前 policy 权重无关。cache key 包含这些固定因子即可。policy weight 每步都在变，不能放进 ref cache key，否则 cache 每步都失效。","id":"u4-l6-q4"},{"type":"judge","prompt":"GRPO 去掉了 critic，因此比 PPO 更适合需要稳定 value 估计的复杂多步推理任务。","answer":false,"explain":"GRPO 用同 group 的 reward 均值作 baseline，简化了系统但也失去了 critic 对 value 的精确估计。对于复杂多步推理（如代码执行、多轮对话），critic 的 value 估计可以显著降低 advantage 方差，提高训练效率。GRPO 更适合 reward 明确、同 prompt 可生成多条独立 response 的场景（如数学、代码正确性判断）。","id":"u4-l6-q5"},{"type":"choice","prompt":"PPO 训练中，RM worker 对同一批 rollout 样本用了两个不同的 template hash 打分，最合适的处理方式是？","options":["取两个 template 打分的平均值作为 final reward","用 hash 较新的 template 分数覆盖旧版本分数","将 template hash 不一致的样本标记为 quarantine，不进入训练","记录日志后继续训练，下次更新时修复"],"answer":2,"explain":"版本不一致的样本不能进入训练，必须 quarantine。不同 template 产生的 reward 分布不同，混批会让训练目标被污染。RM retry 只能在同一 rm_hash + template_hash 下进行；换 template = 换训练目标，不能「修字段」后继续训练。","id":"u4-l6-q6"},{"type":"match","prompt":"将左侧后训练形态与其主要系统瓶颈匹配","left":["SFT","DPO","PPO/RLHF","GRPO"],"right":["数据质量和 chat template 版本一致性，tokenizer 绑定","reference logprob cache 一致性和长度偏差（length bias）","rollout 生成吞吐、RM 延迟、actor/ref/RM 多版本协调","rollout 吞吐（G 倍扩大）和 reward 方差控制"],"explain":"每种形态的系统瓶颈不同：SFT 是离线数据质量，DPO 是 reference 一致性，PPO 是多模型实时协调，GRPO 是大规模 rollout 和 reward 稳定性。","id":"u4-l6-q7"},{"type":"judge","prompt":"PPO 中，如果某个 rollout sample 的 old_logprobs_response 是在 update 前用当前 actor 重新计算的，PPO clip 机制仍然能正常工作。","answer":false,"explain":"old_logprobs_response 必须来自生成 response 时的 actor（rollout 时记录），是 PPO ratio = exp(new_logprob - old_logprob) 的分母。若用 update 后的 actor 重算，ratio 偏离真实策略变化幅度，clip 失效，on-policy 保证被破坏，训练可能发散或 reward hacking。","id":"u4-l6-q8"},{"type":"choice","prompt":"PPO 训练中，「actor too old expire」机制的目的是什么？","options":["防止 actor 模型文件在磁盘上占用过多空间","丢弃来自过旧 actor 版本生成的样本，维持 PPO 的 on-policy 假设","限制 actor 的最大训练步数，避免 over-fitting","触发 critic 自动更新，使 critic 与 actor 版本保持同步"],"answer":1,"explain":"PPO 要求样本来自当前（或近似当前）policy（on-policy 假设），importance sampling clip 的有效性依赖此假设。若 buffer 中的样本来自过旧版本的 actor，old_logprobs 与当前 actor 差距过大，clip ratio 失效，训练不稳定。因此必须 expire 过旧 actor 版本的样本。","id":"u4-l6-q9"},{"type":"judge","prompt":"DPO 训练中，reference logprob cache 的 key 不需要包含 chat_template_hash，因为 template 只影响 prompt 格式而不影响模型的 logprob 计算。","answer":false,"explain":"chat template 决定了 prompt 被 tokenize 成什么 token 序列，直接影响 reference model 计算的 logprob 值。若 template 改变但 cache key 不含 template_hash，旧 cache 的 logprob 会在新 template 下被错误复用，导致 DPO 目标函数使用错误的 reference logprob，silently 破坏训练。","id":"u4-l6-q10"},{"type":"input","prompt":"GRPO 用于估计 baseline 的统计量是什么？（填核心描述，不超过 15 字）","accept":["同组 response 的 reward 均值","group reward 均值","同 prompt 多条 response 的 reward 平均","组内 reward 平均值","同一 prompt 的 G 条 response reward 均值"],"explain":"GRPO 去掉了 critic，改用同一 prompt 的 G 条 response 的 reward 均值作为 baseline 来估计 advantage：advantage_i = reward_i - mean(reward_1...reward_G)。这避免了 critic 网络的训练开销，但代价是需要 G 倍的 rollout 吞吐。","id":"u4-l6-q11"},{"type":"choice","prompt":"PPO 训练系统中，以下哪个版本一致性问题会导致最严重的静默训练错误？","options":["actor checkpoint 版本与 critic checkpoint 版本相差 1 步","rollout 时使用 actor_v1，但打分的 RM 使用了与 actor_v1 不匹配的 prompt template","reference model 与 actor 使用了相同的 base checkpoint，而非 frozen 的单独副本","eval suite 使用了与训练集略有重叠的 benchmark"],"answer":1,"explain":"RM 使用错误的 prompt template 打分时，分数不对应真实 response 质量，且这个错误无法从 loss 曲线上直接观察到（分数可以继续上升，但优化的是错误目标）。actor/critic 版本差 1 步在 PPO clip 允许范围内；reference 与 actor 共享 base 是正常做法；eval 重叠是数据质量问题，不是最严重的 silent 错误。","id":"u4-l6-q12"},{"type":"match","prompt":"将左侧 PPO 组件与其 checkpoint 中必须记录的关键字段匹配","left":["actor","reward model","buffer","tokenizer"],"right":["weight_version、optimizer state、distributed_state（strategy/shards）","artifact hash + prompt_template hash（frozen，不存 optimizer）","policy_version、accepted_policy_versions、sample 状态机当前阶段","artifact hash + chat_template hash（template 变化导致 logprob cache 失效）"],"explain":"PPO manifest 必须精确记录每个组件的版本锁定字段：actor 需要 optimizer 以支持恢复；RM 是 frozen artifact，只记录 hash 和 template hash；buffer 记录版本依赖和状态机阶段；tokenizer 的 chat_template hash 决定 logprob 可复现性。","id":"u4-l6-q13"},{"type":"judge","prompt":"SFT 训练的 checkpoint 只需保存 model weights 和 tokenizer，data_version 和 chat_template_hash 不需要记录，因为 SFT 不涉及在线 rollout。","answer":false,"explain":"SFT checkpoint 必须包含 model + tokenizer + chat_template + data_version。data_version 用于追踪用哪批数据训练的，支持复现和回滚；chat_template 决定训练数据的 tokenize 方式。若缺少这些信息，当出现质量回退时，无法确定是数据问题还是模型问题。","id":"u4-l6-q14"},{"type":"choice","prompt":"DPO 长度偏差（length bias）的根本原因和解决方向分别是什么？","options":["根本原因：chosen response 较长导致 sum_logprob margin 把长度差带入目标；解决：按 chosen/rejected token length 归一化 logprob 或按长度分层采样","根本原因：DPO 使用 sum 而非 mean 的 logprob，长 response 总绝对值更大；解决：改用 mean_logprob","根本原因：数据集标注者偏爱较长 response；解决：只使用长度相近的 pair","根本原因：reference model 对长 sequence 的 perplexity 更低；解决：用 per-token reference logprob 代替 reference model"],"answer":0,"explain":"DPO 目标函数用 sum_logprob，若 chosen 系统性更长，sum_logprob margin 中包含长度差，模型会学习「变长」而非「变好」。平台修复方向：按 token length 归一化（per-token logprob ratio）或按 chosen/rejected 长度比分桶，确保 pair 内长度均衡。","id":"u4-l6-q15"},{"type":"input","prompt":"PPO 多模型训练中，rollout 生成的样本在进入 trainer 消费前，buffer 中每个样本绑定的四个核心 hash 字段是哪些？（填以逗号分隔的关键词）","accept":["actor_hash,rm_hash,template_hash,decoding_config_hash","actor_hash, rm_hash, template_hash, decoding_config_hash","actor hash, RM hash, template hash, decoding config hash"],"explain":"trainer lease 谓词必须匹配 (actor_hash, rm_hash, template_hash, decoding_config_hash) 四个字段，确保 batch 内所有样本来自同一 actor 版本、同一 RM、同一 template 和同一解码配置。跨版本混 batch 会破坏 PPO 的训练语义。","id":"u4-l6-q16"},{"type":"judge","prompt":"GRPO 因为去掉了 critic，所以 batch 内的不同 group 可以任意 shuffle，不需要保持 group 内样本的完整性。","answer":false,"explain":"GRPO 的 advantage 计算依赖同一 prompt 的 G 条 response 的 reward 均值（group-level baseline），如果 batch shuffle 打散了 group 边界，使 advantage 跨 group 混算，baseline 失去统计意义，训练信号错误。GRPO batch 必须以 group 为单位组织，不能把 group 当普通样本 shuffle。","id":"u4-l6-q17"},{"type":"match","prompt":"将左侧后训练故障现象与其最可能的根因匹配","left":["PPO 训练 loss 下降但 reward 持续不上升甚至下降","DPO 训练后 actor 生成内容系统性变长但质量未提升","GRPO rollout 吞吐下降，GPU 利用率低，rollout 成为瓶颈","PPO buffer 中 `actor too old expire` 比例持续上升"],"right":["RM template 与 actor rollout template 不一致，RM 打分的是错误格式的 response","数据集中 chosen 系统性长于 rejected，DPO 学到了长度偏差","G 值过大（每 prompt 需采样 G 条 response），rollout engine 并发度不足","actor 更新速度过快而 rollout engine 并发度不足，样本产生慢于消费速度"],"explain":"PPO reward 不升→打分异常（template mismatch）；DPO 变长不变好→长度偏差；GRPO 吞吐低→G 倍 rollout 需求超过 engine 能力；buffer expire 多→actor 更新比 rollout 快，on-policy window 过紧。","id":"u4-l6-q18"}]},{"id":"u4-l7","title":"微调基础设施与多 Adapter 服务","subtitle":"从 LoRA/QLoRA 到 FTaaS 控制面与多租户 Adapter 生命周期","intro":[{"title":"微调基础设施的不可化简问题","body":"一个已经足够大的 base model，不能为每个租户、每个任务都完整复制、完整训练、完整部署。这个矛盾落在四条路径上：\n\n| 路径 | 不可化简问题 | 没有平台机制的后果 |\n|---|---|---|\n| 训练路径 | 全量更新参数消耗过多 GPU、存储和排队时间 | 短作业被长作业挤压 |\n| 制品路径 | adapter 依附 base model、tokenizer、template、训练数据 | 线上无法证明「这个小文件能挂在哪个服务上」|\n| 服务路径 | 不能为每个 adapter 常驻完整 base model | 成本退回到每租户一模型 |\n| 治理路径 | 多租户需要配额、权限、审批、审计、A/B 和回滚 | 单租户错误影响所有人 |\n\nFTaaS 的目标不是把 pretraining 缩小，而是把「数据提交 → 短训练 → 评测 → adapter registry → hot load → production route」变成可治理闭环。\n\n`time_to_prod = queue_wait + data_stage + train + eval + register + preload + canary_window`\n\n优化目标是降低 p50/p95 `time_to_first_eval` 和 `time_to_prod`，而不是追求单 job 的极限 MFU。"},{"title":"LoRA、QLoRA、DoRA、Full Fine-Tune 的工程边界","body":"四种微调方式的资源交换：\n\n| 方法 | 训练哪些参数 | 训练显存 | 制品大小 | 服务接入 | 回滚难度 |\n|---|---|---|---|---|---|\n| Full fine-tune | 全量或大部分 base 权重 | 最高 | 接近完整模型 | 独立副本或覆盖 checkpoint | 模型级回滚，重 |\n| LoRA | 冻结 base，训练低秩矩阵 | 低 | MB 到数百 MB | dynamic attach 或 merge | 路由切回旧 adapter |\n| QLoRA | 量化 base + LoRA adapter | 更低，可用更便宜 GPU | adapter 小 + 量化元数据 | 需要同等量化环境 | 同 LoRA |\n| DoRA | 方向+幅度拆分，结合 LoRA | 与 LoRA 相当 | 略大于同 rank LoRA | 需要 engine 支持 | schema 验证更严格 |\n\n**LoRA 参数量**：对一个 hidden_size = d_in, d_out 的线性层，rank=r 的 LoRA 参数 = `r × (d_in + d_out)`。target modules 从 `q_proj,v_proj` 扩到全部 7 个 attention+MLP 投影，参数增长远大于调整 rank。\n\n**QLoRA 的复现性要求更高**：training 时的量化配置（NF4/4bit、bitsandbytes 版本、gradient checkpointing）必须完整记录，否则 resume 会数值漂移。服务期不一定使用同一量化路径（通常 merge 后用 BF16 服务）。","tip":"base model digest 必须固定。同名不同 hash 的 base model 是最常见的 adapter 兼容性事故：两个 adapter 都「挂在 LLaMA-3.1-8B」上，实际 hidden_size、RoPE scaling 或 vocab size 已经不同。"},{"title":"Adapter Registry：不只是文件路径","body":"Adapter registry 是 adapter lifecycle 的 source of truth，必须记录**可运行契约**，不只是文件路径。\n\n最小 registry 记录字段：\n- **artifact**：uri、digest（sha256 不可变）、format、size_bytes、created_by_job\n- **base_compatibility**：base_model_id、base_model_digest、architecture、hidden_size、tokenizer_digest、vocab_size、chat_template_version、max_position_embeddings\n- **adapter_schema**：method、rank、alpha、scaling_convention、peft_version、target_modules、dtype、quantization_dependency\n- **policy**：tenant_id、license、safety_policy、pii_review\n- **evaluation**：quality_gate、safety_gate、regression_gate、具体 metrics（task_f1、win_rate_vs_previous）\n- **serving**：allowed_engines、hot_load、rollback_target\n- **audit**：requested_by、approved_by、approval_ticket\n\n**Register 和 Eval 的顺序**：先 freeze artifact（CAS/digest），再 register，再 attach eval report。没有 eval report 的 version 不允许 release。不能让 eval worker 修改 artifact 字节，也不能在 eval 前就把 version 标为可发布。"},{"title":"FTaaS 四个状态机","body":"生产 FTaaS 必须把四个独立对象的状态分离：\n\n**1. TrainingJob**（训练尝试是否完成）：\n`submitted → admitted → queued → running → evaluating → completed`\n异常：`rejected`、`failed`、`retrying`\n\n**2. AdapterVersion**（artifact 是否可发布）：\n`registered → eval_attached → approval_pending → released`\n异常：`non_deployable`、`revoked`\n\n**3. ReleaseRevision**（流量策略 desired state）：\n`created → preload_requested → shadow → canary → ramp → production`\n异常：`rolled_back`、`superseded`\n\n**4. ReplicaLoadState**（单副本 observed state）：\n`not_present → loading → loaded → unloading → not_present`\n异常：`load_failed`、`digest_mismatch_quarantined`\n\n**关键边界**：`released`、`production` 是 desired state（控制面希望做什么）；`loaded`、`load_failed` 是 observed state（副本真实做了什么）。同一个 release 在副本 01 可以 `loaded`，在副本 17 可以 `load_failed`。router 只能消费 registry 已批准的 release，不能直接访问 artifact 字节。","tip":"production route 只引用 registry 中已 release、目标 serving pool 中达到最小副本加载门槛、且未被撤销的 release revision。任何一步失败都不能覆盖旧版本 route。"},{"title":"Multi-LoRA Serving：动态 Attach 的工程边界","body":"Multi-LoRA serving 让 base model 常驻，adapter 按租户、版本和实验桶动态 attach。\n\n**Hot Load 流程**：\n1. release event 触发 serving loader 拉取 adapter\n2. loader 按 CAS digest 校验 artifact（不按文件名猜）\n3. 加载到 GPU/CPU/NVMe adapter cache，上报 `ReplicaLoadState.loaded`\n4. router 确认最小副本加载门槛后，更新流量路由\n\n**Adapter cache 的显存模型**：\n`M_serving_peak = M_base + Σ(M_adapter_i × hot_replicas_i) + M_kv_cache`\nrank 从 16 到 64，adapter 显存线性增长 4 倍。每个 hot adapter 的增量显存 = `P_adapter × dtype_bytes`（BF16 为 2 bytes/param）。\n\n**Batching 与 KV cache 的耦合**：多个 adapter 共用同一 base model 副本时，batching 策略需要考虑 KV cache prefix 隔离、per-adapter padding、context length 分布差异。\n\n**A/B 和 Canary**：`ReleaseRevision` 状态机支持按 traffic_split 逐步放量（shadow → canary → ramp → production）。rollback 是原子的：route 指向上一个 stable revision，不需要重新加载 artifact。"},{"title":"数据准入与版本矩阵","body":"FTaaS 准入的核心原则：**在提交阶段给出确定性拒绝原因，不要把问题留给训练曲线发现**。\n\n数据准入检查清单：\n- 数据格式（JSONL/Parquet schema validate）\n- 样本规模（token count、max length histogram vs quota）\n- 数据许可（与 base license 或商用策略是否冲突）\n- PII/secret（scanner report，高危未脱敏则拒绝）\n- 安全策略（禁止类别样本比例阈值）\n- 去重与污染（与评测集重叠检测）\n\n**版本矩阵必须固定**：trainer image digest、CUDA/driver、PyTorch、PEFT、bitsandbytes、transformers 版本。浮动 tag 会导致「同一配置两次训练结果不可复现」。QLoRA resume 对 bitsandbytes 版本极度敏感。\n\n**Chat template 是高频事故来源**：dataset 用 `chatml-v6` 生成但 trainer 读到 `chatml-v5`，两者都能 tokenize，loss 也会下降，但 assistant token、system prompt 拼接和 EOS 位置不同，后续 DPO 的 KL 和 logprob 会失真。准入规则：`dataset.chat_template_hash != job.chat_template_hash` → 直接拒绝启动。","reveal":{"q":"为什么 adapter 的兼容性检查不能只验证 shape 能否对上，还需要检查 tokenizer_digest 和 chat_template_hash？","a":"LoRA adapter 的权重被添加到 base model 的特定层，依赖 base 的 token embedding 和位置编码约定。如果 tokenizer 增加了 special token（vocab_size 变化），embedding 层 shape 会改变，adapter 加载会失败或静默错配。即使 shape 偶然对上（如 vocab padding 一致），不同 chat template 会让 assistant token、system prompt 和 EOS 的 position 不同，adapter 学到的表示空间已经与新 template 不兼容，表现为质量回退而不是报错。"}}],"questions":[{"type":"choice","prompt":"LoRA adapter 对于 rank=16 的 q_proj（d_in=4096, d_out=4096），参数量约多少？","options":["约 4M","约 131K","约 16M","约 262K"],"answer":1,"explain":"LoRA 参数量 = r × (d_in + d_out) = 16 × (4096 + 4096) = 16 × 8192 = 131,072 ≈ 131K。相比 full fine-tune 的 16.7M 参数，显著减少。这也是为什么 LoRA adapter 文件只有 MB 级别。","id":"u4-l7-q1"},{"type":"judge","prompt":"Adapter registry 记录的 `released` 状态意味着该 adapter 已经在所有 serving 副本上加载完成。","answer":false,"explain":"`released` 是 AdapterVersion 的 desired state，表示控制面批准发布。实际加载状态由每个副本独立上报为 `ReplicaLoadState.loaded` 或 `load_failed`。同一个 release 在不同副本可以有不同加载状态，router 需要确认最小副本加载门槛才能更新流量。","id":"u4-l7-q2"},{"type":"match","prompt":"将左侧 FTaaS 状态机与其 source of truth 匹配","left":["TrainingJob","AdapterVersion","ReleaseRevision","ReplicaLoadState"],"right":["FTaaS control plane / job store：训练尝试是否完成","adapter registry：frozen artifact 是否可评测和发布","registry + route store 投影：某租户希望线上使用哪个版本和流量比例","serving loader 观测并上报：某副本是否真实加载了某 digest"],"explain":"四个状态机各有独立的 source of truth。registry 是 adapter lifecycle 的核心，但 serving loader 的加载状态必须由副本自己上报，不能由控制面假设。","id":"u4-l7-q3"},{"type":"choice","prompt":"Multi-LoRA serving 中，adapter cache eviction 触发时最合适的处理方式是？","options":["立即从所有副本卸载该 adapter，更新流量路由","保持当前路由，在后台异步重新加载 adapter","更新 ReplicaLoadState 为 unloading，通知 router 该副本暂时不可用于该 adapter，再从 artifact store 重新加载","直接用 CPU 内存中的 adapter 继续处理请求"],"answer":2,"explain":"正确流程：更新 ReplicaLoadState 为 unloading → 通知 router 该副本暂时不可用（路由到其他已加载的副本）→ 从 artifact store 按 digest 重新拉取 → 校验后加载 → 上报 loaded → router 恢复路由。不能在 adapter 未加载时继续路由请求，也不能直接用 CPU 内存绕过 GPU 加载。","id":"u4-l7-q4"},{"type":"judge","prompt":"FTaaS 中，如果训练 worker 的 chat template 配置与 dataset 使用的 chat template 不同但 tokenization 成功，作业应该继续运行并在 eval 阶段发现问题。","answer":false,"explain":"chat template 不一致应在准入阶段直接拒绝启动：`dataset.chat_template_hash != job.chat_template_hash → 拒绝`。training 时两种 template 都能 tokenize，loss 也会下降，但 assistant token、EOS 位置等不同，后续 DPO/PPO 的 KL 和 logprob 会失真。不要把这类问题留给训练曲线发现。","id":"u4-l7-q5"},{"type":"choice","prompt":"QLoRA 与 LoRA 的最主要区别是什么？","options":["QLoRA 只训练 q_proj 层，LoRA 训练所有层","QLoRA 对 base model 做量化（如 NF4/4bit）再训练 LoRA，显存需求更低","QLoRA 使用更高的 LoRA rank 弥补量化损失","QLoRA 的 adapter 必须 merge 后才能服务"],"answer":1,"explain":"QLoRA 的核心是：将 base model 用 NF4/4bit 量化（通过 bitsandbytes），在此基础上训练 LoRA adapter。这使得 7B 以上的模型可以在单张消费级 GPU 上微调。代价是对量化库版本更敏感，resume 时需要完全相同的量化配置，服务时通常需要先 merge 并 dequantize。","id":"u4-l7-q6"},{"type":"match","prompt":"将左侧 adapter release 步骤与其关键约束匹配","left":["Export","Digest + CAS/freeze","Attach eval report","Release event"],"right":["training worker 只写 worker_temp_object，exporter 从 checkpoint 抽取 adapter 权重","artifact service 对每个文件做 sha256，写入不可变 content-addressed path，关闭覆盖写权限","eval worker 按 registry version + digest 拉取 artifact，质量/安全/兼容评测结果 attach 到同一 version","approval gate 通过时 AdapterVersion 推进到 released，创建 ReleaseRevision，向目标 serving pool 发事件"],"explain":"release 流程的关键是不可变性和顺序：先 freeze artifact（防止字节变化），再 register，再评测，最后 release。任何阶段失败都不能覆盖旧版本 route。没有 eval report 的 version 不允许进入 release 阶段。","id":"u4-l7-q7"},{"type":"judge","prompt":"LoRA adapter 的 rollback_target 字段记录的是上一个同 base model 的 adapter 版本，rollback 时需要重新从 artifact store 拉取和加载该版本。","answer":false,"explain":"rollback 是路由原子切换，不需要重新加载。`rollback_target` 指向上一个 stable release revision，router 直接将流量切回该 revision。如果目标 revision 的 adapter 已经在 serving pool 中缓存（loaded），切换几乎即时完成。这是 Multi-LoRA serving 相比完整模型 rollback 的核心优势之一。","id":"u4-l7-q8"},{"type":"choice","prompt":"rank=32 的 LoRA adapter 对 hidden_size=8192 的 q_proj 和 v_proj 各一个线性层（d_in=d_out=8192），参数量合计约为多少？","options":["约 262K（每层 r×d = 32×8192）","约 524K（每层 2×r×d = 2×32×8192，两层合计）","约 1.05M（每层 2×r×d，两层合计）","约 16.8M（每层 r×d_in×d_out/r = d_in×d_out）"],"answer":2,"explain":"每个线性层的 LoRA 参数量 = r × d_in + r × d_out = 32×8192 + 32×8192 = 524,288 ≈ 0.52M。两个线性层（q_proj + v_proj）合计 ≈ 1.05M。典型大模型 LoRA adapter 只有几百 KB 到数十 MB，与完整模型参数量（8192²×2 bytes ≈ 128 GiB）相比极小。","id":"u4-l7-q9"},{"type":"judge","prompt":"FTaaS 中，AdapterVersion 处于 `released` 状态意味着该 adapter 的流量路由已经切到 `production` 阶段，所有请求都使用这个新版本。","answer":false,"explain":"`released`（AdapterVersion 状态机）指 artifact 已通过 eval 和 approval，可以发布，但还不意味着流量已切到 production。流量策略由独立的 ReleaseRevision 状态机控制（shadow → canary → ramp → production），released 只是发布的前置条件，而非终态。","id":"u4-l7-q10"},{"type":"input","prompt":"Multi-LoRA serving 中，adapter 加载到 serving 副本后通过什么操作来校验 artifact 完整性（不是按文件名猜）？","accept":["CAS digest 校验","digest 校验","checksum 校验","sha256 校验","CAS digest","digest check"],"explain":"serving loader 必须按 CAS digest（如 SHA256）校验 artifact，而不是按文件名或路径判断。这保证加载的是 registry 中精确记录的那份 artifact，防止文件被替换或在传输中损坏。Adapter registry 中的 `digest` 字段是不可变的（immutable）标识。","id":"u4-l7-q11"},{"type":"choice","prompt":"QLoRA 相比 LoRA 的核心差异是什么？它引入了哪个额外的工程依赖？","options":["QLoRA 使用更高的 rank（r≥128），额外依赖更大的 GPU 显存","QLoRA 对 base model 做量化（如 NF4/INT4），用更少显存训练，额外依赖 bitsandbytes 等量化库，且 resume 对量化库版本极度敏感","QLoRA 对 adapter 本身做量化，base model 保持 BF16，额外依赖 GGUF 格式","QLoRA 是 LoRA 的推理优化版本，训练时与 LoRA 完全相同"],"answer":1,"explain":"QLoRA = 量化 base model（NF4/INT4）+ LoRA adapter（BF16/FP16）。量化 base 大幅降低训练显存，使 70B 模型可在单卡 48 GB GPU 上微调。额外依赖：bitsandbytes（量化/反量化 kernel），且 resume 对 bitsandbytes 版本极度敏感——不同版本的量化元数据（quantization state）不兼容。","id":"u4-l7-q12"},{"type":"match","prompt":"将左侧 FTaaS 数据准入检查项与其失通过的后果匹配","left":["PII/secret 未脱敏（高危）","dataset 与 eval benchmark 重叠","chat_template 版本与 trainer 不匹配（但 tokenization 成功）","数据许可与 base model 许可冲突"],"right":["adapter 上线后泄露用户隐私数据，造成合规和法律风险","eval 指标虚高，无法准确衡量 adapter 的真实泛化能力","训练 loss 下降但 adapter 在 serving 时对话格式错误，可能无法生成预期格式的 response","adapter 无法商业部署，发布后面临法律风险"],"explain":"各准入检查的失通过后果各有侧重：PII→隐私合规风险；eval 重叠→指标虚高，泛化能力被高估；template 不匹配→训练数据格式与 serving 格式不一致，推理时行为异常；许可冲突→商业法律风险。这些问题在提交阶段拒绝代价最小。","id":"u4-l7-q13"},{"type":"judge","prompt":"Multi-LoRA serving 中，为同一 base model 加载更多 adapter 不影响 KV cache 的可用显存，因为 adapter 参数与 KV cache 在不同的显存区域。","answer":false,"explain":"serving 峰值显存 = M_base + Σ(M_adapter_i × hot_replicas) + M_kv_cache，三者共享同一块 HBM。加载更多 hot adapter 直接挤压 M_kv_cache 可用空间，导致 KV cache 命中率下降，推理延迟上升。必须在 adapter 数量和 KV cache 大小之间做显式权衡，不是独立的资源。","id":"u4-l7-q14"},{"type":"choice","prompt":"FTaaS 中，版本矩阵（trainer image digest、PyTorch、PEFT、bitsandbytes 版本）使用浮动 tag（如 `latest`）而非固定版本，最直接的工程后果是什么？","options":["训练速度会随依赖升级而提升，是优化而不是风险","同一配置两次训练结果不可复现，且 checkpoint 可能无法在不同时间点恢复","只影响 eval 指标，不影响 adapter 功能","只在 QLoRA 场景下有风险，普通 LoRA 不受影响"],"answer":1,"explain":"浮动 tag 导致训练环境随依赖库版本漂移而变化，同一配置在不同时间提交可能得到不同结果（不可复现）。更危险的是 checkpoint resume：QLoRA 的 bitsandbytes quantization state 对版本极度敏感，跨版本 resume 可能加载到不兼容的量化元数据，导致静默精度错误。","id":"u4-l7-q15"},{"type":"input","prompt":"FTaaS 中，TrainingJob 状态机从提交到完成的正常路径包含哪些主要状态？（按顺序填，以箭头分隔，如 A→B→C）","accept":["submitted→admitted→queued→running→evaluating→completed","submitted → admitted → queued → running → evaluating → completed"],"explain":"TrainingJob 状态机的正常路径：submitted（用户提交）→ admitted（准入检查通过）→ queued（等待资源）→ running（训练执行中）→ evaluating（自动评测）→ completed（评测完成，结果写入 AdapterVersion）。异常分支：rejected（准入失败）、failed（训练失败）、retrying（自动重试）。","id":"u4-l7-q16"},{"type":"match","prompt":"将左侧微调方法与其最主要的工程特征匹配","left":["Full Fine-Tune","LoRA","QLoRA","DoRA"],"right":["训练全量或大部分 base 权重，制品接近完整模型，serving 需独立副本","冻结 base，只训练低秩矩阵，制品 MB 级，支持 dynamic attach","量化 base（NF4/INT4）+ LoRA adapter，显存最低，resume 对量化库版本极敏感","将权重拆为方向和幅度分别优化，需要 engine 特殊支持，schema 验证更严格"],"explain":"Full Fine-Tune 最重但最通用；LoRA 轻量支持动态加载；QLoRA 显存最低但引入量化依赖；DoRA 精度更高但需要推理引擎支持方向+幅度分离的参数格式。","id":"u4-l7-q17"},{"type":"judge","prompt":"Adapter registry 中的 `base_model_digest` 字段只是一个可选元数据，serving 层可以根据 `base_model_id`（如 `llama3-70b`）来判断兼容性，因为同一 model_id 的 base 参数不会改变。","answer":false,"explain":"`base_model_digest`（SHA256）是不可变标识，比 `base_model_id` 更精确。同一 model_id 的 base 可能存在多个版本（量化版本、安全微调版本、不同精度版本），如果 serving 层只用 model_id 判断兼容性，可能把为 llama3-70b-BF16 训练的 adapter 加载到 llama3-70b-NF4 上，导致隐性精度错误或维度不匹配。","id":"u4-l7-q18"}]}]}