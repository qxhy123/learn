{"items":[
  {
    "id": "u5-l11-q3",
    "ok": false,
    "right": [
      "canary 之前，MMLU/GSM8K/Safety 等离线跑分",
      "canary 放量后持续监控，LLM-as-judge + 人工抽检",
      "production 前置，任务完成率和用户满意度信号"
    ],
    "note": "原 right[0]='canary放量后持续监控' 对应在线评测门禁，right[1]='canary之前离线跑分' 对应离线评测门禁，顺序与 left 相反。修复后 right[0] 对应离线评测门禁（canary之前），right[1] 对应在线评测门禁（canary阶段）。"
  },
  {
    "id": "u5-l11-q14",
    "ok": false,
    "right": [
      "表达兼容性契约（major/minor/patch），对外暴露 API 的模型服务",
      "可读性强，时间顺序自然，适合快速迭代的内部模型",
      "内容可验证，任何变更产生不同版本号，天然不可变"
    ],
    "note": "原 right[0]='内容可验证'（Hash-based），right[1]='兼容性契约'（SemVer），right[2]='可读性强'（Date-based），与 left 顺序完全错位。修复后 SemVer→兼容性契约，Date-based→可读性强，Hash-based→内容可验证。"
  },
  {
    "id": "u5-l12-q4",
    "ok": false,
    "right": [
      "证明产物在某时刻由某可信身份签署",
      "列出产物所有组件、版本和许可证（成分表）",
      "已知 CVE 发现（基于 SBOM 和 CVE 数据库匹配）",
      "在 K8s 层强制执行签名策略，拒绝不满足条件的 Pod"
    ],
    "note": "原 right 顺序：[Kyverno职责, cosign职责, Trivy职责, SBOM职责]，与 left=[cosign, SBOM, Trivy, Kyverno] 完全错位。修复后各项与 left 一一对应。"
  },
  {
    "id": "u5-l12-q14",
    "ok": false,
    "right": [
      "证明制品由可信身份签名，签名记录不可篡改",
      "记录制品成分表，发现已知 CVE，支持快速影响范围评估",
      "在 K8s Pod 创建时强制验证镜像签名，拒绝未通过签名验证的部署"
    ],
    "note": "原 right[0]='K8s Pod强制验证'（Kyverno职责），right[2]='证明制品'（cosign职责），与 left=[cosign+Rekor, SBOM+扫描, Kyverno] 错位。修复后各项与 left 一一对应。"
  },
  {
    "id": "u5-l13-q4",
    "ok": false,
    "right": [
      "必须包含 model_id + tokenizer_version + preprocess_version",
      "必须包含 tenant_id + user_acl_hash + index_version",
      "必须包含 prefix token hash + model_id + policy_version，且不跨租户共享"
    ],
    "note": "原 right[0]='tenant_id+user_acl_hash'（Retrieval cache的key），right[1]='model_id+tokenizer'（Embedding cache的key），与 left=[Embedding cache, Retrieval cache, KV Prefix cache] 前两项互换。修复后对齐。"
  },
  {
    "id": "u5-l13-q12",
    "ok": false,
    "right": [
      "权限变更后旧缓存仍为用户服务已过期特征值",
      "跨租户检索结果泄露，同一 query 返回 A 用户权限下的结果给 B 用户",
      "含检索结果或用户画像的前缀被跨用户共享，产生越权"
    ],
    "note": "原 right[0]='含检索结果前缀跨用户共享'（KV Prefix风险），right[1]='过期特征值'（Feature cache风险），right[2]='跨租户检索结果泄露'（Retrieval cache风险），顺序与 left=[Feature cache, Retrieval cache, KV Prefix cache] 错乱。修复后各项对应正确缓存类型。"
  },
  {
    "id": "u5-l14-q4",
    "ok": false,
    "right": [
      "特征定义的唯一来源与版本管理",
      "历史特征存储与 point-in-time join 支持",
      "Offline 到 Online 的数据同步",
      "亚毫秒级在线特征读取"
    ],
    "note": "原 right[0]='亚毫秒级在线读取'（Online Store职责），right[1]='特征定义唯一来源'（Registry职责），与 left=[Registry, Offline Store, Materialization, Online Store] 错位。修复后各项对应正确组件。"
  },
  {
    "id": "u5-l14-q14",
    "ok": false,
    "right": [
      "时间分区粒度设计不合理，扫描过多分区",
      "批量 Get 未用 pipeline，退化为串行查询",
      "增量 Sync 未使用 UPSERT 语义"
    ],
    "note": "原 right[0]='UPSERT语义'（Materialization根因），right[1]='时间分区'（Offline Store根因），right[2]='pipeline'（Online Store根因），与 left=[Offline Store慢, Online Store P99高, Materialization重复数据] 顺序错乱。修复后各项对应正确失败模式。"
  },
  {
    "id": "u5-l15-q4",
    "ok": false,
    "right": [
      "内存充足（>307GB）、延迟极敏感、要求 97% 召回",
      "256GB RAM 服务器、延迟敏感、召回 96%、4x 内存节省",
      "内存极度受限（3.5GB）、高 QPS、可接受 91% 召回",
      "超大规模向量库、内存受限但有 NVMe SSD"
    ],
    "note": "原 right[0]='内存极度受限3.5GB'（IVF-PQ场景），right[1]='超大规模NVMe SSD'（DiskANN场景），right[2]='256GB RAM'（SQ-HNSW场景），right[3]='内存充足307GB'（HNSW float32场景），与 left=[HNSW float32, SQ-HNSW int8, IVF-PQ, DiskANN] 顺序完全错乱。修复后各项对应正确算法。"
  },
  {
    "id": "u5-l15-q13",
    "ok": false,
    "right": [
      "无需训练阶段，逐个向量插入时在线构建图结构",
      "需要先运行 K-Means 生成 centroid，才能构建倒排桶",
      "需要预训练 codebook，embedding 模型切换后必须重训"
    ],
    "note": "原 right[0]='预训练codebook'（PQ训练要求），right[1]='无需训练'（HNSW训练要求），right[2]='K-Means'（IVF训练要求），与 left=[HNSW, IVF, PQ] 顺序完全错乱。修复后各项对应正确算法。"
  },
  {
    "id": "u5-l16-q4",
    "ok": false,
    "right": [
      "无 WAL、无 HA、无副本，仅适合原型/离线",
      "超大规模需分区策略，纯向量 QPS 受主库限制",
      "运维复杂度高（etcd + MinIO + 多组件）",
      "成本大规模下较高，调优空间有限"
    ],
    "note": "原 right[0]='运维复杂度高'（Milvus限制），right[1]='无WAL'（FAISS/Chroma限制），right[2]='成本高'（Pinecone限制），right[3]='超大规模需分区'（pgvector限制），与 left=[FAISS/Chroma, pgvector, Milvus, Pinecone] 错位。修复后各项对应正确向量库。"
  },
  {
    "id": "u5-l16-q12",
    "ok": false,
    "right": [
      "已有 PostgreSQL 实例、规模 < 千万向量、需要事务 ACID",
      "离线评测、原型开发、无需 HA 和副本",
      "关键词搜索是主力、需要 BM25 + 向量 hybrid"
    ],
    "note": "原 right[0]='离线评测/原型开发'（FAISS/Chroma适用条件），right[1]='已有PG实例/ACID'（pgvector适用条件），与 left=[pgvector, FAISS/Chroma, ES/OpenSearch] 前两项互换。修复后各项对应正确向量库。"
  },
  {
    "id": "u5-l17-q4",
    "ok": false,
    "right": [
      "加 BM25 sparse retrieval，用 RRF 融合",
      "增大 chunk size，检查 context recall 指标",
      "改为 ANN pre-filter，确保无权限文档不参与 ANN 计算",
      "建 golden 集，接入 Ragas/TruLens 评测"
    ],
    "note": "原 right[0]='改为ANN pre-filter'（Post-filter的修复），right[1]='加BM25'（dense retrieval的修复），right[2]='建golden集'（不做eval的修复），right[3]='增大chunk'（chunk太小的修复），与 left=[只用dense, Chunk太小, Post-filter权限, 不做eval] 顺序完全错乱。修复后各项对应正确反模式。"
  },
  {
    "id": "u5-l17-q13",
    "ok": false,
    "right": [
      "先验证 context recall >= 70%，召回层漏召时 reranker 无法修复",
      "HyDE 生成的假设文档含幻觉，拉偏了检索方向，需降低 HyDE 使用比例或加验证",
      "cache key 缺少 index_version 或 model_id 字段，导致过期答案被复用"
    ],
    "note": "原 right[0]='cache key缺字段'（Semantic Cache的修复），right[1]='验证context recall'（reranker的修复），right[2]='HyDE幻觉'（HyDE的修复），与 left=[reranker未提升, HyDE召回变差, Semantic Cache质量下降] 顺序完全错乱。修复后各项对应正确反模式。"
  },
  {
    "id": "u5-l18-q4",
    "ok": false,
    "right": [
      "低（向量本身不含业务数据）",
      "中（相似问题但答案可能因权限不同而不同）",
      "高（包含完整 LLM 回答，必须按 user/tenant 隔离）",
      "极高（必须按租户/权限隔离，包含 LLM KV 状态）"
    ],
    "note": "原 right[0]='极高'（KV Prefix风险），right[1]='低'（Embedding风险），right[2]='中'（Semantic风险），right[3]='高'（Output风险），与 left=[Embedding Cache, Semantic Cache, Output Cache, KV Prefix Cache] 顺序完全错乱。修复后从低到极高与 left 对应。"
  },
  {
    "id": "u5-l18-q13",
    "ok": false,
    "right": [
      "低（向量本身不含业务数据，越权风险极小）",
      "中（相似问题可能因权限不同有不同答案）",
      "极高（必须按租户/权限隔离，绝对不跨租户共享）"
    ],
    "note": "原 right[0]='极高'（KV Prefix风险），right[1]='低'（Embedding风险），right[2]='中'（Semantic风险），与 left=[Embedding Cache, Semantic Cache, KV Prefix Cache] 顺序完全错乱。修复后从低到极高与 left 对应。"
  },
  {
    "id": "u6-l1-q6",
    "ok": true,
    "right": [
      "鉴权、限流、租户识别，不应理解每个副本的 KV 状态",
      "根据模型版本、上下文长度、prefix 亲和和实时容量选择目标副本",
      "持有权重、KV Cache 和推理 runtime 的进程",
      "执行 batching、调度、kernel、KV 管理的内核"
    ],
    "note": "原配对正确：Gateway→鉴权限流，Router→选择副本，Model server→持有权重，Runtime/Engine→执行batching内核。"
  },
  {
    "id": "u6-l1-q14",
    "ok": true,
    "right": [
      "破坏程度最小：影响排序质量但不影响回答完整性",
      "破坏程度较小：质量有损但仍有完整回答",
      "破坏程度较大：可能截断对话历史，影响语义连贯性",
      "破坏程度最大：直接拒绝服务，用户无法获得任何回答"
    ],
    "note": "原配对正确：关闭rerank→最小，切换小模型→较小，截短上下文→较大，返回429/503→最大。"
  },
  {
    "id": "u6-l2-q7",
    "ok": true,
    "right": [
      "短请求完成后槽位空置、GPU 利用率下降",
      "KV Cache 连续分配的碎片和过度预留浪费 60–80% 显存",
      "相同 system prompt 被多个请求重复 prefill，浪费算力和显存",
      "长 prompt 独占一次 forward，阻塞同池 decode 请求的 ITL"
    ],
    "note": "原配对正确：Continuous batching→槽位空置，PagedAttention→显存碎片，Prefix cache→重复prefill，Chunked prefill→长prompt阻塞。"
  },
  {
    "id": "u6-l2-q14",
    "ok": true,
    "right": [
      "单独训练的小模型（如 1B 对 70B），独立推理后批量验证",
      "从请求自身 prompt 做 n-gram 匹配，不需要额外模型",
      "利用目标模型的 hidden state 预测下几个 token",
      "在目标模型上加多头输出层，并行预测多个候选 token"
    ],
    "note": "原配对正确：Draft model→独立小模型，n-gram→n-gram匹配，EAGLE→hidden state，Medusa→多头输出层。"
  },
  {
    "id": "u6-l3-q5",
    "ok": true,
    "right": [
      "把整个 attention 融成一个 kernel，避免写出 O(N²) 的中间矩阵 S 和 P",
      "把一段稳定 CUDA 调用录制成 DAG，减少 CPU kernel launch overhead（每步 5–10 μs）",
      "从 PyTorch 模型图生成更少、更快的算子计划（算子融合 + Triton kernel）",
      "让 INT4 权重在寄存器里反量化后直接喂 Tensor Core，避免反量化 fallback"
    ],
    "note": "原配对正确：FlashAttention→attention融合，CUDA Graph→录制DAG，torch.compile→算子融合计划，Marlin/Machete→INT4反量化。"
  },
  {
    "id": "u6-l3-q13",
    "ok": true,
    "right": [
      "H100/H200 大规模推理，精度接近 BF16，硬件原生支持",
      "decode memory-bound 场景，权重带宽减半，校准快（30 分钟内）",
      "A100 平衡推理，通过迁移激活异常值降低量化误差，精度好于纯 INT4",
      "长尾复杂任务追求更高精度，校准耗时但 Hessian 补偿效果好"
    ],
    "note": "原配对正确：FP8→H100/H200，AWQ→decode memory-bound校准快，SmoothQuant→A100迁移激活，GPTQ→Hessian补偿。"
  },
  {
    "id": "u6-l4-q6",
    "ok": true,
    "right": [
      "vllm.core.block_manager.BlockSpaceManager / vllm.v1.core.kv_cache_manager.KVCacheManager",
      "vllm.core.scheduler.Scheduler / vllm.v1.core.scheduler.Scheduler",
      "vllm.worker.cache_engine.CacheEngine",
      "vllm.attention.backends.abstract.AttentionBackend（数学语义）"
    ],
    "note": "原配对正确：block_manager→BlockSpaceManager/KVCacheManager，scheduler→Scheduler，cache_engine→CacheEngine，reference.py→AttentionBackend。"
  },
  {
    "id": "u6-l4-q14",
    "ok": true,
    "right": [
      "请求已到达但 KV 显存不足或 token budget 用完，在队列中等待",
      "请求正在 GPU 上 decode，block table 在 GPU pool 中有效",
      "请求被抢占，KV block 已搬移到 CPU pool 暂存，等待 GPU 显存恢复"
    ],
    "note": "原配对正确：WAITING→队列等待，RUNNING→GPU decode，SWAPPED→KV搬移到CPU。"
  },
  {
    "id": "u6-l5-q4",
    "ok": false,
    "right": [
      "KV Cache 分页寻址，消除显存碎片",
      "长 prompt 切片与 decode 混合，避免独占 forward",
      "相同 token id 前缀的 KV block 被多请求复用",
      "draft 预测 k 个候选，target 一次 verify，低并发下降低延迟"
    ],
    "note": "原 right[0]='相同token前缀KV复用'（Hash-based prefix cache的作用），right[3]='KV Cache分页寻址'（PagedAttention的作用），与 left=[PagedAttention block table, Chunked prefill, Hash-based prefix cache, Speculative decoding] 错位。修复后各项对应正确机制。"
  },
  {
    "id": "u6-l5-q14",
    "ok": true,
    "right": [
      "scale fuse 进前一层，推理时接近零反量化开销，走 Machete/Marlin kernel",
      "Hessian 补偿量化权重，走 Machete（H100）kernel 避免反量化 fallback",
      "H100 Tensor Core 原生 FP8 matmul，KV Cache 可同时用 FP8 存储",
      "激活迁移后 INT8 matmul，适合 A100 大规模部署"
    ],
    "note": "原配对正确：AWQ→scale fuse/Machete/Marlin，GPTQ→Hessian补偿/Machete，FP8→H100原生TC，INT8 SmoothQuant→激活迁移后INT8 matmul。"
  },
  {
    "id": "u6-l6-q4",
    "ok": false,
    "right": [
      "token 粒度 prefix 复用，支持 fork 分叉不复制 KV",
      "grammar 强制唯一 token 时跳过 forward 直接输出",
      "优先调度能复用最长 prefix 的请求，最大化 cache hit",
      "把生成程序结构（fork/join/select）显式传递给后端调度器"
    ],
    "note": "原 right[0]='优先调度最长prefix'（LSPF调度的作用），right[1]='token粒度prefix复用'（RadixAttention的作用），right[2]='把程序结构传给后端'（Frontend DSL的作用），right[3]='grammar唯一token跳过forward'（Jump-Forward的作用），与 left=[RadixAttention, Jump-Forward, LSPF调度, Frontend DSL] 顺序完全错乱。修复后各项对应正确机制。"
  },
  {
    "id": "u6-l6-q14",
    "ok": true,
    "right": [
      "优势最大：RadixAttention 复用共享前缀 KV，fork/join 减少重复计算",
      "优势最小：无共享前缀可复用，RadixAttention 无额外收益",
      "优势明显：fork 后所有分支共享公共 prefix KV，节省显存和 prefill",
      "有明显优势：jump-forward + FSM 减少实际解码步数，输出格式有保证"
    ],
    "note": "原配对正确：Agent多步→优势最大，单轮不同→优势最小，并行采样fork→优势明显，结构化生成→有明显优势。"
  }
]}
