{
  "items": [
    {
      "id": "u7-l12-q11",
      "ok": false,
      "right": [
        "正常情况下的资源权利/保底配额",
        "绝对上限，无论借用与否都不能超过",
        "队列在同一 cohort 内最多可借用的额外上限，超过后无法继续借"
      ],
      "note": "原 right[0] 是 borrowing limit 的描述，right[1] 是 nominal quota 的描述，right[2] 是 hard cap 的描述。正确顺序：nominal quota→保底配额，hard cap→绝对上限，borrowing limit→可借用的额外上限。"
    },
    {
      "id": "u7-l13-q1",
      "ok": false,
      "right": [
        "70B 在线推理，强 SLA 跨租户",
        "7B 小模型推理，固定规格强隔离",
        "同团队轻量批处理，可容忍互扰",
        "开发、教学、notebook，目标是可获得性"
      ],
      "note": "explain 明确：整卡→强 SLA 大模型；MIG→固定规格小模型；MPS→同信任域轻量任务；time-slicing→开发教学。原 right 顺序错误：right[0]='同团队轻量批处理'对应 MPS，right[1]='70B 在线推理'对应整卡，right[2]='开发教学'对应 time-slicing，right[3]='7B 小模型'对应 MIG。"
    },
    {
      "id": "u7-l13-q11",
      "ok": false,
      "right": [
        "独占整张物理 GPU，最强隔离和性能确定性",
        "GPC 硬件分区，有独立 HBM 段，故障隔离",
        "把 1 张 GPU 在 Device Plugin 层报告为 N 份，无任何空间隔离"
      ],
      "note": "explain 明确：整卡→独占物理 GPU 最强隔离；MIG→GPC 硬件分区有独立 HBM；time-slicing→Device Plugin 虚报无空间隔离。原 right[0]是 time-slicing 的描述，right[1]是整卡的描述，right[2]是 MIG 的描述，顺序全错。"
    },
    {
      "id": "u7-l14-q3",
      "ok": false,
      "right": [
        "Prometheus Adapter 暴露 custom metrics 调整 Deployment 副本",
        "Kafka/SQS 队列积压触发，支持 scale-to-zero",
        "组合 queue wait、KV cache、tokens/s 和 SLO burn 的业务感知决策"
      ],
      "note": "explain 明确：HPA→Kubernetes 原生指标（Prometheus Adapter + custom metrics）；KEDA→事件驱动和队列积压（Kafka/SQS + scale-to-zero）；Custom scaler→业务语义组合信号。原 right[0]='Kafka/SQS'对应 KEDA，right[1]='组合 queue wait'对应 Custom scaler，right[2]='Prometheus Adapter'对应 HPA，顺序全错。"
    },
    {
      "id": "u7-l15-q2",
      "ok": false,
      "right": [
        "ClusterQueue event 显示 quota blocked 或 gang not satisfied",
        "ResourceFlavor 或节点空闲分布显示 SKU/拓扑不匹配",
        "scheduler event: insufficient nvidia.com/gpu 或 affinity mismatch",
        "container log 显示权重加载或 engine 初始化",
        "queue wait 高、KV 满、TTFT/TPOT 异常"
      ],
      "note": "explain 明确：队列→准入 event（ClusterQueue quota blocked/gang）；形状→资源分布（ResourceFlavor/SKU/拓扑）；调度→scheduler event（insufficient gpu/affinity）；启动→容器日志（权重加载/engine 初始化）；运行时→runtime 指标（queue wait/KV/TTFT/TPOT）。原顺序：right[0]='container log'对应启动，right[1]='scheduler event'对应调度，right[2]='queue wait 高'对应运行时，right[3]='ClusterQueue event'对应队列，right[4]='ResourceFlavor'对应形状，全部错位。"
    },
    {
      "id": "u7-l15-q11",
      "ok": false,
      "right": [
        "ClusterQueue event 显示 quota exceeded 或 gang 不满足",
        "有空闲 GPU 但 SKU/MIG profile/拓扑不匹配，Pod 无法调度到合适节点",
        "Pod Pending 事件中 node affinity/taint/PVC 不满足",
        "Pod Running 但日志显示权重加载或 engine 构建中"
      ],
      "note": "explain 明确：队列层→ClusterQueue/PodGroup event（quota exceeded/gang 不满足）；形状层→scheduler event 中资源匹配失败（SKU/MIG 不对，即'有空闲 GPU 但 SKU/MIG profile/拓扑不匹配'）；调度层→Pod event 中 affinity/taint；启动层→容器日志中加载状态（Pod Running 但日志显示加载中）。原 right[0]='Pod Pending affinity/taint'对应调度层，right[1]='ClusterQueue quota exceeded'对应队列层，right[2]='Pod Running 日志'对应启动层，right[3]='有空闲 GPU SKU 不匹配'对应形状层，全部错位。"
    },
    {
      "id": "u8-l1-q4",
      "ok": false,
      "right": [
        "从请求进入系统到用户看到第一个 token 的端到端耗时",
        "每生成一个 output token 的平均耗时（inter-token latency）",
        "满足 SLO 的请求产生的 token 数，排除超时/失败",
        "已用 KV block / 总 KV block，持续 > 90% 需扩容或限流"
      ],
      "note": "explain 明确：TTFT→queue wait + prefill 端到端耗时；TPOT→decode 阶段 inter-token latency；Goodput→真正产生业务价值的吞吐（满足 SLO）；KV Cache utilization→已用/总 KV block。原 right[0]='满足 SLO'对应 Goodput，right[1]='从请求进入系统'对应 TTFT，right[2]='每生成一个 output token'对应 TPOT，right[3]='已用 KV block'对应 KV Cache utilization。顺序错误。"
    },
    {
      "id": "u8-l1-q13",
      "ok": false,
      "right": [
        "高吞吐稳定路径成本可控，按入口比例随机采样",
        "优先保留超时、5xx、P99 以上慢请求，诊断尾延迟",
        "计算全量 P95/P99 延迟分位数，驱动 SLO 告警",
        "记录单次请求的失败原因、模型版本、用户输入摘要，用于下钻诊断"
      ],
      "note": "explain 明确：Head-based→入口决定，成本可控，按比例随机采样；Tail-based→链路结束后决定，保留错误和慢请求；Metrics histogram→全量统计分位数支撑 SLO；Logs→单次请求详情用于根因下钻。原 right[0]='记录单次请求'对应 Logs，right[1]='计算全量 P95/P99'对应 Metrics histogram，right[2]='高吞吐稳定路径'对应 Head-based，right[3]='优先保留超时'对应 Tail-based，全部错位。"
    },
    {
      "id": "u8-l2-q4",
      "ok": false,
      "right": [
        "replay + LLM-as-judge + 分租户/分场景切片，检查质量退化",
        "系统指标稳定 + 人工抽检高风险样本，确认无 P0 质量事故",
        "随机分流验证效果，主指标显著性 + guardrail 双达标",
        "核心指标达基线、回归集零失败、安全测试通过、成本在预算内"
      ],
      "note": "explain 明确：离线评测→入口门禁（replay + LLM-as-judge + 切片）；1% canary→硬指标 + 人工抽检（系统指标稳定 + 人工抽检高风险样本）；5% canary→质量采样和切片分析... 但 explain 说'5% canary 深入质量采样和切片分析'，而 right 中'replay + LLM-as-judge + 分租户/分场景切片'更符合离线评测（explain 直接说离线评测是入口门禁，用 replay+LLM-judge+切片）；'核心指标达基线、回归集零失败'是离线阶段的门禁标准；实际上 explain 说：离线→入口门禁；1% canary→先看硬指标和人工抽检；5% canary→深入质量采样和切片；A/B→统计显著性。对应：离线评测→'replay + LLM-as-judge + 分租户/分场景切片，检查质量退化'；1% canary→'系统指标稳定 + 人工抽检高风险样本，确认无 P0 质量事故'；5% canary→'核心指标达基线、回归集零失败、安全测试通过、成本在预算内'；受控 A/B→'随机分流验证效果，主指标显著性 + guardrail 双达标'。原 right[0]='随机分流'对应 A/B，right[1]='核心指标达基线'对应 5% canary，right[2]='系统指标稳定+人工抽检'对应 1% canary，right[3]='replay+LLM-as-judge'对应离线评测，全部错位。"
    },
    {
      "id": "u8-l2-q12",
      "ok": false,
      "right": [
        "监控成功率、超时率、P95 延迟、平均 token，判断系统是否立刻变坏",
        "将灰度窗口真实请求回放到新旧版本，防止线上噪声掩盖退化",
        "优先审查高价值租户、工具调用、安全敏感场景，识别危险输出",
        "让独立 judge prompt 对新旧版本回答打分，快速覆盖大批量样本"
      ],
      "note": "explain 明确：在线指标监控→最快发现硬错误（监控成功率/超时率/P95/token）；离线 replay→用真实流量对比排除噪声（回放到新旧版本）；人工抽检→判断质量和安全的'是否危险'（优先审查高价值租户/安全敏感场景）；LLM-as-judge→覆盖大批量自动打分（judge prompt 对新旧打分）。原 right[0]='让独立 judge prompt'对应 LLM-as-judge，right[1]='监控成功率'对应在线指标，right[2]='优先审查高价值租户'对应人工抽检，right[3]='将灰度窗口真实请求回放'对应离线 replay，全部错位。"
    },
    {
      "id": "u8-l3-q4",
      "ok": false,
      "right": [
        "暴露镜像和依赖中的已知 CVE 漏洞和配置风险",
        "让产物在部署前可验证「确实由可信身份签过」，防止镜像被替换",
        "声明产物由哪个源码、哪个构建器、哪个流水线生成，支持追溯",
        "防止权重加载时执行任意 Python 代码（替代 pickle 格式）"
      ],
      "note": "explain 明确：Trivy→扫描已知漏洞（暴露镜像和依赖中的已知 CVE）；cosign→签名验证防替换（让产物可验证由可信身份签过）；SLSA provenance→构建可追溯性（声明由哪个源码/构建器/流水线生成）；SafeTensors→消除权重加载时的代码执行风险（防止加载时执行 Python 代码，替代 pickle）。原 right[0]='声明产物由哪个源码'对应 SLSA，right[1]='暴露镜像 CVE'对应 Trivy，right[2]='防止权重加载时执行 Python'对应 SafeTensors，right[3]='让产物可验证由可信身份签过'对应 cosign，全部错位。"
    },
    {
      "id": "u8-l3-q14",
      "ok": false,
      "right": [
        "K8s ServiceAccount + 云 IAM Role，短生命周期凭证，租户身份绑定",
        "K8s ResourceQuota + LimitRange 限制每个 namespace 的 GPU/内存/队列上限",
        "数据集和模型仓库按 RBAC/ABAC 授权，向量索引按租户分区",
        "租户独立 namespace 或 node pool，熔断 + 队列隔离，防止级联故障"
      ],
      "note": "explain 明确：身份隔离→控制'谁能提交/访问'（ServiceAccount + IAM Role + 短生命周期凭证）；资源隔离→控制'能占多少'（ResourceQuota + LimitRange）；数据隔离→控制'能读哪些'（RBAC/ABAC + 向量索引分区）；故障隔离→控制'坏了影响谁'（独立 namespace/node pool + 熔断 + 队列隔离）。原 right[0]='ResourceQuota+LimitRange'对应资源隔离，right[1]='数据集模型仓库 RBAC/ABAC'对应数据隔离，right[2]='ServiceAccount+IAM Role'对应身份隔离，right[3]='租户独立 namespace node pool'对应故障隔离，全部错位。"
    },
    {
      "id": "u9-l1-q4",
      "ok": false,
      "right": [
        "统一训练 Job 入口、镜像基线、命名规范",
        "实验追踪、registry、发布门禁、监控与回滚",
        "队列、配额、租户隔离、成本归因",
        "多地域、智能调度、自动容量优化"
      ],
      "note": "explain 明确：阶段一→规范化（统一入口/命名）；阶段二→闭环化（实验追踪/registry/门禁/监控）；阶段三→平台化（队列/配额/隔离/成本）；阶段四→高阶化（多地域/智能调度/自动优化）。原 right[0]='队列/配额/租户隔离/成本归因'对应阶段三，right[1]='统一训练 Job 入口'对应阶段一，right[2]='多地域/智能调度'对应阶段四，right[3]='实验追踪/registry'对应阶段二，全部错位。"
    },
    {
      "id": "u9-l1-q13",
      "ok": false,
      "right": [
        "GPU / CPU / 存储 / 网络；节点池；镜像基线",
        "队列 / 配额 / 优先级；训练任务抽象；推理服务模板",
        "数据版本；实验追踪；模型 registry；评测门禁；发布与回滚",
        "可观测性；审计；成本归因；SLA / SLO；incident response"
      ],
      "note": "explain 明确：资源层（第 0 层）→硬件基础（GPU/CPU/存储/网络/节点池/镜像基线）；运行层（第 1 层）→容器/队列/任务抽象（队列/配额/优先级/训练任务抽象/推理服务模板）；工作流层（第 2 层）→数据/实验/registry/门禁/发布（数据版本/实验追踪/registry/评测门禁/发布与回滚）；运营层（第 3 层）→可观测性/成本归因/incident（可观测性/审计/成本归因/SLA/SLO/incident response）。原 right[0]='队列/配额/优先级'对应运行层，right[1]='可观测性/审计'对应运营层，right[2]='GPU/CPU/存储'对应资源层，right[3]='数据版本/实验追踪'对应工作流层，全部错位。"
    },
    {
      "id": "u9-l1-q19",
      "ok": true,
      "right": [
        "提交训练任务 / 创建评测运行 / 发布模型版本",
        "直接申请裸 Pod / 用户自己拼 Job-PVC / 手工改 Deployment"
      ],
      "note": "原配对正确：更好的 API 入口→提交训练任务/创建评测运行/发布模型版本（任务语义暴露）；不够好的 API 入口→直接申请裸 Pod/拼 Job-PVC/手工改 Deployment（底层资源暴露）。与 explain 一致。"
    },
    {
      "id": "u9-l2-q4",
      "ok": false,
      "right": [
        "任务拆分、工具执行、状态管理、预算扣减、step trace",
        "单次模型调用的 batching、prefill、decode、KV Cache",
        "在沙箱中执行工具，设置超时、权限、网络和文件边界",
        "每步前 reservation，完成后 settle，失败时释放未用额度"
      ],
      "note": "explain 明确：Agent runtime→流程/状态/预算扣减（任务拆分、工具执行、状态管理、预算扣减、step trace）；推理服务→模型执行（batching/prefill/decode/KV Cache）；Tool runner→外部动作（沙箱/权限/幂等：在沙箱中执行工具，设置超时/权限/网络/文件边界）；quota/billing→预算会计（每步前 reservation，完成后 settle，失败时释放）。原 right[0]='batching/prefill/decode'对应推理服务，right[1]='在沙箱中执行工具'对应 Tool runner，right[2]='任务拆分/工具执行/状态管理'对应 Agent runtime，right[3]='每步前 reservation'对应 quota/billing，全部错位。"
    },
    {
      "id": "u9-l2-q14",
      "ok": false,
      "right": [
        "reasoning token 数量、单请求 decode 时间变长",
        "候选数量 N 倍并发 GPU、树搜索随深度膨胀",
        "verifier 调用次数、测试执行时间、失败后重试",
        "tool wall time、回注 token 数量、权限成本"
      ],
      "note": "explain 明确：隐式推理预算（thinking tokens）→拉长单请求 decode 时间（reasoning token 数量、单请求 decode 时间变长）；Best-of-N→N 倍并发 GPU（候选数量 N 倍并发 GPU、树搜索随深度膨胀）；verifier/critic→额外模型调用和测试执行（verifier 调用次数、测试执行时间、失败后重试）；工具增强执行→wall time 等待和回注 token（tool wall time、回注 token 数量、权限成本）。原 right[0]='verifier 调用次数'对应验证反思，right[1]='tool wall time'对应工具增强，right[2]='reasoning token'对应隐式推理，right[3]='候选数量 N 倍'对应 Best-of-N，全部错位。"
    },
    {
      "id": "u9-l3-q5",
      "ok": false,
      "right": [
        "切 optimizer states（通信量不变，仍 ≈ 2× params/step）",
        "切 optimizer states + gradient（通信 ≈ 2× params/step）",
        "切 optimizer states + gradient + parameters（通信 ≈ 3× params/step）"
      ],
      "note": "explain 明确：ZeRO-1→只切 optimizer states，通信量和纯 DP 一样（约 2× params/step）；ZeRO-2→再切 gradient（optimizer states + gradient，通信约 2× params/step）；ZeRO-3/FSDP→还切 parameters（optimizer states + gradient + parameters，通信约 3× params/step）。原 right[0]='切 optimizer states + gradient + parameters'对应 ZeRO-3，right[1]='切 optimizer states'对应 ZeRO-1，right[2]='切 optimizer states + gradient'对应 ZeRO-2，全部错位。"
    },
    {
      "id": "u9-l3-q15",
      "ok": false,
      "right": [
        "模型支持最广，multi-LoRA 用 Punica 内核",
        "延迟和吞吐最优，但编译复杂调试困难",
        "prefix cache 设计精妙，激进的 KV 复用优化",
        "HuggingFace 生态友好，接入已有 HF 模型最简单"
      ],
      "note": "explain 明确：vLLM→PagedAttention 和模型兼容性广，multi-LoRA 通过 Punica 内核（模型支持最广，multi-LoRA 用 Punica 内核）；TRT-LLM→TensorRT 编译优化，延迟吞吐最佳但调试难（延迟和吞吐最优，但编译复杂调试困难）；SGLang→prefix cache 和 KV 复用更激进（prefix cache 设计精妙，激进的 KV 复用优化）；TGI→HuggingFace 官方推理库，最易集成 HF 生态（HuggingFace 生态友好，接入已有 HF 模型最简单）。原 right[0]='prefix cache 设计精妙'对应 SGLang，right[1]='延迟和吞吐最优'对应 TRT-LLM，right[2]='模型支持最广'对应 vLLM，right[3]='HuggingFace 生态友好'对应 TGI，全部错位。"
    },
    {
      "id": "u9-l4-q3",
      "ok": false,
      "right": [
        "慢：长请求导致 KV 显存不足，decode 被打断",
        "贵：量化策略未启用或 batch size 过小",
        "错：依赖故障（vector DB / registry / external API）",
        "排队：资源容量不足或调度策略导致积压"
      ],
      "note": "explain 明确：慢→p99 高 + KV evict 率高；贵→GPU-second 成本异常但吞吐正常；错→错误率上升 + 依赖层超时；排队→队列等待时间高 + GPU 利用率低。left[0]='p99 高但错误率低、KV evict 率高'→慢；left[1]='GPU-second 成本异常但吞吐正常'→贵；left[2]='错误率上升、依赖层超时'→错；left[3]='队列等待时间高、GPU 利用率低'→排队。原 right[0]='贵'对应 left[1]，right[1]='慢'对应 left[0]，right[2]='排队'对应 left[3]，right[3]='错'对应 left[2]，全部错位。"
    },
    {
      "id": "u9-l4-q13",
      "ok": false,
      "right": [
        "状态变更（提交 Job、发布模型、修改配额），慢路径，强一致",
        "实际请求 / 训练 step / KV 读写 / GPU 计算，热路径，吞吐+延迟优先",
        "metric / log / trace / audit，旁路，最终一致即可"
      ],
      "note": "explain 明确：控制面→状态变更，慢路径，强一致（提交 Job/发布模型/修改配额）；数据面→实际计算，热路径，低延迟高吞吐（推理引擎/训练进程/KV 读写/GPU 计算）；观测面→旁路采集，最终一致（metric/log/trace/audit）。原 right[0]='metric/log/trace/audit'对应观测面，right[1]='状态变更'对应控制面，right[2]='实际请求/训练 step'对应数据面，全部错位。"
    },
    {
      "id": "u9-l4-q19",
      "ok": false,
      "right": [
        "高优先级、不可抢占、独占副本",
        "可抢占，但需提前 5 分钟通知以便保存 checkpoint",
        "低优先级 spot，随时可被抢占，有重试预算"
      ],
      "note": "explain 明确：在线推理（毫秒级 SLA）→不可抢占、必须独占（高优先级、不可抢占、独占副本）；训练 Job（天级）→可抢占但需提前通知以触发 checkpoint（可抢占，但需提前 5 分钟通知以便保存 checkpoint）；夜间数据处理（弹性）→随时被抢有重试预算，适合 spot（低优先级 spot，随时可被抢占，有重试预算）。原 right[0]='低优先级 spot'对应夜间数据处理，right[1]='高优先级不可抢占'对应在线推理，right[2]='可抢占但需提前通知'对应训练 Job，全部错位。"
    }
  ]
}
