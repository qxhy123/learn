{
  "items": [
    {
      "id": "u2-l1-q5",
      "ok": true,
      "right": [
        "容量、带宽、拓扑、成本",
        "工作流、自动化、治理、稳定性",
        "准确率、召回率、生成质量、幻觉率",
        "用户体验、业务效果、SLA"
      ],
      "note": "资源层→容量/带宽/拓扑/成本，平台层→工作流/自动化/治理，模型层→质量效果，业务层→用户和业务目标，全部对齐。"
    },
    {
      "id": "u2-l1-q14",
      "ok": true,
      "right": [
        "数据/代码/模型/Prompt 版本清晰，才能复现、比较和回滚",
        "训练→评测→发布→观测→反馈→再训练，缺少任何一环系统会退化",
        "上线模型能回答谁训练的、用了哪份数据、评测结果如何、谁批准了",
        "每次训练、推理、检索、日志记录都有成本，需要在效果与成本之间取舍"
      ],
      "note": "版本思维→复现回滚，闭环思维→防止退化，证据链思维→发布质量证据，成本思维→效益平衡，全部对齐。"
    },
    {
      "id": "u2-l2-q7",
      "ok": true,
      "right": [
        "数据加载或 CPU 预处理跟不上 GPU 消耗",
        "下游服务抖动、冷启动或长短请求混排",
        "AllReduce 通信代价超过新增计算收益",
        "checkpoint 阻塞训练主循环"
      ],
      "note": "锯齿利用率→数据供给，p99尾延迟→下游抖动，扩卡收益差→通信开销，周期性卡顿→checkpoint阻塞，全部对齐。"
    },
    {
      "id": "u2-l2-q14",
      "ok": true,
      "right": [
        "对象存储（如 S3/OSS），容量大但延迟高",
        "本地 SSD/NVMe，低延迟、高吞吐",
        "DRAM，CPU 直接访问，带宽高于磁盘",
        "HBM，GPU 直接访问，带宽极高但容量有限"
      ],
      "note": "冷存储→对象存储，热存储→本地SSD/NVMe，主存→DRAM，显存→HBM，全部对齐。"
    },
    {
      "id": "u2-l3-q4",
      "ok": false,
      "right": [
        "逐步放量：1% → 5% → 20% → 100%",
        "新模型旁路推理真实流量但不返回给用户",
        "Blue 保持旧版本、Green 部署新版本、切换入口",
        "风险最大，只适合低风险小流量场景"
      ],
      "note": "原right[0]（旁路推理）是Shadow描述，原right[1]（逐步放量）是Canary描述，两者对调。灰度/Canary→逐步放量，Shadow→旁路推理，蓝绿和直接发布位置正确。"
    },
    {
      "id": "u2-l3-q16",
      "ok": true,
      "right": [
        "query 向量化耗时及模型版本",
        "召回命中率、检索延迟和索引版本",
        "首 token 延迟（TTFT）及 prompt token 数",
        "安全过滤、格式解析耗时和 safety_blocked 信号"
      ],
      "note": "embedding→向量化耗时，vector search→召回命中率，llm_prefill→TTFT，postprocess→安全过滤，全部对齐。"
    },
    {
      "id": "u3-l1-q2",
      "ok": false,
      "right": [
        "04a 执行模型与 Tensor Core",
        "04b HBM 与 Roofline",
        "04c 互联与系统形态",
        "04d 选型虚拟化异构"
      ],
      "note": "原排列全错。nsys显示launch空洞/Tensor Core利用低→04a执行模型；HBM OOM→04b HBM与Roofline；NCCL尾延迟跨节点扩展差→04c互联；MIG/MPS资源池切分→04d选型虚拟化异构。"
    },
    {
      "id": "u3-l1-q12",
      "ok": false,
      "right": [
        "`ncu` occupancy、warp stall、tensor pipe",
        "CapacityLedger + OOM 前 allocator snapshot",
        "Roofline + 有效 HBM 带宽 + DCGM counters",
        "`nccl-tests` + `nvidia-smi topo -m` 拓扑矩阵"
      ],
      "note": "执行(SM/Tensor Core利用低)→ncu；容量(OOM/显存水位高)→CapacityLedger；带宽(HBM打满算力空闲)→Roofline/HBM；互联(collective尾延迟大)→nccl-tests/topo。原排列全错。"
    },
    {
      "id": "u3-l1-q18",
      "ok": false,
      "right": [
        "CapacityLedger：权重、激活、optimizer、KV Cache、workspace",
        "ops、bytes moved、有效 HBM 带宽、Tensor Core 利用",
        "`nvidia-smi topo -m`、`nccl-tests`、NCCL topology dump",
        "`nsys profile`、step time、TTFT/TPOT"
      ],
      "note": "容量账本→CapacityLedger各分项；Roofline信号→ops/bytes/带宽；拓扑证据→topo矩阵/nccl-tests；端到端时间线→nsys/step time/TTFT。原排列全错。"
    },
    {
      "id": "u3-l2-q4",
      "ok": false,
      "right": [
        "选择 ready warp 发射指令",
        "保存每个线程的临时变量",
        "矩阵乘加专用路径（FP16/BF16/FP8 等）",
        "block 内线程数据复用和近端缓存"
      ],
      "note": "Warp scheduler→调度/发射指令；Register file→保存临时变量；Tensor Core→矩阵乘加；Shared memory/L1→数据复用近端缓存。原排列全错。"
    },
    {
      "id": "u3-l2-q13",
      "ok": false,
      "right": [
        "ncu 显示 theoretical occupancy 受 register limit 限制",
        "ncu 显示 theoretical occupancy 受 smem limit 限制",
        "block size 与 SM warp 容量不整除，有 idle warp slot",
        "block 数量很少，SM 上只驻留少量 block"
      ],
      "note": "registers过高→register limit；shared memory过大→smem limit（位置正确）；block size不合适→idle warp slot；SM最大block数限制→SM驻留少。原0/2/3错位。"
    },
    {
      "id": "u3-l3-q5",
      "ok": false,
      "right": [
        "减少激活容量（用重算换显存）",
        "减少 attention 中间显存和带宽",
        "减少权重显存和 HBM 读取带宽",
        "降低 KV Cache 碎片和预留浪费"
      ],
      "note": "Activation checkpointing→减少激活容量(重算换显存)；FlashAttention→减少attention中间显存和带宽；INT8/INT4量化→减少权重显存和HBM带宽；Paged KV→KV Cache碎片管理。原0/1/2错位。"
    },
    {
      "id": "u3-l3-q12",
      "ok": false,
      "right": [
        "2 bytes/param",
        "4 bytes/param（Adam 状态）",
        "4 bytes/param（另一个 Adam 状态）",
        "4 bytes/param（混合精度主权重）"
      ],
      "note": "BF16模型权重→2 bytes/param；FP32 Adam一阶矩m→4 bytes/param(Adam状态)；FP32 Adam二阶矩v→4 bytes/param(另一个Adam状态)；FP32 master weight→4 bytes/param(混合精度主权重)。原排列全错。"
    },
    {
      "id": "u3-l4-q3",
      "ok": false,
      "right": [
        "同一 NVSwitch / NVLink domain",
        "可跨节点，重点是网络 rail 对齐",
        "可跨网络，重点是削峰和恢复时间",
        "同一节点或同一 NVL72 partition"
      ],
      "note": "TP(高频阻塞前向)→同一NVSwitch/NVLink domain(scale-up)；DP(每step梯度同步)→可跨节点rail对齐；Checkpoint(低频大块)→可跨网络削峰恢复；EP(MoE token dispatch)→同节点或NVL72 partition。原排列全错。"
    },
    {
      "id": "u3-l4-q13",
      "ok": false,
      "right": [
        "通过 NVLink 直连",
        "同一 PCIe switch 下（近，但非 NVLink）",
        "通过 PCIe host bridge（较近）",
        "跨 NUMA 节点（最远，通常跨 CPU socket）"
      ],
      "note": "NVL→NVLink直连；PIX→同PCIe switch；PHB→PCIe host bridge；SYS→跨NUMA节点(最远)。原排列全错，距离从近到远NVL<PIX<PHB<SYS。"
    },
    {
      "id": "u3-l5-q3",
      "ok": false,
      "right": [
        "多团队独立小模型推理，需要硬隔离",
        "同一推理服务内多 worker 并发提高 GPU 填充度",
        "Notebook、CI、课程实验等低优先级开发"
      ],
      "note": "MIG→硬件级隔离多租户小模型推理；MPS→同租户进程并发提高GPU填充度；Time-slicing→时间片低优先级开发。原排列全错。"
    },
    {
      "id": "u3-l5-q12",
      "ok": false,
      "right": [
        "硬件级隔离，显存和 SM 实例切分，适合多租户推理",
        "软件级并发，共享 GPU，适合同一租户多 worker 并发",
        "时间片共享，隔离最弱，适合 notebook 和 CI"
      ],
      "note": "MIG→硬件级隔离多租户；MPS→软件级并发同一租户；Time-slicing→时间片最弱隔离。原排列全错。"
    },
    {
      "id": "u3-l6-q3",
      "ok": false,
      "right": [
        "05b：主机设备 IO、PCIe、NUMA 与 overlap",
        "05c：RDMA、Collective 与集群拓扑",
        "05a：内存与存储层级、数据驻留",
        "05d：训练存储、Checkpoint 与 IO 诊断"
      ],
      "note": "DataLoader queue空转→05b(H2D/pinned memory)；64卡扩展效率低→05c(集群拓扑)；模型冷启动慢→05a(热层预热)；全平台周期性尖刺→05d(checkpoint storm)。原0/1对调。"
    },
    {
      "id": "u3-l6-q12",
      "ok": false,
      "right": [
        "PCIe 时序细节",
        "跨节点 collective",
        "checkpoint 存储",
        "泛化存储产品理论"
      ],
      "note": "05a不展开PCIe时序细节；05b不展开跨节点collective；05c不展开checkpoint存储；05d不展开泛化存储产品理论。原排列全错。"
    },
    {
      "id": "u3-l7-q4",
      "ok": false,
      "right": [
        "模型权重、激活、KV Cache、GPU kernel workspace",
        "训练热数据 shard 缓存、模型热副本、shuffle 临时文件",
        "原始训练数据冷层、长期模型制品归档",
        "DataLoader 队列、解码后 batch、主机运行时元数据"
      ],
      "note": "HBM→GPU直接消费(权重/激活/KV Cache)；本地NVMe→热数据shard缓存；对象存储→冷层归档；DRAM→主机侧缓冲(DataLoader队列/batch)。原排列全错。"
    },
    {
      "id": "u3-l7-q12",
      "ok": false,
      "right": [
        "不可精确控制，受全机内存压力影响，无法绑定数据版本",
        "节点失效时缓存丢失，不是长期真相源",
        "非 POSIX，rename 不原子，不适合训练内循环随机读",
        "注意元数据瓶颈（MDS）和小文件问题"
      ],
      "note": "Page Cache→不可控无版本绑定；本地NVMe→节点失效丢失；对象存储→非POSIX rename不原子；并行文件系统(Lustre)→元数据瓶颈小文件。原排列全错。"
    },
    {
      "id": "u3-l8-q4",
      "ok": false,
      "right": [
        "并行读取和预处理的进程数",
        "输出 batch 是否进入锁页内存以支持异步 H2D",
        "每个 worker 提前准备的 batch 数量",
        "epoch 间是否保留 worker 进程，避免每轮重启开销"
      ],
      "note": "num_workers→预处理并发进程数；pin_memory→锁页内存支持异步H2D；prefetch_factor→提前准备batch数量；persistent_workers→epoch间保留worker进程。原排列全错。"
    },
    {
      "id": "u3-l8-q12",
      "ok": false,
      "right": [
        "CPU 并行读取和预处理的 worker 进程数",
        "分配页锁定内存，让 DMA 直接搬运无需 staging",
        "DataLoader 提前准备 batch 的队列深度",
        "让 H2D 拷贝与 GPU compute 异步重叠"
      ],
      "note": "num_workers→CPU预处理并行度；pin_memory=True→锁页DMA；prefetch_factor→队列深度；non_blocking=True→异步H2D重叠。原排列全错。"
    },
    {
      "id": "u3-l9-q4",
      "ok": false,
      "right": [
        "追求任意节点间等价路径和均匀带宽，适合多租户训练",
        "相同编号 NIC 接同一 rail，GPU-NIC 对齐清晰，多 rail 并行可控",
        "group 内高带宽连接 + group 间 global links，适合超大规模但调度复杂"
      ],
      "note": "Fat-tree/Clos→均匀带宽多租户友好；Rail-optimized→GPU-NIC-rail对齐多rail并行；DragonFly+→大规模低成本group内高带宽但调度复杂。原排列全错。"
    },
    {
      "id": "u3-l9-q12",
      "ok": false,
      "right": [
        "DDP 梯度同步，所有 rank 拿到相同的聚合梯度",
        "ZeRO/FSDP 梯度分片，每 rank 只保留一片",
        "FSDP/ZeRO 参数重建，每个 rank 收齐全量参数",
        "Pipeline Parallel 相邻 stage 传 microbatch activation"
      ],
      "note": "AllReduce→DDP梯度同步；ReduceScatter→ZeRO梯度分片；AllGather→参数重建收齐全量；Send/Recv→PP stage间点对点传activation。原0/1/2错位。"
    },
    {
      "id": "u3-l10-q4",
      "ok": false,
      "right": [
        "大规模训练热数据和 checkpoint 大文件，HPC 生态成熟",
        "多租户企业 AI 平台，细粒度配额、快照、策略迁移能力强",
        "用通用 NVMe 服务器快速搭建高吞吐热层，调优直观",
        "AI 热层与对象存储透明分层，商业 AI 平台常见"
      ],
      "note": "Lustre→HPC大文件顺序吞吐成熟；GPFS→企业级治理细粒度配额；BeeGFS→轻量部署通用NVMe快速搭建；WekaFS→AI热层与对象存储分层。原排列全错。"
    },
    {
      "id": "u3-l10-q12",
      "ok": false,
      "right": [
        "大量 worker 并发 stat/open 小文件，GPU 利用率锯齿",
        "首次训练前缓存为空，前几步数据加载极慢",
        "大量并发随机 open 导致吞吐极低且抖动严重",
        "恢复时读到写了一半的 shard，训练崩溃"
      ],
      "note": "热层元数据节点高负载→小文件元数据风暴GPU锯齿；NVMe冷启动→缓存为空前几步极慢；对象存储做热层→高并发随机open吞吐极低；缺少原子提交→读到半成品shard崩溃。原排列全错。"
    },
    {
      "id": "u3-l11-q6",
      "ok": true,
      "right": [
        "GPU HW 时间线空洞、H2D/NCCL 是否重叠",
        "哪个 PyTorch op/module 触发了小 kernel 或同步",
        "单个 kernel 的 occupancy、寄存器压力、warp stall",
        "定位异步错误的真实出错位置（不用于性能结论）"
      ],
      "note": "nsys→全系统时间线，torch.profiler→op/module关联，ncu→单kernel微结构，CUDA_LAUNCH_BLOCKING→同步错误定位，全部对齐。"
    }
  ]
}
