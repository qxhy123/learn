{"items":[
  {
    "id": "u3-l11-q12",
    "ok": false,
    "right": [
      "逐元素小循环、每步 .item() 同步、Python 对象创建开销",
      "算子路由到 CPU fallback 或走非预期的低效实现路径",
      "non-contiguous tensor 触发额外 transpose，或 workspace 过大",
      "每次 launch 有固定 CPU 成本，小 kernel 多时积累显著"
    ],
    "note": "原 right[0]（每次launch固定CPU成本）对应 CUDA runtime/driver 层（left[3]），原 right[1]（逐元素小循环/同步）对应 Python/API 层（left[0]），原 right[2]（算子路由fallback）对应 dispatcher 层（left[1]），原 right[3]（non-contiguous transpose/workspace）对应 ATen 层（left[2]）。重排使 fixedRight[i] 与 left[i] 一一对应。"
  },
  {
    "id": "u3-l12-q6",
    "ok": true,
    "right": [
      "需要额外 cast 或落到慢路径",
      "需要隐式 copy 或使用低效 stride 访问路径",
      "launch 开销主导，GPU 并行度不足",
      "fused attention 后端不可用，退回多个分散 kernel"
    ],
    "note": "right[0]→dtype不匹配→cast/慢路径✓；right[1]→non-contiguous→copy/stride✓；right[2]→shape太小→launch开销主导✓；right[3]→特殊mask→fused attention不可用✓。顺序正确。"
  },
  {
    "id": "u3-l12-q13",
    "ok": false,
    "right": [
      "记录反向图、保存中间 tensor 引用，增加内存和引用管理开销",
      "每个 eager op 的固定路由和 dispatch key 匹配成本",
      "shape 检查、临时 workspace 分配、算法选择",
      "每次 kernel 提交的固定 CPU 驱动路径成本"
    ],
    "note": "原 right[0]（每次kernel提交固定驱动成本）对应 CUDA runtime/driver 层（left[3]），原 right[1]（记录反向图）对应 Autograd 层（left[0]），原 right[3]（每个eager op路由成本）对应 dispatcher 层（left[1]），原 right[2]（shape检查/workspace）对应 ATen 层（left[2]）。重排后 fixedRight=[right[1], right[3], right[2], right[0]]。"
  },
  {
    "id": "u3-l13-q6",
    "ok": true,
    "right": [
      "torch.cuda.synchronize() 放在测量边界",
      "cudaEventRecord + cudaStreamWaitEvent / stream.wait_stream",
      "CUDA_LAUNCH_BLOCKING=1 强制同步",
      "nsys 查看 NCCL row 与 backward compute row 是否交叠"
    ],
    "note": "right[0]→benchmark边界→synchronize✓；right[1]→copy stream局部依赖→event/wait✓；right[2]→debug定位错误→CUDA_LAUNCH_BLOCKING✓；right[3]→检测NCCL overlap→nsys✓。顺序正确。"
  },
  {
    "id": "u3-l13-q12",
    "ok": false,
    "right": [
      "跨 stream 局部数据依赖，避免等整个 GPU 空闲",
      "等待所有 stream 完成，调试或作业结束时使用",
      "CPU 必须等某特定 stream 完成才能进行下一步处理",
      "PyTorch 中跨 stream 简单等待，other 的已提交工作"
    ],
    "note": "原 right[2]（跨stream局部依赖）对应 cudaEventRecord+cudaStreamWaitEvent（left[0]）；原 right[3]（等待所有stream）对应 cudaDeviceSynchronize（left[1]）；原 right[0]（CPU等特定stream）对应 cudaStreamSynchronize（left[2]）；原 right[1]（PyTorch简单等待）对应 stream.wait_stream（left[3]）。"
  },
  {
    "id": "u3-l14-q5",
    "ok": true,
    "right": [
      "register spill：fusion 后 registers/thread 过高",
      "dtype/layout/shape 未命中 Tensor Core 路径",
      "访存不合并（coalescing 差）或 stride 访问",
      "shared memory/register 占用限制了 SM 驻留 warp"
    ],
    "note": "right[0]→fused kernel慢+local memory load→register spill✓；right[1]→Tensor Core utilization低→dtype/layout/shape问题✓；right[2]→HBM bandwidth低但时间长→coalescing差✓；right[3]→occupancy低+memory stall→shared mem/register占用过高✓。顺序正确。"
  },
  {
    "id": "u3-l14-q13",
    "ok": false,
    "right": [
      "中间 tensor 留在 register/shared memory，不写回 HBM",
      "融合后每线程需要更多临时变量，占用更多 register file",
      "多个小 kernel 合并，减少固定调度开销",
      "融合后包含 mask/branch 逻辑，同一 warp 内线程走不同分支"
    ],
    "note": "原 right[1]（中间tensor留在寄存器）对应「减少HBM中间读写」（left[0]）；原 right[3]（临时变量增多）对应「增加register pressure」（left[1]）；原 right[2]（launch减少）对应「降低kernel调度次数」（left[2]）；原 right[0]（mask/branch）对应「可能触发warp divergence」（left[3]）。"
  },
  {
    "id": "u3-l15-q5",
    "ok": true,
    "right": [
      "torch.profiler 按 CPU self time 排序，找小 op/同步/graph break",
      "nsys 看 Memcpy row、DataLoader timing、检查 pinned memory/non_blocking/copy stream",
      "ncu 看 Tensor Core、memory throughput、warp stall、register",
      "日志时间戳 + NVTX range，逐一关闭 checkpoint/logger/eval 做 A/B"
    ],
    "note": "right[0]→launch稀疏→torch.profiler✓；right[1]→H2D串行→nsys Memcpy✓；right[2]→kernel慢→ncu✓；right[3]→周期尖刺→日志时间戳+NVTX✓。顺序正确。"
  },
  {
    "id": "u3-l15-q12",
    "ok": false,
    "right": [
      "torch.profiler 找小 op 热点，尝试 torch.compile 减少 dispatch 次数",
      "检查 DataLoader pinned memory、non_blocking 设置、是否有默认 stream 依赖",
      "多 rank nsys 对比，检查 NCCL 日志和 rank 计算均衡性",
      "检查 checkpoint 写入时机、manifest flush 和文件系统 IO"
    ],
    "note": "原 right[3]（torch.profiler+compile）对应「kernel间空洞+kernel短」（left[0]）；原 right[2]（DataLoader pinned）对应「H2D与compute交替」（left[1]）；原 right[0]（多rank nsys）对应「backward后NCCL尾巴」（left[2]）；原 right[1]（checkpoint IO）对应「每N step GPU长空洞」（left[3]）。"
  },
  {
    "id": "u4-l1-q4",
    "ok": true,
    "right": [
      "PyTorch 各 op 的 CPU/CUDA 时间和 memory 分配",
      "CUDA stream 时间线、H2D DMA、kernel launch gap",
      "单个 kernel 的 SM occupancy 和 Tensor Core 使用率",
      "本地磁盘 I/O 队列深度和 await 延迟"
    ],
    "note": "right[0]→torch.profiler✓；right[1]→Nsight Systems✓；right[2]→Nsight Compute✓；right[3]→iostat✓。顺序正确。"
  },
  {
    "id": "u4-l1-q9",
    "ok": true,
    "right": [
      "固定 shape 中的 token 槽位数，等于 B × S × N_gpu",
      "排除 padding 的真实 token 数，等于 attention_mask.sum()",
      "进入 cross entropy 的 shifted labels 数，labels != -100",
      "实际执行主要模型 FLOPs 的 token 数，无 unpadding 时等于 raw slots"
    ],
    "note": "right[0]→raw_sequence_slots✓；right[1]→non_pad_tokens✓；right[2]→loss_tokens✓；right[3]→compute_tokens✓。顺序正确。"
  },
  {
    "id": "u4-l1-q14",
    "ok": true,
    "right": [
      "GPU 利用率低，DataLoader visible wait 超 step time 的 20%",
      "resume 后 LR schedule 错位，loss 出现台阶式跳变",
      "训练中途 loss 突然变 NaN，GradScaler found_inf 频繁出现",
      "训练循环卡死，无日志输出，进程存活但无前进"
    ],
    "note": "right[0]→数据路径✓；right[1]→状态路径✓；right[2]→精度策略路径✓；right[3]→故障路径✓。顺序正确。"
  },
  {
    "id": "u4-l2-q3",
    "ok": true,
    "right": [
      "AllGather：拼出当前层完整参数",
      "ReduceScatter：每 rank 保留自己的 grad shard",
      "本地更新：只更新自己持有的 param/Adam shard",
      "AllGather：预取下一层参数"
    ],
    "note": "right[0]→forward开始前→AllGather完整参数✓；right[1]→backward结束后→ReduceScatter✓；right[2]→optimizer step→本地更新✓；right[3]→prefetch→AllGather预取✓。顺序正确。"
  },
  {
    "id": "u4-l2-q8",
    "ok": true,
    "right": [
      "dataset shard 按样本数均分，未按 token 数平衡",
      "RDMA 未注入容器，NCCL fallback 到 socket",
      "bucket 太大，最后一个 bucket 延迟高；或有 rank straggler 破坏 overlap",
      "live AllGather 参数增多或 prefetch 导致 HBM 峰值超限"
    ],
    "note": "right[0]→rank/token skew同时高✓；right[1]→NCCL timeout/NET/Socket✓；right[2]→backward后180ms NCCL tail✓；right[3]→OOM只在多节点✓。顺序正确。"
  },
  {
    "id": "u4-l2-q14",
    "ok": true,
    "right": [
      "只切 optimizer state（Adam m/v），params 和 grads 每卡完整复制",
      "切 optimizer state + grads，params 每卡完整复制",
      "params、grads、optimizer state 全部切分，forward 时 AllGather 临时拼出完整参数",
      "params、grads、optimizer state 全部每卡完整复制，无切分"
    ],
    "note": "right[0]→ZeRO-1✓；right[1]→ZeRO-2✓；right[2]→ZeRO-3/FSDP✓；right[3]→DDP✓。顺序正确。"
  },
  {
    "id": "u4-l2-q19",
    "ok": true,
    "right": [
      "缩小 bucket size 或启用 gradient compression 减少通信量",
      "检查 DCGM/nvidia-smi 排查 GPU 降频、ECC 错误或 NUMA 错配",
      "改为按 non-pad token budget 均分 shard，而非按样本数均分",
      "用 profiler 验证 AllGather 瞬时峰值并调整 limit_all_gathers 参数"
    ],
    "note": "right[0]→通信暴露25%✓；right[1]→rank持续慢30%✓；right[2]→dataset按样本均分seq波动✓；right[3]→FSDP多节点OOM✓。顺序正确。"
  },
  {
    "id": "u4-l3-q4",
    "ok": true,
    "right": [
      "单层矩阵/activation 超出单卡容量，切层内 hidden/heads",
      "整网层数太多，每卡只承载一段层段，microbatch 像流水线流过",
      "超长 attention context，每 rank 只持有 S/CP 的 token，ring 交换 KV",
      "TP 组内非 attention activation 仍重复存储，按 sequence 分片减少冗余"
    ],
    "note": "right[0]→TP✓；right[1]→PP✓；right[2]→CP✓；right[3]→SP✓。顺序正确。"
  },
  {
    "id": "u4-l3-q8",
    "ok": true,
    "right": [
      "约 2(p-1)/m",
      "(p-1)/m",
      "约 (p-1)/(v×m)",
      "接近 0（W-pass 填空槽）"
    ],
    "note": "right[0]→GPipe✓；right[1]→1F1B✓；right[2]→Interleaved 1F1B✓；right[3]→Zero Bubble✓。顺序正确。"
  },
  {
    "id": "u4-l3-q14",
    "ok": true,
    "right": [
      "同层下一个 ColumnParallel GEMM（需 CUDA_DEVICE_MAX_CONNECTIONS=1 + 独立 stream）",
      "同 stage 下一个 microbatch 的 forward/backward compute（需 m ≥ PP）",
      "前一 FSDP unit 的 backward compute（需 BACKWARD_PRE 模式）",
      "本地 context block 的 attention 计算（Ring FlashAttention 实现）"
    ],
    "note": "right[0]→TP AllReduce覆盖下一GEMM✓；right[1]→PP send/recv覆盖下一microbatch✓；right[2]→FSDP AllGather prefetch覆盖前一unit backward✓；right[3]→CP Ring KV覆盖本地attention✓。顺序正确。"
  },
  {
    "id": "u4-l3-q19",
    "ok": true,
    "right": [
      "Tensor Parallel（TP），在节点内 NVSwitch 切分层内 hidden/heads",
      "Pipeline Parallel（PP），切层段减少每卡持有的层数",
      "Context Parallel（CP），按 token 分片做 Ring FlashAttention",
      "FSDP FULL_SHARD 或 ZeRO-3，切分训练状态"
    ],
    "note": "right[0]→单层hidden矩阵超单卡→TP✓；right[1]→层数极多→PP✓；right[2]→128K context→CP✓；right[3]→optimizer state无法放单卡→FSDP/ZeRO-3✓。顺序正确。"
  },
  {
    "id": "u4-l4-q4",
    "ok": true,
    "right": [
      "λ × Σ(f_i × P_i) 加入主 loss，但与主 loss 梯度方向可能冲突",
      "动态 bias 调整 expert score，不污染主 loss 梯度，bias 须随 checkpoint 保存",
      "预分配固定 token 槽，超出 token 被 drop，监控 drop_rate",
      "按实际 token 数动态 dispatch，监控 max_load、buffer peak 和 straggler"
    ],
    "note": "right[0]→Aux Loss✓；right[1]→Loss-Free Balancing✓；right[2]→Capacity Factor dropping✓；right[3]→Dropless MoE✓。顺序正确。"
  },
  {
    "id": "u4-l4-q8",
    "ok": true,
    "right": [
      "drop_rate（每层，告警阈值 >20% 持续 1k step）",
      "p99 expert tokens、dispatch buffer peak、overflow fallback count",
      "某几个 expert 的 f_i（实际选中频率）远高于均值",
      "alltoallv bytes 的 send/recv rank skew，某些 rank 成为 incast 热点"
    ],
    "note": "right[0]→Dropping MoE→drop_rate✓；right[1]→Dropless MoE→buffer peak✓；right[2]→Expert坍缩→f_i分布✓；right[3]→EP网络→alltoallv rank skew✓。顺序正确。"
  },
  {
    "id": "u4-l4-q15",
    "ok": true,
    "right": [
      "通过 expert_owner manifest 重映射，不能按 rank 索引直接计算",
      "bias 必须随 expert 一起迁移到新 ep_rank，否则平衡状态丢失",
      "checkpoint writer 选 canonical replica 或 checksum 去重，避免误认为独立 shard",
      "比对 reshape 前后每个 expert 的权重 checksum，确认无静默错误"
    ],
    "note": "right[0]→确定ep_rank→manifest重映射✓；right[1]→迁移bias✓；right[2]→处理ETP>1副本→canonical replica去重✓；right[3]→验证权重正确性→checksum✓。顺序正确。"
  },
  {
    "id": "u4-l4-q19",
    "ok": true,
    "right": [
      "Router 坍缩：无负载均衡约束导致 token 集中于少数热门 expert",
      "All-to-All incast 打爆交换机 shared buffer，导致丢包和重传",
      "Aux Loss 系数 α 过小，负载均衡约束不足，expert 逐渐分化",
      "EP Reshape 未使用 expert manifest 重映射，expert 参数错位"
    ],
    "note": "right[0]→大部分GPU利用率低少数OOM→Router坍缩✓；right[1]→All-to-All延迟突增IB丢包→incast✓；right[2]→expert利用率极不均匀→aux loss不足✓；right[3]→EP Reshape后loss异常→manifest映射错误✓。顺序正确。"
  },
  {
    "id": "u4-l5-q3",
    "ok": true,
    "right": [
      "用重算 forward 换取 activation 显存降低 30-70%",
      "降低 optimizer state 的单卡常驻量，端到端约 2.9×-N× reduction",
      "将 optimizer state 移到 CPU DRAM，GPU 容量手段但可能大幅降低 GPU duty cycle",
      "减少 CUDA allocator 的内存碎片，缓解 reserved >> allocated 问题"
    ],
    "note": "right[0]→Activation checkpointing✓；right[1]→ZeRO✓；right[2]→CPU offload✓；right[3]→expandable_segments✓。顺序正确。"
  },
  {
    "id": "u4-l5-q7",
    "ok": true,
    "right": [
      "GPU tensor 已转移到 staging buffer，但 flush 尚未完成",
      "训练可进入下一 step，但 checkpoint 仍在后台 flush",
      "manifest validated，latest.json 以 CAS/generation 原子更新",
      "所有 rank 观察到同一 latest pointer，该 step 计入 last_validated_capture_step"
    ],
    "note": "right[0]→SnapshotStaged✓；right[1]→TrainMayAdvance✓；right[2]→PublishCAS✓；right[3]→PostPublishBarrier✓。顺序正确。"
  },
  {
    "id": "u4-l5-q13",
    "ok": true,
    "right": [
      "LR 重复 warmup 或提前 decay，收敛轨迹不可复现",
      "dropout 掩码和数据增强随机顺序改变，训练结果不可复现",
      "恢复后重复消费已训练样本或跳过部分样本",
      "scale 重置为初始值，前几步可能因 scale 过高产生大量 overflow"
    ],
    "note": "right[0]→scheduler state→LR错位✓；right[1]→RNG state→随机性改变✓；right[2]→dataset sampler cursor→样本重复/跳过✓；right[3]→GradScaler state→overflow✓。顺序正确。"
  },
  {
    "id": "u4-l5-q18",
    "ok": true,
    "right": [
      "大规模作业 async staging 在 CPU DRAM 不够时会溢出到 GPU，但计算显存预算里常不计入",
      "reserved >> allocated 时 nvidia-smi 显示空闲但大块 allocation 仍会 OOM",
      "AllGather/ReduceScatter 的临时 buffer 在 profiler 里不直接显示为 peak",
      "随 batch × seq × hidden × layers 增长最快，且在短序列 baseline 上很难被预估到长序列规模"
    ],
    "note": "right[0]→M_ckpt_buffer✓；right[1]→M_frag✓；right[2]→M_comm_buffers✓；right[3]→M_activations✓。顺序正确。"
  }
]}
