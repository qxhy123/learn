[
  {
    "id": "u1c15",
    "title": "文件系统与存储内核导览",
    "subtitle": "AI Infra 工程师的存储问题地图",
    "intro": [
      {
        "title": "为什么 AI Infra 必须懂存储",
        "body": "训练系统最终处理的是字节。dataset、权重、checkpoint、日志都要在**慢速、会故障、被共享**的介质上存在。\n\n三个不可化简的问题：\n- **介质慢**：GPU step 毫秒级，NVMe 微秒级，网络文件系统常进入毫秒级\n- **机器会失败**：进程、节点、电源可能在任意系统调用边界失败\n- **命名空间共享**：多 worker、多 rank 同时 `open/stat/read/write/rename`，元数据路径会成瓶颈\n\n「磁盘够不够快」只是其中最简单的问题。"
      },
      {
        "title": "存储 IO 链路全景",
        "body": "一次文件读写的完整路径：\n\n`训练进程` → `系统调用 / SDK` → `VFS（POSIX 抽象层）` → `dentry/inode（命名与身份）` → `Page Cache（文件页缓存）` → `本地文件系统 ext4/XFS/ZFS` → `Block layer（bio + 调度器）` → `NVMe / 云盘 / RAID`\n\n对象存储走另一条路：进程 → `对象存储 SDK` → `Multipart upload + Manifest`\n\n并行文件系统：进程 → `客户端` → `MDS（元数据）` + `OSS/OST（条带化数据）`",
        "tip": "数据路径（读写字节）与元数据路径（open/stat/rename/list）是两条独立瓶颈，排障时必须分开分析。"
      },
      {
        "title": "按 workload 形状匹配阅读重点",
        "body": "| workload | IO 形状 | 首要风险 |\n|---|---|---|\n| checkpoint 写入 | 大文件顺序写 + 原子发布 | 半成品暴露、`fsync` 尾延迟 |\n| WebDataset / tar shard | 大块顺序读 | 带宽、预读、worker 分片 |\n| 小图片目录 | 海量 `open/stat/read/close` | dentry/inode/MDS/IOPS |\n| safetensors 权重加载 | 大文件顺序读或 `mmap` fault | Page Cache 误判、NUMA |\n| 对象存储 dataset | Range GET + 本地 cache | LIST、重试、manifest 语义 |"
      },
      {
        "title": "五大常见误判",
        "body": "1. `write()` 返回快 ≠ 设备写快：可能只是 **Page Cache 吸收**了脏页，数据未落盘\n2. 第二轮 epoch 快 ≠ dataset 格式好：可能只是 Page Cache / 客户端 cache 命中\n3. `fio --direct=0` 高吞吐 ≠ 真实设备持续写入能力，要用 `--direct=1` 测\n4. 对象存储的 key ≠ POSIX inode，`rename` 通常是 copy/delete 或应用层模拟\n5. 并行文件系统大带宽 ≠ 小文件 `stat` 快，**MDS 可能先被打满**"
      },
      {
        "title": "关于 S3 一致性的细节",
        "body": "S3 自 2020 年 12 月起提供 **strong read-after-write** 与 **strong LIST**。但注意：\n- FUSE 挂载层（如 `s3fs`）会重新引入弱一致性窗口\n- LIST 在对象数极多时依然昂贵——「LIST 语义强了」不等于「LIST 便宜了」\n- 正确做法：用显式 manifest 文件记录数据集成员，不依赖 LIST 的正确性",
        "tip": "把\"S3 强一致\"当成\"任意 LIST 都便宜且随时可用\"是一个常见的生产事故来源。"
      },
      {
        "title": "排障快速入口",
        "body": "| 命令 | 用途 |\n|---|---|\n| `findmnt -T /mnt/dataset -o TARGET,SOURCE,FSTYPE,OPTIONS` | 查看挂载类型和选项 |\n| `iostat -x 1` | 观察设备级 IO 利用率与队列深度 |\n| `pidstat -d -p <pid> 1` | 观察进程级读写字节数 |\n| `grep -E 'Cached\\|Dirty\\|Writeback' /proc/meminfo` | Page Cache 与脏页状态 |\n| `df -ih /mnt/dataset` | inode 使用量（小文件集群常先耗尽 inode） |\n| `lsof +L1` | 列出 link count=0 仍被打开的文件（\"删了但 df 不降\"）|",
        "tip": "GPU idle + 磁盘吞吐低 + `open()` 慢 → 首先怀疑**元数据路径**，而非数据带宽。"
      },
      {
        "title": "存储选型 checklist",
        "body": "进入任何存储优化项目前，先回答：\n- 是否把 workload 拆成**吞吐、IOPS、延迟、元数据、一致性、成本**六个维度？\n- 是否分别做 cold-cache 和 warm-cache benchmark？\n- 是否明确 `write()` → `fsync()` → `rename()` → manifest 的发布边界？\n- 是否避免把对象存储当作完全 POSIX 文件系统？\n- 是否为百万小文件设计 shard、索引和本地 cache？",
        "reveal": {
          "q": "为什么 checkpoint 通常先写临时文件，再做 rename 发布？",
          "a": "直接写目标路径时，如果进程崩溃，读者可能读到半成品。先写临时文件（同一目录），写完后 rename 是接近原子的操作（POSIX 保证 rename 是原子的），可以保证读者要么看到完整旧版，要么看到完整新版，永远不会看到半截数据。"
        }
      }
    ],
    "questions": [
      {
        "type": "choice",
        "prompt": "训练时 `write()` 系统调用返回很快，最可能的原因是什么？",
        "options": [
          "NVMe SSD 写入速度极快，数据已落盘",
          "Page Cache 吸收了脏页，数据尚未写到设备",
          "内核开启了 Direct IO 模式，绕过缓存",
          "文件系统 journal 加速了写入"
        ],
        "answer": 1,
        "explain": "默认情况下 write() 写到 Page Cache 就返回，内核异步将脏页刷到设备。数据未必已落盘。Direct IO 恰好相反——它绕过 Page Cache，write() 会等设备确认。"
      },
      {
        "type": "judge",
        "prompt": "用 `fio --direct=0` 测得高吞吐，可以认为设备具备同等的持续写入能力。",
        "answer": false,
        "explain": "`--direct=0` 即 buffered IO，写入先进 Page Cache，fio 测的是内存带宽而非设备带宽。需用 `--direct=1` 绕过 Page Cache，才能测到真实设备持续写入能力。"
      },
      {
        "type": "choice",
        "prompt": "GPU idle、磁盘吞吐低、`open()` 系统调用很慢，最可能的瓶颈是哪里？",
        "options": [
          "数据路径带宽不足，需要换更快的 NVMe",
          "元数据路径瓶颈，如 dentry/inode 缓存未命中或 MDS 打满",
          "Page Cache 容量不足导致频繁 eviction",
          "GPU PCIe 带宽受限"
        ],
        "answer": 1,
        "explain": "磁盘吞吐低但 open() 慢，说明数据没有大量传输却卡在文件打开阶段——这是典型的元数据路径瓶颈（dentry/inode miss、并行文件系统 MDS 过载等）。数据路径带宽问题会表现为高吞吐但仍不够快。"
      },
      {
        "type": "match",
        "prompt": "将存储 IO 链路的层次与其职责匹配：",
        "left": ["VFS", "Page Cache", "dentry/inode", "Block layer"],
        "right": [
          "将文件页缓存在内存，减少设备访问",
          "提供 POSIX 文件操作的统一抽象接口",
          "管理 bio 请求的调度与合并",
          "记录文件命名（路径）与身份（权限/大小/位置）"
        ],
        "explain": "VFS 是 POSIX 抽象层；Page Cache 缓存文件页；dentry 是路径缓存，inode 是文件元数据；Block layer 做 bio 请求调度与合并后发到设备。"
      },
      {
        "type": "judge",
        "prompt": "S3 在 2020 年底更新为强一致性后，对对象存储做 LIST 操作的代价也大幅降低了。",
        "answer": false,
        "explain": "S3 强一致性保证了 LIST 结果的正确性，但 LIST 操作本身在对象数量极多时依然很昂贵（延迟高、请求数多）。「一致性强」和「操作代价低」是两件不同的事。"
      },
      {
        "type": "input",
        "prompt": "查看某路径挂载类型、来源和选项，应使用什么命令？（写出命令名即可）",
        "accept": ["findmnt"],
        "explain": "`findmnt -T /path -o TARGET,SOURCE,FSTYPE,OPTIONS` 可快速查看挂载信息，是排查文件系统类型的第一步。"
      },
      {
        "type": "choice",
        "prompt": "对象存储（如 S3）中的 `rename` 操作与 POSIX 本地文件系统的 `rename` 最主要的区别是什么？",
        "options": [
          "对象存储的 rename 更快，因为只改元数据指针",
          "对象存储没有 rename，必须应用自己实现 copy + delete",
          "对象存储的 rename 通常是 copy/delete 或应用层模拟，代价随文件大小增长",
          "两者完全等价，均为 O(1) 的原子操作"
        ],
        "answer": 2,
        "explain": "S3 等对象存储没有原子 rename 原语。常见实现是先 copy 对象到新 key，再 delete 旧 key，代价是 O(文件大小)，且两步之间有窗口。部分工具用 manifest 文件模拟原子发布。"
      },
      {
        "type": "judge",
        "prompt": "用 `lsof +L1` 可以找出已被 `unlink` 删除但仍被进程持有（因此 `df` 空间不释放）的文件。",
        "answer": true,
        "explain": "`+L1` 过滤 link count < 1 的文件，即已从目录树中删除（unlink）但仍有进程打开的文件。这些文件占用的磁盘空间不会释放，直到最后一个文件描述符关闭。"
      },
      {
        "type": "choice",
        "prompt": "百万小图片存在目录树里训练，发现 `df -h` 显示空间充足，但写新文件报错。最可能的原因是？",
        "options": [
          "Page Cache 已满，无法缓存新文件",
          "inode 已耗尽，文件系统无法分配新文件元数据",
          "Block layer 调度队列溢出",
          "VFS dentry cache 被 evict 导致路径解析失败"
        ],
        "answer": 1,
        "explain": "文件系统的 inode 数量在格式化时固定（ext4 默认每 16KB 数据分配一个 inode）。百万小文件可能先耗尽 inode，即使数据块空间充足也无法创建新文件。`df -ih` 可查 inode 使用率。"
      }
    ]
  }
]
