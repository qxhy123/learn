export const LESSONS = [
  {
    id: 't10-a',
    title: '多项分布与多元正态分布',
    subtitle: '从二项推广到多类别，从一维正态推广到向量正态',
    intro: [
      {
        title: '多项分布：多类别的计数',
        body: '二项分布描述 n 次试验中"成功"次数，每次只有 2 个结果。当每次试验有 k 个可能结果（概率分别为 p₁, …, pₖ，Σpᵢ = 1）时，n 次试验中各类别的计数向量 (X₁, …, Xₖ) 服从多项分布 Multinomial(n, p)。联合 PMF：将 n 次试验分配给 k 类的方案数（多项式系数 n!/(x₁!…xₖ!)）× 每种分配的概率（Πpᵢ^{xᵢ}）。约束：ΣXᵢ = n。',
        formula: 'P(X_1=x_1,\\ldots,X_k=x_k) = \\frac{n!}{x_1!\\cdots x_k!}\\prod_{i=1}^k p_i^{x_i}, \\quad \\sum_i x_i = n',
        tip: '多项分布是二项分布在 k > 2 类别时的推广，k=2 时退化为二项分布。',
      },
      {
        title: '多项分布的边缘分布与协方差',
        body: '边缘分布：每个 Xᵢ 单独看服从二项 Binomial(n, pᵢ)，期望 E[Xᵢ] = npᵢ，方差 Var(Xᵢ) = npᵢ(1−pᵢ)。协方差：不同类别之间负相关——一类多了必然挤占其他类，Cov(Xᵢ, Xⱼ) = −npᵢpⱼ（i≠j）。推导思路：ΣXᵢ = n（常数），Var(ΣXᵢ) = 0，展开后各协方差之和 = −各方差之和，整理得上式。n=1 时多项分布退化为类别分布（Categorical），正是 softmax 输出对应的分布。',
        formula: 'E[X_i] = np_i, \\quad \\mathrm{Cov}(X_i, X_j) = -np_ip_j\\ (i \\ne j)',
        tip: '负协方差直觉：总量固定，一类多了其他类必然少——就像分蛋糕，你多了我就少。',
      },
      {
        title: '数字例：三候选人选举',
        body: '三位候选人支持率 p₁=0.5, p₂=0.3, p₃=0.2，随机调查 10 人，(X₁, X₂, X₃) ~ Multinomial(10, (0.5, 0.3, 0.2))。P(X₁=5, X₂=3, X₃=2) = 10!/(5!3!2!) × 0.5⁵ × 0.3³ × 0.2² = 252 × 0.03125 × 0.027 × 0.04 ≈ 0.085。期望：E[X₁] = 5，E[X₂] = 3，E[X₃] = 2。协方差：Cov(X₁,X₂) = −10×0.5×0.3 = −1.5（负相关）。',
        tip: '多项系数 n!/(x₁!…xₖ!) 是把 n 次试验分配给 k 类的方案数，是二项系数 C(n,k) 的推广。',
      },
      {
        title: '多元正态分布：向量版的高斯',
        body: 'd 维随机向量 X ~ N(μ, Σ)，均值向量 μ ∈ ℝ^d，协方差矩阵 Σ（对称正定，d×d）。PDF 中的指数项是"马氏距离"的平方 (x−μ)ᵀΣ⁻¹(x−μ)，其等值面是一个椭球体。Σ 的特征向量给出椭球的主轴方向，特征值决定各轴的"伸展量"。一维时退化为普通正态。',
        formula: 'f(\\mathbf{x}) = \\frac{1}{(2\\pi)^{d/2}|\\boldsymbol{\\Sigma}|^{1/2}}\\exp\\!\\left(-\\frac{1}{2}(\\mathbf{x}-\\boldsymbol{\\mu})^{\\top}\\boldsymbol{\\Sigma}^{-1}(\\mathbf{x}-\\boldsymbol{\\mu})\\right)',
        tip: '协方差矩阵 Σ 必须对称且半正定（所有特征值 ≥ 0），否则不是合法的分布参数。',
      },
      {
        title: '多元正态的关键性质',
        body: '① 线性变换封闭：若 X ~ N(μ, Σ)，A 是矩阵，b 是向量，则 AX+b ~ N(Aμ+b, AΣAᵀ)。② 边缘分布仍是正态：任意分量的子集仍服从多元正态（直接读对应的 μ₁、Σ₁₁）。③ 条件分布仍是正态，且条件均值是观测值的线性函数（高斯过程、卡尔曼滤波的基础）。④ 不相关等价于独立（仅对正态成立！一般分布中不成立）。',
        tip: '多元正态的不相关 ↔ 独立是它的特殊性质，不能推广到一般分布。',
      },
      {
        title: '协方差矩阵的三种典型形式与条件分布公式',
        body: '协方差矩阵形式决定等值面形状：Σ = σ²I → 各维独立同方差 → 球形等值面；Σ = diag(σ₁², …, σ_d²) → 各维独立方差不同 → 轴对齐椭球；一般正定矩阵 → 各维相关 → 旋转椭球。将 X 分块为 (X₁, X₂)，给定 X₂ = x₂，X₁ 的条件分布：条件均值 = μ₁ + Σ₁₂Σ₂₂⁻¹(x₂−μ₂)（线性预测）；条件协方差 = Σ₁₁ − Σ₁₂Σ₂₂⁻¹Σ₂₁（Schur 补，比无条件方差更小）。',
        formula: '\\boldsymbol{\\mu}_{1\\mid 2} = \\boldsymbol{\\mu}_1 + \\boldsymbol{\\Sigma}_{12}\\boldsymbol{\\Sigma}_{22}^{-1}(\\mathbf{x}_2 - \\boldsymbol{\\mu}_2), \\quad \\boldsymbol{\\Sigma}_{1\\mid 2} = \\boldsymbol{\\Sigma}_{11} - \\boldsymbol{\\Sigma}_{12}\\boldsymbol{\\Sigma}_{22}^{-1}\\boldsymbol{\\Sigma}_{21}',
        reveal: {
          q: '(X₁,X₂) ~ N(μ,Σ)，Σ = [[4,2],[2,9]]，μ=(1,3)ᵀ，给定 X₂=6，X₁ 的条件期望是？',
          a: 'μ₁|₂ = 1 + (2)(1/9)(6−3) = 1 + 6/9 = 1 + 2/3 = 5/3 ≈ 1.67。观测到 X₂=6 > μ₂=3，正相关（Σ₁₂=2 > 0）使条件期望从 1 上升到 5/3。',
        },
      },
    ],
    questions: [
      {
        type: 'input',
        prompt: '(X₁,X₂,X₃) ~ Multinomial(10, (0.5, 0.3, 0.2))，E[X₂] = ？（填整数）',
        accept: ['3'],
        explain: 'E[Xᵢ] = npᵢ = 10 × 0.3 = 3。多项分布各分量边缘期望 = n × 对应概率。',
      },
      {
        type: 'choice',
        prompt: '多元正态分布 X ~ N(μ, Σ) 中，若 Σ = σ²I（单位矩阵乘以标量），则各分量之间',
        options: [
          '完全相关，协方差等于 σ²',
          '独立且方差相同，等值面为球形',
          '独立但方差不同，等值面为轴对齐椭球',
          '相关，椭球沿 45° 旋转',
        ],
        answer: 1,
        explain: 'Σ = σ²I 时，协方差矩阵为对角阵且对角元素相同，各分量独立（不相关↔独立对正态成立）且方差均为 σ²，等值面为球形。',
      },
      {
        type: 'judge',
        prompt: '多项分布 Multinomial(n, p) 中，Cov(Xᵢ, Xⱼ) > 0（i ≠ j）。',
        answer: false,
        explain: 'Cov(Xᵢ, Xⱼ) = −npᵢpⱼ < 0（i ≠ j）。总量 ΣXᵢ = n 固定，一类多了其他类必然少，因此不同类别之间负相关。',
      },
      {
        type: 'choice',
        prompt: '多元正态的下列性质中，哪条不能推广到一般分布？',
        options: [
          '边缘分布存在',
          '线性变换后仍有均值向量和协方差矩阵',
          '不相关等价于独立',
          '条件分布的期望存在',
        ],
        answer: 2,
        explain: '对一般分布，不相关（Cov=0）不等于独立。仅对多元正态，不相关 ↔ 独立成立。这是正态分布的特殊性质，是一个常见的易错点。',
      },
      {
        type: 'match',
        prompt: '将 Σ 的结构与等值面形状匹配',
        left: ['Σ = σ²I', 'Σ = diag(σ₁²,σ₂²)', '一般正定矩阵'],
        right: ['旋转椭球（主轴非坐标轴方向）', '球形（各方向伸展相同）', '轴对齐椭球（主轴平行坐标轴）'],
        explain: '单位矩阵倍数→球形；对角矩阵→轴对齐椭球；一般正定→旋转椭球（特征向量方向是主轴）。',
      },
      {
        type: 'judge',
        prompt: '多元正态分布的任意子集（边缘分布）仍服从多元正态分布。',
        answer: true,
        explain: '这是多元正态的重要性质：任意子集 X₁ 的边缘分布是 N(μ₁, Σ₁₁)，直接读取对应的均值向量和协方差矩阵子块即可。',
      },
    ],
  },
  {
    id: 't10-b',
    title: 'Dirichlet 分布与抽样分布',
    subtitle: '概率向量的先验，以及卡方、t、F 分布在统计推断中的角色',
    intro: [
      {
        title: 'Dirichlet 分布：概率向量的分布',
        body: 'Beta 分布定义在 (0,1) 上，是二项分布成功概率的先验。Dirichlet 分布是 Beta 的多类推广：定义在 k−1 维单纯形（{p : pᵢ > 0, Σpᵢ = 1}）上，参数 α = (α₁,…,αₖ)（αᵢ > 0）。PDF：Γ(Σαᵢ)/ΠΓ(αᵢ) × Πpᵢ^{αᵢ−1}。特殊情形：k=2 时退化为 Beta(α₁, α₂)；α = (1,…,1) 时是单纯形上的均匀分布。',
        formula: 'f(\\mathbf{p}; \\boldsymbol{\\alpha}) = \\frac{\\Gamma\\!\\bigl(\\sum_i \\alpha_i\\bigr)}{\\prod_i \\Gamma(\\alpha_i)} \\prod_{i=1}^k p_i^{\\alpha_i - 1}, \\quad p_i > 0, \\sum_i p_i = 1',
        tip: 'k=2 时 Dirichlet 退化为 Beta，α₁ 对应 β_Beta 的 α，α₂ 对应 β_Beta 的 β。',
      },
      {
        title: 'Dirichlet 分布的参数直觉与均值',
        body: '将 αᵢ 理解为"伪计数"：αᵢ 越大，pᵢ 的期望越高。令 α₀ = Σαᵢ（浓度参数）：α₀ 大 → 分布集中在均值附近（确定性高）；α₀ 小（接近 0）→ 分布集中在单纯形顶点（稀疏，某一类概率接近 1）。期望：E[pᵢ] = αᵢ/α₀。方差：Var(pᵢ) = αᵢ(α₀−αᵢ)/(α₀²(α₀+1))，随 α₀ 增大而减小（越来越确定）。',
        formula: 'E[p_i] = \\frac{\\alpha_i}{\\alpha_0}, \\quad \\alpha_0 = \\sum_{j=1}^k \\alpha_j; \\quad \\mathrm{Cov}(p_i, p_j) = \\frac{-\\alpha_i\\alpha_j}{\\alpha_0^2(\\alpha_0+1)}',
        tip: '浓度参数 α₀ 越大，分布越集中（确定性越强），类比 Beta 中先验样本量越大，后验越窄。',
      },
      {
        title: 'Dirichlet 是多项分布的共轭先验',
        body: '最重要的性质：设先验 p ~ Dir(α)，观测 n 次试验中第 i 类出现 xᵢ 次（x ~ Multinomial(n, p)），则后验 p | x ~ Dir(α + x)，即 Dir(α₁+x₁, …, αₖ+xₖ)。推导思路：后验 ∝ 似然 × 先验 ∝ Πpᵢ^{xᵢ} × Πpᵢ^{αᵢ−1} = Πpᵢ^{(αᵢ+xᵢ)−1}，恰好是 Dir(α+x) 的核密度。应用：文本建模中词频的贝叶斯估计，LDA 主题模型的先验。',
        formula: '\\mathbf{p} \\sim \\mathrm{Dir}(\\boldsymbol{\\alpha}),\\ \\mathbf{x} \\sim \\mathrm{Multinomial}(n,\\mathbf{p}) \\implies \\mathbf{p}\\mid\\mathbf{x} \\sim \\mathrm{Dir}(\\boldsymbol{\\alpha}+\\mathbf{x})',
        tip: '共轭性的价值：先验与后验属于同一分布族，更新只需"加计数"，计算极其简洁。',
      },
      {
        title: '分布变换：Jacobian 行列式',
        body: '设 Y = g(X) 是可逆变换，X 的 PDF 为 f_X。Y 的 PDF 为 f_Y(y) = f_X(g⁻¹(y)) · |det J_{g⁻¹}(y)|，其中 Jacobian 行列式 |det J| 是变换在局部的体积缩放比例。概率密度 = 概率质量/体积，变换后需要用 Jacobian 修正。例：X ~ Exp(λ/2) 等价于 χ²(2) = 2X ~ Gamma(1, 1/2)；Box-Muller 变换从均匀分布生成正态分布。',
        formula: 'f_Y(y) = f_X\\!\\left(g^{-1}(y)\\right) \\cdot \\left|\\det \\mathbf{J}_{g^{-1}}(y)\\right|',
        tip: 'Jacobian 行列式度量变换的"体积伸缩比"——概率密度随体积变化而反向缩放。',
      },
      {
        title: '样本均值与样本方差的分布',
        body: '设 X₁,…,Xₙ i.i.d. ~ N(μ,σ²)，X̄ = (1/n)ΣXᵢ，S² = Σ(Xᵢ−X̄)²/(n−1)。结论：① X̄ ~ N(μ, σ²/n)；② (n−1)S²/σ² ~ χ²(n−1)（Cochran 定理，利用正交变换分解平方和）；③ X̄ 与 S² 独立（仅正态总体）；④ T = (X̄−μ)/(S/√n) ~ t(n−1)（σ 未知时用 S 代替 σ）。自由度 n−1 不是 n：因为 Σ(Xᵢ−X̄) = 0 这个约束消耗了 1 个自由度。',
        formula: '\\bar{X} \\sim \\mathcal{N}\\!\\left(\\mu, \\frac{\\sigma^2}{n}\\right); \\quad \\frac{(n-1)S^2}{\\sigma^2} \\sim \\chi^2(n-1); \\quad T = \\frac{\\bar{X}-\\mu}{S/\\sqrt{n}} \\sim t(n-1)',
        tip: 'X̄ 与 S² 独立是正态总体独有的性质，正是由正态分布的旋转不变性决定的。',
      },
      {
        title: '两样本问题与 F 分布',
        body: '两个正态总体 X ~ N(μ₁,σ₁²)（样本量 m）和 Y ~ N(μ₂,σ₂²)（样本量 n）独立：① 均值差：X̄−Ȳ ~ N(μ₁−μ₂, σ₁²/m+σ₂²/n)；② 方差比：若 σ₁²=σ₂²，则 S_X²/S_Y² ~ F(m−1, n−1)（用于检验方差是否相等）；③ 等方差合并 t 检验：合并方差 S_p² = [(m−1)S_X²+(n−1)S_Y²]/(m+n−2)，统计量 ~ t(m+n−2)。关键关系：t(n)² = F(1, n)。',
        reveal: {
          q: 'X₁,…,X₁₆ i.i.d. ~ N(μ, 9)，σ=3，X̄ 的分布是？T = (X̄−μ)/(S/4) 服从什么分布？',
          a: 'X̄ ~ N(μ, 9/16)（方差 = σ²/n = 9/16，标准差 = 3/4）。由于 σ 未知用 S 代替，T = (X̄−μ)/(S/√16) = (X̄−μ)/(S/4) ~ t(n−1) = t(15)，自由度 = 16−1 = 15。',
        },
      },
      {
        title: '五类多维/抽样分布的选择逻辑',
        body: '① 多个类别的计数（n 次试验，k 类）→ 多项 Multinomial(n, p)；② 多维连续随机向量 → 多元正态 N(μ, Σ)；③ 概率向量的先验（单纯形上）→ Dirichlet Dir(α)；④ 标准正态平方和 → χ²(n)；⑤ 正态总体均值推断，σ 未知 → t(n−1)；⑥ 两组方差之比 → F(m,n)。记忆口诀：计数多类→多项；连续多维→多元正态；概率先验→Dirichlet；平方和→χ²；σ 未知推断→t；方差比→F。',
        tip: 'χ²、t、F 三者都由标准正态导出：χ²是平方和，t是正态除以χ²根号，F是两个χ²比。',
      },
    ],
    questions: [
      {
        type: 'choice',
        prompt: 'p ~ Dir(2, 5, 3)（三类，α₀=10），E[p₂] = ？',
        options: ['5/10 = 0.5', '5/8', '2/10 = 0.2', '3/10 = 0.3'],
        answer: 0,
        explain: 'E[pᵢ] = αᵢ/α₀ = 5/(2+5+3) = 5/10 = 0.5。Dirichlet 分布每个分量的期望是其参数除以总参数之和。',
      },
      {
        type: 'judge',
        prompt: 'Dirichlet 分布是多项分布成功概率向量 p 的共轭先验，因此先验 Dir(α) 与后验 Dir(α+x) 属于同一分布族。',
        answer: true,
        explain: '共轭先验的定义：先验与后验属于同一分布族。Dirichlet 是多项分布的共轭先验，观测后只需将 αᵢ 加上对应类别的计数 xᵢ，后验仍是 Dirichlet 分布。',
      },
      {
        type: 'input',
        prompt: 'X₁,…,X₂₅ i.i.d. ~ N(μ, σ²)，样本方差 S² 对应的 (n−1)S²/σ² 服从 χ²(n)，n = ？（填整数）',
        accept: ['24'],
        explain: '自由度 = n−1 = 25−1 = 24。一个自由度被均值估计"消耗"（约束 Σ(Xᵢ−X̄)=0），所以卡方分布的自由度是 n−1 而不是 n。',
      },
      {
        type: 'choice',
        prompt: 'Dirichlet(α₁, α₂) 退化为哪种分布？',
        options: ['多项分布', 'Beta(α₁, α₂)', '均匀分布 U(0,1)', '正态分布'],
        answer: 1,
        explain: 'k=2 时，Dirichlet(α₁, α₂) 退化为 Beta(α₁, α₂)，因为两类情形的概率向量 (p₁, p₂) 满足 p₁+p₂=1，只有一个自由变量 p₁ ∈ (0,1)。',
      },
      {
        type: 'match',
        prompt: '将每种统计情境与对应的检验分布匹配',
        left: ['σ 已知，检验正态总体均值', 'σ 未知，小样本检验正态总体均值', '检验两正态总体方差是否相等', 'n 个独立标准正态平方和'],
        right: ['χ²(n)', 'N(0,1)', 'F(m−1, n−1)', 't(n−1)'],
        explain: 'σ 已知用正态；σ 未知用 t；方差比用 F；平方和是 χ²。三大抽样分布各司其职。',
      },
      {
        type: 'judge',
        prompt: 'X̄ 与 S² 独立这一结论对所有总体分布都成立，不仅限于正态总体。',
        answer: false,
        explain: 'X̄ 与 S² 独立是正态总体的特有性质（由 Cochran 定理保证，依赖正态分布的旋转不变性）。对均匀分布、指数分布等非正态总体，X̄ 与 S² 一般不独立。',
      },
      {
        type: 'choice',
        prompt: 'T = Z / √(χ²(8)/8)，其中 Z ~ N(0,1) 与 χ²(8) 独立，则 T 服从',
        options: ['N(0,1)', 't(8)', 't(7)', 'F(1,8)'],
        answer: 1,
        explain: 't 分布的定义：t(n) = Z/√(χ²(n)/n)，分子为标准正态，分母为卡方除以自由度开根号，两者独立。这里 n=8，所以 T ~ t(8)。',
      },
      {
        type: 'input',
        prompt: 'p ~ Dir(1,1,1)（均匀 Dirichlet），E[p₁] = ？（化简分数）',
        accept: ['1/3'],
        explain: 'E[p₁] = α₁/α₀ = 1/(1+1+1) = 1/3。均匀 Dirichlet（所有 αᵢ = 1）是单纯形上的均匀先验，各类期望相等。',
      },
    ],
  },
]
